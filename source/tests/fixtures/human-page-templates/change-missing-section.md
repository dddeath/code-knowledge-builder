# 会话检索行为变更

## 修改内容

会话内复用同一个检索进程。

## 修改时间

合并时间由验证记录提供。

## 修改方式

生命周期管理负责创建和释放进程。

## 关联特性

与检索回退直接关联。

## 验证结果与适用边界

连续检索通过；缺少结束事件时需要显式关闭。

## 关键源码范围

- `scripts/ckb_core/session_stdio.py`：负责会话生命周期。
