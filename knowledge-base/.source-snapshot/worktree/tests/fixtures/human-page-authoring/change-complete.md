# 页面编写命令变更

## 修改内容
页面编写命令生成完整候选并执行合同检查。

## 修改时间
合并时间记录在验证清单。

## 修改原因
原流程缺少按页面类型收集字段的入口。

## 修改方式
`human_page_authoring` 负责骨架、渲染和检查。

## 关联特性
该入口与冻结模板注册表直接关联。

## 验证结果与适用边界
固定样例通过；结论只覆盖 schema 化输入。

## 关键源码范围
- `human_page_authoring.py`：承担确定性页面编写。
