# 建立人类页面模板与稳定维护入口

标签：#类型/变更

## 修改内容

本次修改为人类知识库补齐了统一的页面写作合同，并把“提出模板、人工审阅、按模板创建页面、通过稳定提示词复现维护动作”连成一条受控流程。人类首先看到页面应解决的问题、何时需要阅读、修改入口和当前边界；完整实现实体仍留在机器知识层。

覆盖的页面类型包括变更、分析、实验、踩坑、会话、学习笔记、参考资料与源码说明等十四类。模板给出必填章节和允许补充的内容，不要求每页展示全部内部实体。

## 修改时间

2026 年 9 月 2 日。

## 修改原因

此前不同页面依赖撰写者自行组织内容，容易出现实现细节过量、结论入口不清和同类页面结构漂移。稳定提示词也缺少与页面模板的统一约束，导致人类即使知道要完成什么任务，也难以让 Agent 稳定复现同一维护动作。

## 修改方式

修改集中在四个关键入口：

- 页面模板合同规定各类页面必须回答的问题；
- 模板提案把新增页面类型先交给人类审阅，不直接扩张正式模板；
- 页面创建引导根据已确认模板生成内容，并保留人工审阅环节；
- 维护提示词把常见任务整理为可直接交给 Agent 的稳定输入。

## 涉及的其他特性

这些修改与人类首页导航、工作记录投影、Agent Policy、检索包和维护审计相连。模板负责页面内部可理解性，首页与索引负责可发现性，机器知识层继续保存完整源码实体和关系。

## 验证结果

合并后的完整回归测试共 260 项，全部通过；页面模板、模板提案、页面创建和维护提示词的专项测试也在集成分支重新通过。稳定知识库随后以固定源码快照在隔离 staging 中重建并接受独立审计。

## 当前边界

模板只能约束新建或受控重写的页面。现有五个可读性不足的 change 页仍需使用安全的正文替换语义重写；当前记录接口只有追加语义，因此本次没有直接改写旧页，避免重复正文进入人类知识库。

## 相关知识页

- [[get_human_page_template]]

## 源码入口

- [打开源码：scripts/ckb_core/human_page_templates.py 第 598 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/human_page_templates.py:598:1)  `scripts/ckb_core/human_page_templates.py:598-616`
