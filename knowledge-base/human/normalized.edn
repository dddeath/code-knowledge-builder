{:pages-and-blocks [
{:page {:block/title "AgentProtocolBatchApplyTests"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`AgentProtocolBatchApp...` 是 `tests/test_agent_protocol_batch.py` 第 272-685 行定义的类，本页绑定该固定源码范围。 该类作为可执行验证入口，检查标识符 `AgentProtocolBatchApplyTests` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `AgentProtocolBatchApplyTests` 的说明。"} {:block/title "源码入口：[打开源码：tests/test_agent_protocol_batch.py 第 272 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_agent_protocol_batch.py:272:1)  `tests/test_agent_protocol_batch.py:272-685`"} {:block/title "内部实现：AgentProtocolBatchApplyTests.test_current_version_fixture_is_audited_and_idempotently_skipped — `AgentProtocolBatchApp...` 是第 273-284 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchApplyTests.test_apply_rejects_plan_target_drift_before_transaction — `AgentProtocolBatchApp...` 是第 286-300 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchApplyTests.test_apply_updates_protocol_contract_and_preserves_user_bytes — `AgentProtocolBatchApp...` 是第 302-349 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchApplyTests.test_partial_failure_restores_failed_project_and_audits_each_result — `AgentProtocolBatchApp...` 是第 351-391 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchApplyTests.test_partial_failure_restores_failed_project_and_audits_each_result.selective_audit — `AgentProtocolBatchApp...` 是第 371-372 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchApplyTests.test_interrupted_apply_restores_baseline_then_resumes — `AgentProtocolBatchApp...` 是第 393-423 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchApplyTests.test_interrupted_apply_restores_baseline_then_resumes.interrupt_after_first_write — `AgentProtocolBatchApp...` 是第 402-408 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchApplyTests.test_existing_output_lock_reports_concurrent_failure_without_writes — `AgentProtocolBatchApp...` 是第 425-438 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchApplyTests.test_live_cross_process_owner_is_not_stolen_after_stale_threshold — `AgentProtocolBatchApp...` 是第 440-489 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchApplyTests.test_dead_cross_process_owner_is_recovered_only_after_stale_threshold — `AgentProtocolBatchApp...` 是第 491-531 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchApplyTests.test_corrupt_pid_reused_unverifiable_and_release_drift_are_classified — `AgentProtocolBatchApp...` 是第 533-607 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchApplyTests.test_single_project_rollback_restores_bytes_modes_and_source_version — `AgentProtocolBatchApp...` 是第 609-638 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchApplyTests.test_subset_rollback_keeps_unselected_success_applied — `AgentProtocolBatchApp...` 是第 640-667 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchApplyTests.test_rollback_refuses_post_batch_user_drift — `AgentProtocolBatchApp...` 是第 669-685 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[AgentProtocolBatchApplyTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[audit_agent_protocol]]。"} {:block/title "协作：实现时会用到 [[audit_output_contract]]。"} {:block/title "协作：实现时会用到 [[create_batch_plan]]。"} {:block/title "协作：实现时会用到 [[create_batch_plan 与 ProtocolRelease 的协作实现]]。"} {:block/title "协作：[[AgentProtocolBatchApplyTests 等测试场景]] 汇总了本页。"} {:block/title "协作：[[CkbError]] 关联到这里的验证场景。"} {:block/title "协作：[[CkbError 与 DependencyError 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[_Transport.close]] 关联到这里的验证场景。"} {:block/title "协作：[[append]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_agent_protocol]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_agent_protocol 与 _default_python 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_feedback]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_feedback 与 _contains_chinese 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_operation_journal 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_output_contract]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_output_contract 与 _default_ckb 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_work_record_index]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_work_record_index 与 _contains_chinese 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[command]] 关联到这里的验证场景。"} {:block/title "协作：[[create_batch_plan]] 关联到这里的验证场景。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[execute]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[search_terms]] 关联到这里的验证场景。"} {:block/title "协作：[[start_scope_extension 与 _error 的协作实现]] 关联到这里的验证场景。"}]}
{:page {:block/title "AgentProtocolBatchApplyTests 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/test_agent_protocol_batch.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `test_agent_protocol_batch.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/test_agent_protocol_batch.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/test_agent_protocol_batch.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_agent_protocol_batch.py:1:1)  `tests/test_agent_protocol_batch.py:1-690`"} {:block/title "内部实现：tree_digest — `tree_digest` 是第 38-44 行的函数，供所属页面定位实现。"} {:block/title "内部实现：create_protocol_fixture — `create_protocol_fixture` 是第 47-118 行的函数，供所属页面定位实现。"} {:block/title "内部实现：install_fake_plugin — `install_fake_plugin` 是第 121-126 行的函数，供所属页面定位实现。"} {:block/title "内部实现：outside_managed_bytes — `outside_managed_bytes` 是第 129-132 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchMatrixTests — `AgentProtocolBatchMat...` 是第 135-158 行的类，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchMatrixTests.test_frozen_historical_fixtures_match_matrix — `AgentProtocolBatchMat...` 是第 136-151 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchMatrixTests.test_unknown_and_backward_paths_are_rejected — `AgentProtocolBatchMat...` 是第 153-158 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchPlanTests — `AgentProtocolBatchPla...` 是第 161-269 行的类，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchPlanTests.test_plan_is_byte_stable_and_does_not_write_target — `AgentProtocolBatchPla...` 是第 162-178 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchPlanTests.test_manifest_and_project_failures_have_stable_categories — `AgentProtocolBatchPla...` 是第 180-202 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchPlanTests.test_required_failure_fixtures_are_classified_without_guessing — `AgentProtocolBatchPla...` 是第 204-253 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AgentProtocolBatchPlanTests.test_duplicate_and_nested_outputs_fail_manifest_preflight — `AgentProtocolBatchPla...` 是第 255-269 行的函数，供所属页面定位实现。"} {:block/title "协作：主要代码单元是 [[AgentProtocolBatchApplyTests]]。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[create_batch_plan]]。"} {:block/title "协作：实现时会用到 [[create_batch_plan 与 ProtocolRelease 的协作实现]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：[[AgentProtocolBatchApplyTests]] 会使用这里提供的行为。"} {:block/title "协作：[[CkbError]] 关联到这里的验证场景。"} {:block/title "协作：[[CkbError 与 DependencyError 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[_Transport.close]] 关联到这里的验证场景。"} {:block/title "协作：[[append]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_agent_protocol 与 _default_python 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[command]] 关联到这里的验证场景。"} {:block/title "协作：[[create_batch_plan]] 关联到这里的验证场景。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[execute]] 关联到这里的验证场景。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[search_terms]] 关联到这里的验证场景。"} {:block/title "协作：[[search_terms 与 _split_camel 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[start_scope_extension 与 _error 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "AutomationTest.register"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`AutomationTest.register` 是 `tests/test_automation.py` 第 78-80 行定义的函数，本页绑定该固定源码范围。 该函数作为可执行验证入口，检查标识符 `register` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `register` 的说明。"} {:block/title "源码入口：[打开源码：tests/test_automation.py 第 78 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_automation.py:78:1)  `tests/test_automation.py:78-80`"} {:block/title "协作：实现时会用到 [[ingest_event 与 default_registry_path 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.register 等测试场景]] 汇总了本页。"} {:block/title "协作：[[CkbError]] 关联到这里的验证场景。"} {:block/title "协作：[[CkbError 与 DependencyError 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[_Transport.close]] 关联到这里的验证场景。"} {:block/title "协作：[[append]] 关联到这里的验证场景。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[command]] 关联到这里的验证场景。"} {:block/title "协作：[[execute]] 关联到这里的验证场景。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[source_files 与 sha256 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "AutomationTest.register 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/test_automation.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `test_automation.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/test_automation.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/test_automation.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_automation.py:1:1)  `tests/test_automation.py:1-890`"} {:block/title "内部实现：git — `git` 是第 42-45 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest — `AutomationTest` 是第 48-885 行的类，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.setUp — `AutomationTest.setUp` 是第 49-73 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.tearDown — `AutomationTest.tearDown` 是第 75-76 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.event — `AutomationTest.event` 是第 82-92 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_event_path_text_maps_wsl_mounts_for_windows_runtime — `AutomationTest.test_e...` 是第 94-104 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_windows_runtime_matches_wsl_cwd_to_registered_workspace — `AutomationTest.test_w...` 是第 107-117 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_project_opt_in_and_hook_output — `AutomationTest.test_p...` 是第 119-127 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_registered_session_stays_idle_until_skill_is_explicitly_applied — `AutomationTest.test_r...` 是第 129-178 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_agent_activation_command_uses_harness_session_and_workspace — `AutomationTest.test_a...` 是第 180-197 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_redaction_idempotency_change_capture_and_pending_review — `AutomationTest.test_r...` 是第 199-258 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_claude_without_turn_ids_allows_repeated_prompt_after_completion — `AutomationTest.test_c...` 是第 260-270 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_stop_detects_further_change_to_file_dirty_at_session_start — `AutomationTest.test_s...` 是第 272-289 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_nested_untracked_project_bounds_git_status_and_uses_project_relative_paths — `AutomationTest.test_n...` 是第 291-354 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_workspace_root_maps_parent_task_to_nested_repository — `AutomationTest.test_w...` 是第 356-422 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_version_one_registry_is_read_compatibly — `AutomationTest.test_v...` 是第 424-449 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_version_two_registry_upgrades_to_session_activation_contract — `AutomationTest.test_v...` 是第 451-490 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_opencode_and_generic_normalization — `AutomationTest.test_o...` 是第 492-538 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_gemini_copilot_and_cursor_normalization — `AutomationTest.test_g...` 是第 540-569 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_concurrent_hooks_are_serialized_without_event_loss — `AutomationTest.test_c...` 是第 571-597 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_failed_spool_is_retained_and_retryable — `AutomationTest.test_f...` 是第 599-607 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest._prepare_human_projection — `AutomationTest._prepa...` 是第 609-630 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_agent_review_promotes_one_chinese_human_note — `AutomationTest.test_a...` 是第 632-737 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_render_all_harness_integrations — `AutomationTest.test_r...` 是第 739-797 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_codex_windows_python_uses_wsl_launch_path_for_posix_command — `AutomationTest.test_c...` 是第 799-824 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_codex_windows_bridge_uses_cmd_and_forward_slash_launcher_path — `AutomationTest.test_c...` 是第 826-833 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_automation_fts_finds_pending_machine_record — `AutomationTest.test_a...` 是第 835-849 行的函数，供所属页面定位实现。"} {:block/title "内部实现：AutomationTest.test_machine_retrieval_and_changes_include_pending_automation — `AutomationTest.test_m...` 是第 851-885 行的函数，供所属页面定位实现。"} {:block/title "协作：主要代码单元是 [[AutomationTest.register]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[doctor_report 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[ingest_event]]。"} {:block/title "协作：实现时会用到 [[ingest_event 与 default_registry_path 的协作实现]]。"} {:block/title "协作：实现时会用到 [[render_integration 与 _looks_windows 的协作实现]]。"} {:block/title "协作：[[AutomationTest.register]] 关联到这里的验证场景。"} {:block/title "协作：[[CkbError]] 关联到这里的验证场景。"} {:block/title "协作：[[CkbError 与 DependencyError 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[_Transport.close]] 关联到这里的验证场景。"} {:block/title "协作：[[_Transport.close 与 _StartGate 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[append]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_feedback 与 _contains_chinese 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_gap_register 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_operation_journal 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_references 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_work_record_index]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_work_record_index 与 _contains_chinese 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[command]] 关联到这里的验证场景。"} {:block/title "协作：[[doctor_report 与 _version_matches 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[emit]] 关联到这里的验证场景。"} {:block/title "协作：[[execute]] 关联到这里的验证场景。"} {:block/title "协作：[[ingest_event]] 关联到这里的验证场景。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[normalize]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[render_integration]] 关联到这里的验证场景。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[run_keyword_provider]] 关联到这里的验证场景。"} {:block/title "协作：[[run_keyword_provider 与 KeywordProviderConfig 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[search_terms]] 关联到这里的验证场景。"} {:block/title "协作：[[search_terms 与 _split_camel 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[start_scope_extension 与 _error 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "Box 等测试场景（explicit-instantiation-valid 测试）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/fixtures/cpp-parser-scons/explicit-instantiation-valid.cpp` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `explicit-instantiation-valid.cpp` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/fixtures/cpp-parser-scons/explicit-instantiation-valid.cpp` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/fixtures/cpp-parser-scons/explicit-instantiation-valid.cpp 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/fixtures/cpp-parser-scons/explicit-instantiation-valid.cpp:1:1)  `tests/fixtures/cpp-parser-scons/explicit-instantiation-valid.cpp:1-5`"} {:block/title "内部实现：run — `run` 是第 3-3 行的函数，供所属页面定位实现。"} {:block/title "协作：主要代码单元是 [[Box（explicit-instantiation-valid 测试）]]。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "Box 等测试场景（main 测试）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/fixtures/cpp-parser-scons/scons-project/src/main.cpp` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `main.cpp` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/fixtures/cpp-parser-scons/scons-project/src/main.cpp` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/fixtures/cpp-parser-scons/scons-project/src/main.cpp 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/fixtures/cpp-parser-scons/scons-project/src/main.cpp:1:1)  `tests/fixtures/cpp-parser-scons/scons-project/src/main.cpp:1-9`"} {:block/title "内部实现：public_service — `public_service` 是第 6-8 行的函数，供所属页面定位实现。"} {:block/title "协作：主要代码单元是 [[Box（main 测试）]]。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[execute 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "Box（explicit-instantiation-valid 测试）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`Box` 是 `tests/fixtures/cpp-parser-scons/explicit-instantiation-valid.cpp` 第 1-1 行定义的类，本页绑定该固定源码范围。 该类作为可执行验证入口，检查标识符 `Box` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `Box` 的说明。"} {:block/title "源码入口：[打开源码：tests/fixtures/cpp-parser-scons/explicit-instantiation-valid.cpp 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/fixtures/cpp-parser-scons/explicit-instantiation-valid.cpp:1:1)  `tests/fixtures/cpp-parser-scons/explicit-instantiation-valid.cpp:1-1`"} {:block/title "协作：[[Box 等测试场景（explicit-instantiation-valid 测试）]] 汇总了本页。"}]}
{:page {:block/title "Box（main 测试）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`Box` 是 `tests/fixtures/cpp-parser-scons/scons-project/src/main.cpp` 第 2-2 行定义的类，本页绑定该固定源码范围。 该类作为可执行验证入口，检查标识符 `Box` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `Box` 的说明。"} {:block/title "源码入口：[打开源码：tests/fixtures/cpp-parser-scons/scons-project/src/main.cpp 第 2 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/fixtures/cpp-parser-scons/scons-project/src/main.cpp:2:1)  `tests/fixtures/cpp-parser-scons/scons-project/src/main.cpp:2-2`"} {:block/title "协作：[[Box 等测试场景（main 测试）]] 汇总了本页。"}]}
{:page {:block/title "ChineseRetrievalFixtureTests"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`ChineseRetrievalFixtu...` 是 `tests/test_chinese_retrieval_fixture.py` 第 11-42 行定义的类，本页绑定该固定源码范围。 该类作为可执行验证入口，检查标识符 `ChineseRetrievalFixtureTests` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `ChineseRetrievalFixtureTests` 的说明。"} {:block/title "源码入口：[打开源码：tests/test_chinese_retrieval_fixture.py 第 11 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_chinese_retrieval_fixture.py:11:1)  `tests/test_chinese_retrieval_fixture.py:11-42`"} {:block/title "内部实现：ChineseRetrievalFixtureTests.test_frozen_protocol_shape — `ChineseRetrievalFixtu...` 是第 12-21 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ChineseRetrievalFixtureTests.test_baseline_records_all_mechanical_fragments — `ChineseRetrievalFixtu...` 是第 23-35 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ChineseRetrievalFixtureTests.test_baseline_corpus_and_recall_are_complete — `ChineseRetrievalFixtu...` 是第 37-42 行的函数，供所属页面定位实现。"} {:block/title "协作：[[ChineseRetrievalFixtureTests 等测试场景]] 汇总了本页。"}]}
{:page {:block/title "ChineseRetrievalFixtureTests 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/test_chinese_retrieval_fixture.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `test_chinese_retrieval_fixture.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/test_chinese_retrieval_fixture.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/test_chinese_retrieval_fixture.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_chinese_retrieval_fixture.py:1:1)  `tests/test_chinese_retrieval_fixture.py:1-47`"} {:block/title "协作：主要代码单元是 [[ChineseRetrievalFixtureTests]]。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "CkbError"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`CkbError` 是 `scripts/ckb_core/common.py` 中负责为 CKB 输入或状态错误提供固定进程退出码的类。 它按源码所示的参数、条件分支和数据结构完成为 CKB 输入或状态错误提供固定进程退出码，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当路径、时间、JSON、哈希、进程与通用错误处理的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/common.py 第 24 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/common.py:24:1)  `scripts/ckb_core/common.py:24-25`"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[AgentProtocolBatchApplyTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[CkbError 与 DependencyError 的协作实现]] 汇总了本页。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[ScopeExtensionTest]] 会使用这里提供的行为。"} {:block/title "协作：[[SessionStdioLifecycleTests]] 会使用这里提供的行为。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[_Transport.close 与 _StartGate 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[append 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_agent_protocol 与 _default_python 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_feedback]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_feedback 与 _contains_chinese 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_gap_register 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_obsidian]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_operation_journal 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_references]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_references 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_work_record_index 与 _contains_chinese 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[bind_conversation]] 会使用这里提供的行为。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[create_batch_plan]] 会使用这里提供的行为。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[deployment_plan 与 skill_root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[doctor_report 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[keyword_provider_config]] 会使用这里提供的行为。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[load_page_config]] 会使用这里提供的行为。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[maintenance_check]] 会使用这里提供的行为。"} {:block/title "协作：[[maintenance_check 与 capability_matrix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[package_showcase]] 会使用这里提供的行为。"} {:block/title "协作：[[package_showcase 与 _parse_sample 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[preflight 与 git 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[refresh 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[run_keyword_benchmark 与 _text_list 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[run_keyword_provider]] 会使用这里提供的行为。"} {:block/title "协作：[[run_keyword_provider 与 KeywordProviderConfig 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[serve_stdio]] 会使用这里提供的行为。"} {:block/title "协作：[[serve_stdio 与 _write_line 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[start_scope_extension]] 会使用这里提供的行为。"} {:block/title "协作：[[start_scope_extension 与 _error 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session 与 _session_directory 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[sync_human_layer]] 会使用这里提供的行为。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "CkbError 与 DependencyError 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/common.py` 是 `scripts/ckb_core/common.py` 中负责汇总并提供路径、时间、JSON、哈希、进程与通用错误处理的文件入口。 它按源码所示的参数、条件分支和数据结构完成汇总并提供路径、时间、JSON、哈希、进程与通用错误处理，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当路径、时间、JSON、哈希、进程与通用错误处理的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/common.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/common.py:1:1)  `scripts/ckb_core/common.py:1-168`"} {:block/title "内部实现：DependencyError — 处理 `dependencyerror` 对应的数据与约束。"} {:block/title "内部实现：ReviewRequired — 处理 `reviewrequired` 对应的数据与约束。"} {:block/title "内部实现：AuditError — 处理 `auditerror` 对应的数据与约束。"} {:block/title "内部实现：StaleSourceError — 处理 `stalesourceerror` 对应的数据与约束。"} {:block/title "内部实现：utc_now — 处理 `now` 对应的数据与约束。"} {:block/title "内部实现：stable_id — 根据固定输入计算 `stable_id` 稳定标识。"} {:block/title "内部实现：sha256_file — 处理 `file` 对应的数据与约束。"} {:block/title "内部实现：json_load — 处理 `load` 对应的数据与约束。"} {:block/title "内部实现：json_write — 处理 `write` 对应的数据与约束。"} {:block/title "内部实现：background_process_options — 处理 `process_options` 对应的数据与约束。"} {:block/title "内部实现：run — 执行 `run` 对应的数据与约束。"} {:block/title "内部实现：command_version — 处理 `version` 对应的数据与约束。"} {:block/title "内部实现：clear_markers — 处理 `markers` 对应的数据与约束。"} {:block/title "内部实现：write_marker — 写入 `marker` 对应的数据与约束。"} {:block/title "内部实现：safe_title — 处理 `title` 对应的数据与约束。"} {:block/title "内部实现：path_inside — 生成 `inside` 对应的数据与约束。"} {:block/title "内部实现：safe_rmtree — 处理 `rmtree` 对应的数据与约束。"} {:block/title "内部实现：temp_directory — 处理 `directory` 对应的数据与约束。"} {:block/title "协作：主要代码单元是 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[ScopeExtensionTest]] 会使用这里提供的行为。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[_Transport.close 与 _StartGate 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_agent_protocol 与 _default_python 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_feedback 与 _contains_chinese 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_gap_register 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_obsidian 与 prepare_vault 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_operation_journal 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_references]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_references 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[bind_conversation]] 会使用这里提供的行为。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[command 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[deployment_plan]] 会使用这里提供的行为。"} {:block/title "协作：[[deployment_plan 与 skill_root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[doctor_report]] 会使用这里提供的行为。"} {:block/title "协作：[[doctor_report 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[maintenance_check]] 会使用这里提供的行为。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[package_showcase]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[preflight]] 会使用这里提供的行为。"} {:block/title "协作：[[preflight 与 git 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[refresh]] 会使用这里提供的行为。"} {:block/title "协作：[[refresh 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[register_obsidian_plugin]] 会使用这里提供的行为。"} {:block/title "协作：[[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[start_scope_extension]] 会使用这里提供的行为。"} {:block/title "协作：[[start_scope_extension 与 _error 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session 与 _session_directory 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[sync_human_layer]] 会使用这里提供的行为。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "CodeKnowledgeBuilderTests"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`CodeKnowledgeBuilderT...` 是 `tests/test_ckb.py` 第 199-2232 行定义的类，本页绑定该固定源码范围。 该类作为可执行验证入口，检查标识符 `CodeKnowledgeBuilderTests` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `CodeKnowledgeBuilderTests` 的说明。"} {:block/title "源码入口：[打开源码：tests/test_ckb.py 第 199 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_ckb.py:199:1)  `tests/test_ckb.py:199-2232`"} {:block/title "内部实现：CodeKnowledgeBuilderTests.setUp — `CodeKnowledgeBuilderT...` 是第 200-203 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.tearDown — `CodeKnowledgeBuilderT...` 是第 205-206 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_noninteractive_subprocesses_use_the_platform_background_flag — `CodeKnowledgeBuilderT...` 是第 208-222 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_bounded_machine_operation_journal_is_private_deduplicated_and_audited — `CodeKnowledgeBuilderT...` 是第 224-276 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_research_gap_register_is_machine_only_deduplicated_and_resolvable — `CodeKnowledgeBuilderT...` 是第 278-326 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_llm_wiki_capability_matrix_has_closed_four_state_boundaries — `CodeKnowledgeBuilderT...` 是第 328-342 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_reviewed_text_reference_is_searchable_revisioned_and_reversible — `CodeKnowledgeBuilderT...` 是第 344-553 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_empty_document_symbols_are_not_a_provider_failure — `CodeKnowledgeBuilderT...` 是第 555-562 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_python_attribute_accessors_stay_in_the_file_appendix — `CodeKnowledgeBuilderT...` 是第 564-598 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_stdio_retrieval_protocol_is_jsonl_and_errors_do_not_stop_server — `CodeKnowledgeBuilderT...` 是第 600-687 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_stdio_retrieval_protocol_is_jsonl_and_errors_do_not_stop_server.fake_retrieve — `CodeKnowledgeBuilderT...` 是第 607-617 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_stdio_retrieval_protocol_is_jsonl_and_errors_do_not_stop_server.fake_record — `CodeKnowledgeBuilderT...` 是第 619-621 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_record_explanation_writes_utf8_machine_evidence_not_analysis_page — `CodeKnowledgeBuilderT...` 是第 689-729 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_obsidian_plugin_package_can_be_registered_deployed_and_removed — `CodeKnowledgeBuilderT...` 是第 731-773 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_obsidian_plugin_deploy_falls_back_when_vault_directory_is_locked — `CodeKnowledgeBuilderT...` 是第 775-802 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_obsidian_output_contract_is_not_required_without_installed_plugin — `CodeKnowledgeBuilderT...` 是第 804-815 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_non_git_path_reminds_then_opt_in_creates_one_initial_commit — `CodeKnowledgeBuilderT...` 是第 817-917 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_unborn_git_repo_requires_opt_in_and_existing_dirty_repo_is_not_committed — `CodeKnowledgeBuilderT...` 是第 919-968 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_fast_run_can_bootstrap_non_git_source_and_stops_for_review — `CodeKnowledgeBuilderT...` 是第 970-1001 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_feedback_anchor_mirroring_audit_and_archive_are_deterministic — `CodeKnowledgeBuilderT...` 是第 1003-1101 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_work_record_index_covers_every_note_with_one_chinese_summary — `CodeKnowledgeBuilderT...` 是第 1103-1143 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_markdown_whole_repository_and_completion_gate — `CodeKnowledgeBuilderT...` 是第 1145-1689 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_local_scope_has_one_hop_boundary — `CodeKnowledgeBuilderT...` 是第 1691-1707 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_entry_scope_uses_fixed_snapshot_while_live_worktree_changes — `CodeKnowledgeBuilderT...` 是第 1709-1768 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_review_set_mismatch_fails — `CodeKnowledgeBuilderT...` 是第 1770-1782 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_runtime_plan_lite — `CodeKnowledgeBuilderT...` 是第 1784-1794 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_obsidian_companion_distribution_reuses_pinned_claudian — `CodeKnowledgeBuilderT...` 是第 1796-1856 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_required_format_duplicate_entry_and_syntax_stage — `CodeKnowledgeBuilderT...` 是第 1858-1872 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_oversized_file_splits_on_declarations_without_duplicate_ids — `CodeKnowledgeBuilderT...` 是第 1874-1893 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_both_projection_parity_with_cli_contract_double — `CodeKnowledgeBuilderT...` 是第 1895-1916 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_logseq_only_projection_has_format_neutral_agent_index — `CodeKnowledgeBuilderT...` 是第 1918-1941 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_english_only_agent_review_is_rejected — `CodeKnowledgeBuilderT...` 是第 1943-1967 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_navigation_page_quota_relation_budget_and_context_bundle — `CodeKnowledgeBuilderT...` 是第 1969-2005 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_page_configuration_controls_quotas_content_and_is_pinned — `CodeKnowledgeBuilderT...` 是第 2007-2118 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_csharp_project_selection_partial_types_and_generated_exclusions — `CodeKnowledgeBuilderT...` 是第 2120-2186 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_csharp_property_and_enum_land_in_class_or_file_aggregation — `CodeKnowledgeBuilderT...` 是第 2188-2216 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_fallback_standard_derivation_and_stable_ids — `CodeKnowledgeBuilderT...` 是第 2218-2232 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[audit_feedback 与 _contains_chinese 的协作实现]]。"} {:block/title "协作：实现时会用到 [[audit_gap_register 与 _root 的协作实现]]。"} {:block/title "协作：实现时会用到 [[audit_operation_journal 与 _root 的协作实现]]。"} {:block/title "协作：实现时会用到 [[doctor_report 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve]]。"} {:block/title "协作：[[AutomationTest.register]] 关联到这里的验证场景。"} {:block/title "协作：[[CkbError]] 关联到这里的验证场景。"} {:block/title "协作：[[CkbError 与 DependencyError 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[CodeKnowledgeBuilderTests 等测试场景]] 汇总了本页。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[_Transport.close]] 关联到这里的验证场景。"} {:block/title "协作：[[append]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_agent_protocol]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_agent_protocol 与 _default_python 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_feedback]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_feedback 与 _contains_chinese 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_gap_register]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_gap_register 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_obsidian]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_obsidian 与 prepare_vault 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_operation_journal]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_operation_journal 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_output_contract]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_output_contract 与 _default_ckb 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_references]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_references 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_work_record_index]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_work_record_index 与 _contains_chinese 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[command]] 关联到这里的验证场景。"} {:block/title "协作：[[doctor_report 与 _version_matches 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[execute]] 关联到这里的验证场景。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[maintenance_check 与 capability_matrix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[normalize]] 关联到这里的验证场景。"} {:block/title "协作：[[preflight 与 git 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[refresh]] 关联到这里的验证场景。"} {:block/title "协作：[[register_obsidian_plugin]] 关联到这里的验证场景。"} {:block/title "协作：[[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[run_keyword_provider]] 关联到这里的验证场景。"} {:block/title "协作：[[run_keyword_provider 与 KeywordProviderConfig 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[search_terms]] 关联到这里的验证场景。"} {:block/title "协作：[[search_terms 与 _split_camel 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[serve_stdio]] 关联到这里的验证场景。"} {:block/title "协作：[[serve_stdio 与 _write_line 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[source_files]] 关联到这里的验证场景。"} {:block/title "协作：[[start_scope_extension 与 _error 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 关联到这里的验证场景。"}]}
{:page {:block/title "CodeKnowledgeBuilderTests 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/test_ckb.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `test_ckb.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/test_ckb.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/test_ckb.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_ckb.py:1:1)  `tests/test_ckb.py:1-2339`"} {:block/title "内部实现：invoke — `invoke` 是第 64-81 行的函数，供所属页面定位实现。"} {:block/title "内部实现：write — `write` 是第 84-86 行的函数，供所属页面定位实现。"} {:block/title "内部实现：git — `git` 是第 89-92 行的函数，供所属页面定位实现。"} {:block/title "内部实现：make_repo — `make_repo` 是第 95-168 行的函数，供所属页面定位实现。"} {:block/title "内部实现：review_all — `review_all` 是第 171-196 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CppParserAndSconsTests — `CppParserAndSconsTests` 是第 2235-2334 行的类，供所属页面定位实现。"} {:block/title "内部实现：CppParserAndSconsTests.setUp — `CppParserAndSconsTest...` 是第 2238-2240 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CppParserAndSconsTests.tearDown — `CppParserAndSconsTest...` 是第 2242-2243 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CppParserAndSconsTests.parse_fixture — `CppParserAndSconsTest...` 是第 2245-2252 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CppParserAndSconsTests.test_cpp_conditional_compilation_valid_and_incomplete — `CppParserAndSconsTest...` 是第 2254-2263 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CppParserAndSconsTests.test_cpp_reference_direct_initialization_is_declaration_not_function — `CppParserAndSconsTest...` 是第 2265-2273 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CppParserAndSconsTests.test_cpp_explicit_template_instantiation_has_no_pseudo_entities — `CppParserAndSconsTest...` 是第 2275-2292 行的函数，供所属页面定位实现。"} {:block/title "内部实现：CppParserAndSconsTests.test_scons_fallback_is_auditable_and_compile_database_stays_exact — `CppParserAndSconsTest...` 是第 2294-2334 行的函数，供所属页面定位实现。"} {:block/title "协作：主要代码单元是 [[CodeKnowledgeBuilderTests]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[bind_reference]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[debug_value 等测试场景]]。"} {:block/title "协作：实现时会用到 [[doctor_report 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[parse_file]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.register]] 关联到这里的验证场景。"} {:block/title "协作：[[CkbError]] 关联到这里的验证场景。"} {:block/title "协作：[[CkbError 与 DependencyError 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[_Transport.close]] 关联到这里的验证场景。"} {:block/title "协作：[[_Transport.close 与 _StartGate 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[append]] 关联到这里的验证场景。"} {:block/title "协作：[[append 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_agent_protocol]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_agent_protocol 与 _default_python 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_gap_register 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_references]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_references 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[bind_conversation]] 关联到这里的验证场景。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[bind_reference]] 关联到这里的验证场景。"} {:block/title "协作：[[bind_reference 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[command]] 关联到这里的验证场景。"} {:block/title "协作：[[debug_value 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[doctor_report 与 _version_matches 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[execute]] 关联到这里的验证场景。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[initialize]] 关联到这里的验证场景。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[normalize]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[parser]] 关联到这里的验证场景。"} {:block/title "协作：[[preflight]] 关联到这里的验证场景。"} {:block/title "协作：[[preflight 与 git 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[refresh]] 关联到这里的验证场景。"} {:block/title "协作：[[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[run_keyword_provider 与 KeywordProviderConfig 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[search_terms]] 关联到这里的验证场景。"} {:block/title "协作：[[search_terms 与 _split_camel 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[start_scope_extension 与 _error 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "KeywordFallbackRetrievalWiringTests"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`KeywordFallbackRetrie...` 是 `tests/test_keyword_fallback.py` 第 198-349 行定义的类，本页绑定该固定源码范围。 该类作为可执行验证入口，检查标识符 `KeywordFallbackRetrievalWiringTests` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `KeywordFallbackRetrievalWiringTests` 的说明。"} {:block/title "源码入口：[打开源码：tests/test_keyword_fallback.py 第 198 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_keyword_fallback.py:198:1)  `tests/test_keyword_fallback.py:198-349`"} {:block/title "内部实现：KeywordFallbackRetrievalWiringTests.setUp — `KeywordFallbackRetrie...` 是第 199-204 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackRetrievalWiringTests.tearDown — `KeywordFallbackRetrie...` 是第 206-207 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackRetrievalWiringTests.test_default_result_is_returned_unchanged_and_provider_is_not_called — `KeywordFallbackRetrie...` 是第 209-217 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackRetrievalWiringTests.test_allow_mode_does_not_start_provider_after_passed_result — `KeywordFallbackRetrie...` 是第 219-235 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackRetrievalWiringTests.test_needs_source_read_uses_validated_terms_then_deterministic_selection — `KeywordFallbackRetrie...` 是第 237-281 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackRetrievalWiringTests.test_provider_failure_returns_original_result_with_structured_reason — `KeywordFallbackRetrie...` 是第 283-309 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackRetrievalWiringTests.test_stdio_exposes_the_same_nested_canonical_options — `KeywordFallbackRetrie...` 是第 311-349 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackRetrievalWiringTests.test_stdio_exposes_the_same_nested_canonical_options.fake_retrieve — `KeywordFallbackRetrie...` 是第 314-320 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[retrieve]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run_keyword_provider]]。"} {:block/title "协作：实现时会用到 [[run_keyword_provider 与 KeywordProviderConfig 的协作实现]]。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests 等测试场景]] 汇总了本页。"} {:block/title "协作：[[SourceLinkRenderer.uri]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_agent_protocol]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_agent_protocol 与 _default_python 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_feedback]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_feedback 与 _contains_chinese 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_output_contract]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_references]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_work_record_index]] 关联到这里的验证场景。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[maintenance_check 与 capability_matrix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[normalize]] 关联到这里的验证场景。"} {:block/title "协作：[[preflight 与 git 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[run_keyword_provider]] 关联到这里的验证场景。"} {:block/title "协作：[[run_keyword_provider 与 KeywordProviderConfig 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[search_terms]] 关联到这里的验证场景。"} {:block/title "协作：[[search_terms 与 _split_camel 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[serve_stdio]] 关联到这里的验证场景。"} {:block/title "协作：[[serve_stdio 与 _write_line 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[start_scope_extension 与 _error 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 关联到这里的验证场景。"}]}
{:page {:block/title "KeywordFallbackRetrievalWiringTests 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/test_keyword_fallback.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `test_keyword_fallback.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/test_keyword_fallback.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/test_keyword_fallback.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_keyword_fallback.py:1:1)  `tests/test_keyword_fallback.py:1-438`"} {:block/title "内部实现：fixture_output — `fixture_output` 是第 47-58 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackSchemaTests — `KeywordFallbackSchema...` 是第 61-103 行的类，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackSchemaTests.test_canonical_fixture_output_is_bounded_and_normalized — `KeywordFallbackSchema...` 是第 62-71 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackSchemaTests.test_invalid_json_is_rejected — `KeywordFallbackSchema...` 是第 73-75 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackSchemaTests.test_oversized_duplicate_injected_and_invalid_candidates_are_rejected — `KeywordFallbackSchema...` 是第 77-91 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackSchemaTests.test_canonical_failure_types_are_preserved_without_candidates — `KeywordFallbackSchema...` 是第 93-103 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackAdapterTests — `KeywordFallbackAdapte...` 是第 106-195 行的类，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackAdapterTests.setUp — `KeywordFallbackAdapte...` 是第 107-110 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackAdapterTests.tearDown — `KeywordFallbackAdapte...` 是第 112-113 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackAdapterTests.config — `KeywordFallbackAdapte...` 是第 115-124 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackAdapterTests.run_mode — `KeywordFallbackAdapte...` 是第 126-128 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackAdapterTests.test_command_adapter_caches_only_validated_output_under_identity_key — `KeywordFallbackAdapte...` 是第 130-147 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackAdapterTests.test_invalid_json_output_and_process_failures_fall_back — `KeywordFallbackAdapte...` 是第 149-163 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackAdapterTests.test_timeout_is_bounded_and_retried_at_most_once — `KeywordFallbackAdapte...` 是第 165-169 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackAdapterTests.test_missing_credentials_prevents_process_start — `KeywordFallbackAdapte...` 是第 171-184 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackAdapterTests.test_cache_audit_rejects_secret_shaped_content — `KeywordFallbackAdapte...` 是第 186-195 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackBenchmarkTests — `KeywordFallbackBenchm...` 是第 352-433 行的类，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackBenchmarkTests.test_fixed_benchmark_compares_quality_latency_context_usage_and_restores_cache — `KeywordFallbackBenchm...` 是第 353-433 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：主要代码单元是 [[KeywordFallbackRetrievalWiringTests]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[run_keyword_benchmark]]。"} {:block/title "协作：实现时会用到 [[run_keyword_provider]]。"} {:block/title "协作：实现时会用到 [[run_keyword_provider 与 KeywordProviderConfig 的协作实现]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.register 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests]] 会使用这里提供的行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests]] 会使用这里提供的行为。"} {:block/title "协作：[[MigrationTest]] 会使用这里提供的行为。"} {:block/title "协作：[[SourceLinkRenderer.uri]] 会使用这里提供的行为。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[append 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_feedback 与 _contains_chinese 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_obsidian]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_obsidian 与 prepare_vault 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[command 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[deploy 的协作边界]] 会使用这里提供的行为。"} {:block/title "协作：[[doctor_report 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[execute 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[initialize]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[keyword_provider_config]] 会使用这里提供的行为。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[load_page_config]] 会使用这里提供的行为。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[main（generate_large_fixture 测试）]] 会使用这里提供的行为。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[normalize]] 关联到这里的验证场景。"} {:block/title "协作：[[package_showcase]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[preflight 与 git 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[refresh 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[run 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[run_keyword_benchmark]] 关联到这里的验证场景。"} {:block/title "协作：[[run_keyword_benchmark 与 _text_list 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[run_keyword_provider]] 关联到这里的验证场景。"} {:block/title "协作：[[run_keyword_provider 与 KeywordProviderConfig 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[search_terms 与 _split_camel 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[serve_stdio 与 _write_line 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[start_scope_extension]] 会使用这里提供的行为。"} {:block/title "协作：[[sync_human_layer]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "MigrationTest"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`MigrationTest` 构造连续两个源码提交和迁移前后知识库。 它验证增量复用、delta 审阅、目录提升后的固定快照重定位、可变数据续写和基线防篡改。"} {:block/title "需要修改时：迁移计划、切换方式、审阅字段或可变层规则变化时，需要修改该测试类。"} {:block/title "源码入口：[打开源码：tests/test_migration.py 第 57 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_migration.py:57:1)  `tests/test_migration.py:57-189`"} {:block/title "内部实现：MigrationTest.setUp — 创建临时 Python 仓库、首个 Git 提交和确定性测试提供器。"} {:block/title "内部实现：MigrationTest.tearDown — 恢复测试环境变量并清理临时迁移夹具。"} {:block/title "内部实现：MigrationTest.test_exact_blob_facts_and_agent_reviews_are_reused — 验证精确 blob 与中文审阅复用、可变层基线、目录提升后的路径重定位及篡改失败门。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[audit_migration]]。"} {:block/title "协作：实现时会用到 [[audit_migration 与 _entity_key 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[initialize]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：[[AutomationTest.register]] 关联到这里的验证场景。"} {:block/title "协作：[[MigrationTest 等测试场景]] 汇总了本页。"} {:block/title "协作：[[append 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_agent_protocol]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_agent_protocol 与 _default_python 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_feedback]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_feedback 与 _contains_chinese 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_gap_register 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_obsidian]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_obsidian 与 prepare_vault 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_output_contract]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_output_contract 与 _default_ckb 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_references]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_references 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_work_record_index]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_work_record_index 与 _contains_chinese 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[doctor_report 与 _version_matches 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[initialize]] 关联到这里的验证场景。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name]] 关联到这里的验证场景。"} {:block/title "协作：[[normalize]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[parser]] 关联到这里的验证场景。"} {:block/title "协作：[[preflight]] 关联到这里的验证场景。"} {:block/title "协作：[[preflight 与 git 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[refresh]] 关联到这里的验证场景。"} {:block/title "协作：[[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[run_keyword_provider 与 KeywordProviderConfig 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 关联到这里的验证场景。"}]}
{:page {:block/title "MigrationTest 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件验证知识库在新 Git 提交上的精确增量迁移和完成态目录提升。 它覆盖 blob 与中文审阅复用、可变笔记、Hook 数据、不可变基线、Wiki 重链接、输出路径重定位及篡改失败门。"} {:block/title "需要修改时：迁移复用、目录切换、可变层或审计契约变化时，需要修改该文件。"} {:block/title "源码入口：[打开源码：tests/test_migration.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_migration.py:1:1)  `tests/test_migration.py:1-194`"} {:block/title "内部实现：git — 在临时夹具仓库执行 Git 命令并把非零退出转为测试错误。"} {:block/title "内部实现：review_all — 逐包填充固定中文审阅字段并通过普通审阅门。"} {:block/title "协作：主要代码单元是 [[MigrationTest]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：[[doctor_report 与 _version_matches 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[initialize]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[preflight]] 关联到这里的验证场景。"} {:block/title "协作：[[preflight 与 git 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "PackageReleaseTests"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`PackageReleaseTests` 是 `tests/test_package_release.py` 第 23-72 行定义的类，本页绑定该固定源码范围。 该类作为可执行验证入口，检查标识符 `PackageReleaseTests` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `PackageReleaseTests` 的说明。"} {:block/title "源码入口：[打开源码：tests/test_package_release.py 第 23 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_package_release.py:23:1)  `tests/test_package_release.py:23-72`"} {:block/title "内部实现：PackageReleaseTests.test_core_packages_exclude_plugins_and_full_only_adds_runtime — `PackageReleaseTests.t...` 是第 24-30 行的函数，供所属页面定位实现。"} {:block/title "内部实现：PackageReleaseTests.test_lite_manifest_retains_core_capabilities — `PackageReleaseTests.t...` 是第 32-60 行的函数，供所属页面定位实现。"} {:block/title "内部实现：PackageReleaseTests.test_obsidian_plugin_is_independently_versioned — `PackageReleaseTests.t...` 是第 62-72 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[source_files]]。"} {:block/title "协作：[[PackageReleaseTests 等测试场景]] 汇总了本页。"} {:block/title "协作：[[source_files]] 关联到这里的验证场景。"}]}
{:page {:block/title "PackageReleaseTests 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/test_package_release.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `test_package_release.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/test_package_release.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/test_package_release.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_package_release.py:1:1)  `tests/test_package_release.py:1-77`"} {:block/title "协作：主要代码单元是 [[PackageReleaseTests]]。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "QueryTermsTests"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`QueryTermsTests` 是 `tests/test_query_terms.py` 第 25-107 行定义的类，本页绑定该固定源码范围。 该类作为可执行验证入口，检查标识符 `QueryTermsTests` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `QueryTermsTests` 的说明。"} {:block/title "源码入口：[打开源码：tests/test_query_terms.py 第 25 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_query_terms.py:25:1)  `tests/test_query_terms.py:25-107`"} {:block/title "内部实现：QueryTermsTests.test_mechanical_fragments_do_not_enter_fts_terms — `QueryTermsTests.test_...` 是第 26-34 行的函数，供所属页面定位实现。"} {:block/title "内部实现：QueryTermsTests.test_complete_phrase_and_content_grams_are_preserved — `QueryTermsTests.test_...` 是第 36-40 行的函数，供所属页面定位实现。"} {:block/title "内部实现：QueryTermsTests.test_mixed_camel_identifier_order_is_fixed — `QueryTermsTests.test_...` 是第 42-47 行的函数，供所属页面定位实现。"} {:block/title "内部实现：QueryTermsTests.test_path_api_and_digits_are_preserved — `QueryTermsTests.test_...` 是第 49-57 行的函数，供所属页面定位实现。"} {:block/title "内部实现：QueryTermsTests.test_empty_punctuation_and_single_han_boundaries — `QueryTermsTests.test_...` 是第 59-63 行的函数，供所属页面定位实现。"} {:block/title "内部实现：QueryTermsTests.test_query_terms_are_bounded_but_index_terms_are_complete — `QueryTermsTests.test_...` 是第 65-72 行的函数，供所属页面定位实现。"} {:block/title "内部实现：QueryTermsTests.test_fts_limit_and_order_are_explicit — `QueryTermsTests.test_...` 是第 74-81 行的函数，供所属页面定位实现。"} {:block/title "内部实现：QueryTermsTests.test_deduplication_keeps_best_priority — `QueryTermsTests.test_...` 是第 83-86 行的函数，供所属页面定位实现。"} {:block/title "内部实现：QueryTermsTests.test_nfkc_is_deterministic — `QueryTermsTests.test_...` 是第 88-89 行的函数，供所属页面定位实现。"} {:block/title "内部实现：QueryTermsTests.test_no_synonym_is_synthesized_without_overlap — `QueryTermsTests.test_...` 是第 91-94 行的函数，供所属页面定位实现。"} {:block/title "内部实现：QueryTermsTests.test_compatibility_index_uses_the_same_rules_and_negative_boundary — `QueryTermsTests.test_...` 是第 96-101 行的函数，供所属页面定位实现。"} {:block/title "内部实现：QueryTermsTests.test_negative_limits_fail_closed — `QueryTermsTests.test_...` 是第 103-107 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[retrieve]]。"} {:block/title "协作：实现时会用到 [[search_terms]]。"} {:block/title "协作：实现时会用到 [[search_terms 与 _split_camel 的协作实现]]。"} {:block/title "协作：[[QueryTermsTests 等测试场景]] 汇总了本页。"}]}
{:page {:block/title "QueryTermsTests 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/test_query_terms.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `test_query_terms.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/test_query_terms.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/test_query_terms.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_query_terms.py:1:1)  `tests/test_query_terms.py:1-112`"} {:block/title "协作：主要代码单元是 [[QueryTermsTests]]。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "ScopeExtensionTest"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`ScopeExtensionTest` 是 `tests/test_scope_extension.py` 第 63-418 行定义的类，本页绑定该固定源码范围。 该类作为可执行验证入口，检查标识符 `ScopeExtensionTest` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `ScopeExtensionTest` 的说明。"} {:block/title "源码入口：[打开源码：tests/test_scope_extension.py 第 63 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_scope_extension.py:63:1)  `tests/test_scope_extension.py:63-418`"} {:block/title "内部实现：ScopeExtensionTest.setUp — `ScopeExtensionTest.setUp` 是第 64-91 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ScopeExtensionTest.add_preserved_layers — `ScopeExtensionTest.ad...` 是第 93-192 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ScopeExtensionTest.tearDown — `ScopeExtensionTest.te...` 是第 194-199 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ScopeExtensionTest.test_union_delta_idempotence_cutover_and_byte_exact_rollback — `ScopeExtensionTest.te...` 是第 201-255 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ScopeExtensionTest.test_cutover_failure_restores_origin — `ScopeExtensionTest.te...` 是第 257-268 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ScopeExtensionTest.test_audit_drift_and_rollback_failure_are_recoverable — `ScopeExtensionTest.te...` 是第 270-294 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ScopeExtensionTest.test_fixed_failure_categories — `ScopeExtensionTest.te...` 是第 296-316 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ScopeExtensionTest.test_sequential_extensions_form_an_unwindable_active_chain — `ScopeExtensionTest.te...` 是第 318-418 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_gap_register 与 _root 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[start_scope_extension]]。"} {:block/title "协作：实现时会用到 [[start_scope_extension 与 _error 的协作实现]]。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.register]] 关联到这里的验证场景。"} {:block/title "协作：[[ScopeExtensionTest 等测试场景]] 汇总了本页。"} {:block/title "协作：[[append 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_agent_protocol]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_feedback]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_gap_register]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_gap_register 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_obsidian]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_obsidian 与 prepare_vault 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_operation_journal]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_operation_journal 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_output_contract]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_output_contract 与 _default_ckb 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_references]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_references 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_work_record_index]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_work_record_index 与 _contains_chinese 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[doctor_report 与 _version_matches 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[initialize]] 关联到这里的验证场景。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[maintenance_check]] 关联到这里的验证场景。"} {:block/title "协作：[[maintenance_check 与 capability_matrix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[parser]] 关联到这里的验证场景。"} {:block/title "协作：[[preflight]] 关联到这里的验证场景。"} {:block/title "协作：[[preflight 与 git 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note]] 关联到这里的验证场景。"} {:block/title "协作：[[refresh]] 关联到这里的验证场景。"} {:block/title "协作：[[refresh 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[source_files]] 关联到这里的验证场景。"} {:block/title "协作：[[start_scope_extension]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 关联到这里的验证场景。"}]}
{:page {:block/title "ScopeExtensionTest 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/test_scope_extension.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `test_scope_extension.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/test_scope_extension.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/test_scope_extension.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_scope_extension.py:1:1)  `tests/test_scope_extension.py:1-423`"} {:block/title "内部实现：git — `git` 是第 31-35 行的函数，供所属页面定位实现。"} {:block/title "内部实现：review_all — `review_all` 是第 38-60 行的函数，供所属页面定位实现。"} {:block/title "协作：主要代码单元是 [[ScopeExtensionTest]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：[[initialize]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[preflight]] 关联到这里的验证场景。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "SessionStdioLifecycleTests"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`SessionStdioLifecycle...` 是 `tests/test_session_stdio.py` 第 77-536 行定义的类，本页绑定该固定源码范围。 该类作为可执行验证入口，检查标识符 `SessionStdioLifecycleTests` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `SessionStdioLifecycleTests` 的说明。"} {:block/title "源码入口：[打开源码：tests/test_session_stdio.py 第 77 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_session_stdio.py:77:1)  `tests/test_session_stdio.py:77-536`"} {:block/title "内部实现：SessionStdioLifecycleTests.setUp — `SessionStdioLifecycle...` 是第 78-93 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.tearDown — `SessionStdioLifecycle...` 是第 95-107 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.event — `SessionStdioLifecycle...` 是第 109-116 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_session_start_and_plain_prompt_do_not_start_stdio — `SessionStdioLifecycle...` 是第 118-129 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_exact_skill_activation_reuses_pid_across_turn_stop_and_session_end_reaps — `SessionStdioLifecycle...` 是第 131-184 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_sixteen_concurrent_first_requests_singleflight_and_isolation — `SessionStdioLifecycle...` 是第 186-240 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_sixteen_concurrent_first_requests_singleflight_and_isolation.request — `SessionStdioLifecycle...` 是第 207-214 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_parent_death_reaps_and_explicit_close_is_idempotent — `SessionStdioLifecycle...` 是第 242-269 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_start_failure_is_bounded_cli_fallback_not_resident — `SessionStdioLifecycle...` 是第 271-284 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_same_external_id_immediate_reactivation_uses_new_generation — `SessionStdioLifecycle...` 是第 286-324 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_compact_lease_temporary_supports_long_windows_root — `SessionStdioLifecycle...` 是第 326-362 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_over_budget_windows_root_returns_explicit_fallback — `SessionStdioLifecycle...` 是第 365-381 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_server_crash_restarts_once_then_falls_back_to_per_command_cli — `SessionStdioLifecycle...` 是第 383-421 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_supervisor_crash_stale_lease_is_cleaned_before_reactivation — `SessionStdioLifecycle...` 是第 423-454 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_protocol_version_mismatch_fails_handshake_with_bounded_reason — `SessionStdioLifecycle...` 是第 456-497 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_protocol_version_mismatch_fails_handshake_with_bounded_reason.FakeProcess — `SessionStdioLifecycle...` 是第 457-477 行的类，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_protocol_version_mismatch_fails_handshake_with_bounded_reason.FakeProcess.__init__ — `SessionStdioLifecycle...` 是第 461-464 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_protocol_version_mismatch_fails_handshake_with_bounded_reason.FakeProcess.poll — `SessionStdioLifecycle...` 是第 466-467 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_protocol_version_mismatch_fails_handshake_with_bounded_reason.FakeProcess.wait — `SessionStdioLifecycle...` 是第 469-471 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_protocol_version_mismatch_fails_handshake_with_bounded_reason.FakeProcess.terminate — `SessionStdioLifecycle...` 是第 473-474 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_protocol_version_mismatch_fails_handshake_with_bounded_reason.FakeProcess.kill — `SessionStdioLifecycle...` 是第 476-477 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_close_timeout_escalates_to_terminate_then_kill_and_waits — `SessionStdioLifecycle...` 是第 499-536 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_close_timeout_escalates_to_terminate_then_kill_and_waits.TimeoutProcess — `SessionStdioLifecycle...` 是第 500-526 行的类，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_close_timeout_escalates_to_terminate_then_kill_and_waits.TimeoutProcess.__init__ — `SessionStdioLifecycle...` 是第 504-510 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_close_timeout_escalates_to_terminate_then_kill_and_waits.TimeoutProcess.poll — `SessionStdioLifecycle...` 是第 512-513 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_close_timeout_escalates_to_terminate_then_kill_and_waits.TimeoutProcess.wait — `SessionStdioLifecycle...` 是第 515-520 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_close_timeout_escalates_to_terminate_then_kill_and_waits.TimeoutProcess.terminate — `SessionStdioLifecycle...` 是第 522-523 行的函数，供所属页面定位实现。"} {:block/title "内部实现：SessionStdioLifecycleTests.test_close_timeout_escalates_to_terminate_then_kill_and_waits.TimeoutProcess.kill — `SessionStdioLifecycle...` 是第 525-526 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[SessionStdioLifecycleTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[_Transport.close 与 _StartGate 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[doctor_report 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[ingest_event]]。"} {:block/title "协作：实现时会用到 [[ingest_event 与 default_registry_path 的协作实现]]。"} {:block/title "协作：[[SessionStdioLifecycleTests 等测试场景]] 汇总了本页。"} {:block/title "协作：[[_Transport.close 与 _StartGate 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[ingest_event]] 关联到这里的验证场景。"}]}
{:page {:block/title "SessionStdioLifecycleTests 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/test_session_stdio.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `test_session_stdio.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/test_session_stdio.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/test_session_stdio.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_session_stdio.py:1:1)  `tests/test_session_stdio.py:1-541`"} {:block/title "内部实现：make_output — `make_output` 是第 38-74 行的函数，供所属页面定位实现。"} {:block/title "协作：主要代码单元是 [[SessionStdioLifecycleTests]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：由 [[SessionStdioLifecycleTests]] 覆盖相关行为。"} {:block/title "协作：[[SessionStdioLifecycleTests]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "SourceLinkRenderer.uri"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`SourceLinkRenderer.uri` 将仓库内源码位置编码为编辑器可打开的 URI。 它复用已校验绝对路径，并按 VS Code、Insiders、file 或自定义模板生成确定性链接。"} {:block/title "需要修改时：新增编辑器、调整 URI 编码或改变自定义模板字段时，需要修改该函数。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/source_links.py 第 60 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/source_links.py:60:1)  `scripts/ckb_core/source_links.py:60-75`"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]]。"} {:block/title "协作：实现时会用到 [[render_integration 与 _looks_windows 的协作实现]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 汇总了本页。"} {:block/title "协作：[[_Transport.close 与 _StartGate 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_agent_protocol 与 _default_python 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_references]] 会使用这里提供的行为。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[doctor_report 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[normalize 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[start_scope_extension 与 _error 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件生成指向本地源码精确位置和 Obsidian 笔记的可点击链接。 它集中验证打开器配置、限制仓库路径边界，并让批量检索复用同一渲染器与路径缓存。"} {:block/title "需要修改时：支持的编辑器、URI 格式、路径边界或缓存行为变化时，需要修改该文件。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/source_links.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/source_links.py:1:1)  `scripts/ckb_core/source_links.py:1-184`"} {:block/title "内部实现：SourceLinkRenderer — 复用已验证配置和路径缓存，为多个实体生成可点击的本地源码链接。"} {:block/title "内部实现：SourceLinkRenderer.__init__ — 一次验证源码打开器配置并初始化仓库根目录与路径缓存。"} {:block/title "内部实现：SourceLinkRenderer.cache_size — 报告当前已缓存的源码相对路径数量。"} {:block/title "内部实现：SourceLinkRenderer.absolute_path — 校验相对路径边界，并按需缓存对应的仓库内绝对路径。"} {:block/title "内部实现：SourceLinkRenderer.markdown_link — 把源码 URI、可读路径和行范围组合为 Markdown 链接。"} {:block/title "内部实现：default_openers — 根据仓库和可选快照目录生成默认源码打开器配置。"} {:block/title "内部实现：ensure_local_openers — 读取已有打开器配置，或在缺失时写入并返回默认配置。"} {:block/title "内部实现：validate_local_openers — 校验编辑器类型、源码视图、模板和仓库根目录字段。"} {:block/title "内部实现：update_local_openers — 合并用户指定的编辑器选项并重新验证后保存配置。"} {:block/title "内部实现：source_absolute_path — 通过一次性渲染器把仓库相对路径转换为受边界约束的绝对路径。"} {:block/title "内部实现：source_uri — 通过一次性渲染器生成指定源码行列的编辑器 URI。"} {:block/title "内部实现：source_markdown_link — 通过一次性渲染器生成带可读行范围的源码 Markdown 链接。"} {:block/title "内部实现：obsidian_open_uri — 把本地 Markdown 路径编码为 Obsidian 打开链接。"} {:block/title "内部实现：audit_source_uri — 解析并核对源码 URI 是否准确指向配置根目录内的目标行。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：主要代码单元是 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[render_integration 与 _looks_windows 的协作实现]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：[[SourceLinkRenderer.uri]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "_Transport.close"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`_Transport.close` 是 `scripts/ckb_core/session_stdio.py` 第 457-496 行定义的函数，本页绑定该固定源码范围。 负责会话级 stdio 服务的首次激活、租约续用、关闭与资源释放。"} {:block/title "需要修改时：当 `close` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/session_stdio.py 第 457 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/session_stdio.py:457:1)  `scripts/ckb_core/session_stdio.py:457-496`"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.register 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[MigrationTest]] 会使用这里提供的行为。"} {:block/title "协作：[[ScopeExtensionTest]] 会使用这里提供的行为。"} {:block/title "协作：[[SessionStdioLifecycleTests]] 会使用这里提供的行为。"} {:block/title "协作：[[SessionStdioLifecycleTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[_Transport.close 与 _StartGate 的协作实现]] 汇总了本页。"} {:block/title "协作：[[append 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_agent_protocol 与 _default_python 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_operation_journal 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_references]] 会使用这里提供的行为。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[doctor_report]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[main（session_stdio_reactivation_probe 测试）]] 会使用这里提供的行为。"} {:block/title "协作：[[one_cycle]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[start_scope_extension 与 _error 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "_Transport.close 与 _StartGate 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/session_stdio.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责会话级 stdio 服务的首次激活、租约续用、关闭与资源释放。"} {:block/title "需要修改时：当 `scripts/ckb_core/session_stdio.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/session_stdio.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/session_stdio.py:1:1)  `scripts/ckb_core/session_stdio.py:1-1459`"} {:block/title "内部实现：_StartGate — `_StartGate` 是第 56-59 行的类，供所属页面定位实现。"} {:block/title "内部实现：_StartGate.__init__ — `_StartGate.__init__` 是第 57-59 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_retain_start_gate — `_retain_start_gate` 是第 66-73 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_release_start_gate — `_release_start_gate` 是第 76-80 行的函数，供所属页面定位实现。"} {:block/title "内部实现：default_session_stdio_root — `default_session_stdio...` 是第 83-85 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_path_identity — `_path_identity` 是第 88-90 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_digest — `_digest` 是第 93-95 行的函数，供所属页面定位实现。"} {:block/title "内部实现：session_digest — `session_digest` 是第 98-102 行的函数，供所属页面定位实现。"} {:block/title "内部实现：lifecycle_key — `lifecycle_key` 是第 105-121 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_lifecycle_directory — `_lifecycle_directory` 是第 124-125 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_lease_path — `_lease_path` 是第 128-129 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_read_json — `_read_json` 是第 132-137 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_validate_lifecycle_path_budget — `_validate_lifecycle_p...` 是第 140-152 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_write_lease — `_write_lease` 是第 155-185 行的函数，供所属页面定位实现。"} {:block/title "内部实现：pid_exists — `pid_exists` 是第 188-218 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_force_terminate_pid — `_force_terminate_pid` 是第 221-251 行的函数，供所属页面定位实现。"} {:block/title "内部实现：process_metrics — `process_metrics` 是第 254-303 行的函数，供所属页面定位实现。"} {:block/title "内部实现：process_metrics.PROCESS_MEMORY_COUNTERS — `process_metrics.PROCE...` 是第 262-274 行的类，供所属页面定位实现。"} {:block/title "内部实现：_state_counts — `_state_counts` 是第 306-324 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_base_lease — `_base_lease` 是第 327-360 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_Reader — `_Reader` 是第 363-378 行的类，供所属页面定位实现。"} {:block/title "内部实现：_Reader.__init__ — `_Reader.__init__` 是第 364-367 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_Reader.run — `_Reader.run` 是第 369-378 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_Transport — `_Transport` 是第 382-496 行的类，供所属页面定位实现。"} {:block/title "内部实现：_Transport.start — `_Transport.start` 是第 393-423 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_Transport.request — `_Transport.request` 是第 425-455 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_write_response — `_write_response` 是第 499-500 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_clear_transient — `_clear_transient` 是第 503-515 行的函数，供所属页面定位实现。"} {:block/title "内部实现：controller_main — `controller_main` 是第 518-722 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_supervisor_process_options — `_supervisor_process_o...` 是第 725-729 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_reap_supervisor — `_reap_supervisor` 是第 732-736 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_acquire_start_lock — `_acquire_start_lock` 是第 739-767 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_release_start_lock — `_release_start_lock` 是第 770-785 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_active_lease — `_active_lease` 是第 788-799 行的函数，供所属页面定位实现。"} {:block/title "内部实现：activate_session_stdio — `activate_session_stdio` 是第 802-887 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_activation_exists — `_activation_exists` 是第 890-905 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_start_supervisor_locked — `_start_supervisor_locked` 是第 908-1004 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_start_supervisor — `_start_supervisor` 是第 1007-1019 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_fallback_command — `_fallback_command` 是第 1022-1051 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_run_cli_fallback — `_run_cli_fallback` 是第 1054-1136 行的函数，供所属页面定位实现。"} {:block/title "内部实现：request_session — `request_session` 是第 1139-1255 行的函数，供所属页面定位实现。"} {:block/title "内部实现：environment_session — `environment_session` 是第 1258-1283 行的函数，供所属页面定位实现。"} {:block/title "内部实现：maybe_request_session — `maybe_request_session` 是第 1286-1303 行的函数，供所属页面定位实现。"} {:block/title "内部实现：list_sessions — `list_sessions` 是第 1306-1327 行的函数，供所属页面定位实现。"} {:block/title "内部实现：close_session — `close_session` 是第 1330-1389 行的函数，供所属页面定位实现。"} {:block/title "内部实现：cleanup_sessions — `cleanup_sessions` 是第 1392-1431 行的函数，供所属页面定位实现。"} {:block/title "内部实现：audit_sessions — `audit_sessions` 是第 1434-1458 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：主要代码单元是 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[doctor_report 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve]]。"} {:block/title "协作：实现时会用到 [[start_scope_extension 与 _error 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[SessionStdioLifecycleTests]] 覆盖相关行为。"} {:block/title "协作：由 [[append 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[SessionStdioLifecycleTests]] 会使用这里提供的行为。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[main（session_stdio_harness_probe 测试）]] 会使用这里提供的行为。"} {:block/title "协作：[[main（session_stdio_reactivation_probe 测试）]] 会使用这里提供的行为。"} {:block/title "协作：[[one_cycle]] 会使用这里提供的行为。"} {:block/title "协作：[[one_cycle 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "__init__ 实现概览"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/__init__.py` 是 `scripts/ckb_core/__init__.py` 中负责汇总并提供scripts/ckb_core/__init__.py 所属模块的局部职责的文件入口。 它按源码所示的参数、条件分支和数据结构完成汇总并提供scripts/ckb_core/__init__.py 所属模块的局部职责，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当scripts/ckb_core/__init__.py 的职责的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/__init__.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/__init__.py:1:1)  `scripts/ckb_core/__init__.py:1-5`"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "append"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`ManagementSchemaPersi...` 是 `tests/test_management_agent.py` 第 111-113 行定义的函数，本页绑定该固定源码范围。 该函数作为可执行验证入口，检查标识符 `append` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `append` 的说明。"} {:block/title "源码入口：[打开源码：tests/test_management_agent.py 第 111 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_management_agent.py:111:1)  `tests/test_management_agent.py:111-113`"} {:block/title "协作：实现时会用到 [[bind_conversation 与 default_management_registry_path 的协作实现]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[AgentProtocolBatchApplyTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests]] 会使用这里提供的行为。"} {:block/title "协作：[[ScopeExtensionTest]] 会使用这里提供的行为。"} {:block/title "协作：[[SessionStdioLifecycleTests]] 会使用这里提供的行为。"} {:block/title "协作：[[_Transport.close 与 _StartGate 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[append 等测试场景]] 汇总了本页。"} {:block/title "协作：[[audit_agent_protocol]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_agent_protocol 与 _default_python 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_feedback]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_feedback 与 _contains_chinese 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_gap_register]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_gap_register 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_obsidian]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_operation_journal]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_operation_journal 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_output_contract]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_references]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_references 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_work_record_index]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_work_record_index 与 _contains_chinese 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[bind_conversation]] 会使用这里提供的行为。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[build 的协作边界]] 会使用这里提供的行为。"} {:block/title "协作：[[command 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[create_batch_plan]] 会使用这里提供的行为。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[doctor_report]] 会使用这里提供的行为。"} {:block/title "协作：[[doctor_report 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[maintenance_check]] 会使用这里提供的行为。"} {:block/title "协作：[[maintenance_check 与 capability_matrix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[main（make_source_patch 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[main（session_stdio_reactivation_probe 测试）]] 会使用这里提供的行为。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[normalize 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[package_showcase]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[preflight 与 git 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[refresh 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[run]] 会使用这里提供的行为。"} {:block/title "协作：[[run 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[run_keyword_benchmark]] 会使用这里提供的行为。"} {:block/title "协作：[[run_keyword_benchmark 与 _text_list 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[run_keyword_provider 与 KeywordProviderConfig 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[source_files]] 会使用这里提供的行为。"} {:block/title "协作：[[source_files 与 sha256 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[start_scope_extension]] 会使用这里提供的行为。"} {:block/title "协作：[[start_scope_extension 与 _error 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session 与 _session_directory 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[sync_human_layer]] 会使用这里提供的行为。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "append 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/test_management_agent.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `test_management_agent.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/test_management_agent.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/test_management_agent.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_management_agent.py:1:1)  `tests/test_management_agent.py:1-475`"} {:block/title "内部实现：git — `git` 是第 36-46 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementSchemaPersistenceTest — `ManagementSchemaPersi...` 是第 49-125 行的类，供所属页面定位实现。"} {:block/title "内部实现：ManagementSchemaPersistenceTest.setUp — `ManagementSchemaPersi...` 是第 50-55 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementSchemaPersistenceTest.tearDown — `ManagementSchemaPersi...` 是第 57-58 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementSchemaPersistenceTest.payload — `ManagementSchemaPersi...` 是第 60-70 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementSchemaPersistenceTest.test_public_schema_has_four_separate_capabilities_and_privacy_contract — `ManagementSchemaPersi...` 是第 72-78 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementSchemaPersistenceTest.test_canonical_input_drops_unrecognized_sensitive_content — `ManagementSchemaPersi...` 是第 80-94 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementSchemaPersistenceTest.test_unknown_harness_declares_only_generic_binding — `ManagementSchemaPersi...` 是第 96-101 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementSchemaPersistenceTest.test_malformed_registry_is_reported_without_replacement — `ManagementSchemaPersi...` 是第 103-108 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementSchemaPersistenceTest.test_locked_registry_serializes_concurrent_audit_events — `ManagementSchemaPersi...` 是第 110-119 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementSchemaPersistenceTest.test_canonical_input_rejects_missing_identity — `ManagementSchemaPersi...` 是第 121-125 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest — `ManagementBindingLife...` 是第 128-470 行的类，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.setUp — `ManagementBindingLife...` 是第 129-155 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.tearDown — `ManagementBindingLife...` 是第 157-158 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.payload — `ManagementBindingLife...` 是第 160-170 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.test_bind_is_idempotent_status_is_live_and_unbind_preserves_audit — `ManagementBindingLife...` 是第 172-194 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.test_conflicting_project_for_same_conversation_fails_without_rebinding — `ManagementBindingLife...` 是第 196-217 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.test_missing_repo_output_branch_and_dirty_tree_fail_preflight — `ManagementBindingLife...` 是第 219-229 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.test_non_git_unborn_and_missing_state_outputs_fail_preflight — `ManagementBindingLife...` 是第 231-248 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.test_missing_conversation_status_and_unbind_fail_without_creating_binding — `ManagementBindingLife...` 是第 250-258 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.test_same_workspace_routes_distinct_conversations_to_explicit_nested_repos — `ManagementBindingLife...` 是第 260-288 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.test_status_detects_head_drift_and_dirty_tree_after_binding — `ManagementBindingLife...` 是第 290-300 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.test_concurrent_repeated_bind_and_unbind_have_one_active_object — `ManagementBindingLife...` 是第 302-320 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.test_concurrent_repeated_bind_and_unbind_have_one_active_object.bind — `ManagementBindingLife...` 是第 303-304 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.test_concurrent_repeated_bind_and_unbind_have_one_active_object.unbind — `ManagementBindingLife...` 是第 313-314 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.test_context_rechecks_feedback_sqlite_and_maintenance_gates — `ManagementBindingLife...` 是第 322-352 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.ready_context — `ManagementBindingLife...` 是第 354-361 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.test_task_dispatch_creates_independent_worktree_prompt_and_review_gate — `ManagementBindingLife...` 是第 363-413 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.test_task_dispatch_is_idempotent_and_blocks_integration_drift — `ManagementBindingLife...` 是第 415-448 行的函数，供所属页面定位实现。"} {:block/title "内部实现：ManagementBindingLifecycleTest.test_task_review_records_literal_failure_without_merge — `ManagementBindingLife...` 是第 450-470 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：主要代码单元是 [[append]]。"} {:block/title "协作：实现时会用到 [[bind_conversation]]。"} {:block/title "协作：实现时会用到 [[bind_conversation 与 default_management_registry_path 的协作实现]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[main（generate_large_fixture 测试）]] 覆盖相关行为。"} {:block/title "协作：[[_Transport.close 与 _StartGate 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_gap_register]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_gap_register 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_operation_journal]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_references]] 关联到这里的验证场景。"} {:block/title "协作：[[bind_conversation]] 关联到这里的验证场景。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize]] 关联到这里的验证场景。"} {:block/title "协作：[[maintenance_check]] 关联到这里的验证场景。"} {:block/title "协作：[[maintenance_check 与 capability_matrix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[preflight]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine]] 关联到这里的验证场景。"} {:block/title "协作：[[run_keyword_provider]] 关联到这里的验证场景。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "audit_agent_protocol"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`audit_agent_protocol` 是 `scripts/ckb_core/agent_protocol.py` 中负责核对各 Harness 指令文件、工作区绑定、反馈、工作记录与输出契约的函数。 它按源码所示的参数、条件分支和数据结构完成核对各 Harness 指令文件、工作区绑定、反馈、工作记录与输出契约，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当跨 Harness Agent 协议生成、安装、检查与维护入口的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/agent_protocol.py 第 420 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_protocol.py:420:1)  `scripts/ckb_core/agent_protocol.py:420-496`"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_agent_protocol 与 _default_python 的协作实现]]。"} {:block/title "协作：实现时会用到 [[audit_feedback]]。"} {:block/title "协作：实现时会用到 [[audit_output_contract]]。"} {:block/title "协作：实现时会用到 [[audit_work_record_index]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：[[AgentProtocolBatchApplyTests]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_agent_protocol 与 _default_python 的协作实现]] 汇总了本页。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[maintenance_check]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[serve_stdio 与 _write_line 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "audit_agent_protocol 与 _default_python 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/agent_protocol.py` 是 `scripts/ckb_core/agent_protocol.py` 中负责汇总并提供跨 Harness Agent 协议生成、安装、检查与维护入口的文件入口。 它按源码所示的参数、条件分支和数据结构完成汇总并提供跨 Harness Agent 协议生成、安装、检查与维护入口，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当跨 Harness Agent 协议生成、安装、检查与维护入口的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/agent_protocol.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_protocol.py:1:1)  `scripts/ckb_core/agent_protocol.py:1-507`"} {:block/title "内部实现：_default_python — 返回默认 `python` 对应的数据与约束。"} {:block/title "内部实现：_default_ckb — 返回默认 `ckb` 对应的数据与约束。"} {:block/title "内部实现：_single_quote — 处理 `quote` 对应的数据与约束。"} {:block/title "内部实现：_command_examples — 处理 `examples` 对应的数据与约束。"} {:block/title "内部实现：_protocol_text — 处理 `text` 对应的数据与约束。"} {:block/title "内部实现：_adapter_texts — 处理 `texts` 对应的数据与约束。"} {:block/title "内部实现：_managed_block — 处理 `block` 对应的数据与约束。"} {:block/title "内部实现：_replace_managed_block — 替换 `managed_block` 对应的数据与约束。"} {:block/title "内部实现：_instruction_roots — 处理 `roots` 对应的数据与约束。"} {:block/title "内部实现：_write_exact_root — 写入 `exact_root` 对应的数据与约束。"} {:block/title "内部实现：_update_markdown_ownership — 更新 `markdown_ownership` 对应的数据与约束。"} {:block/title "内部实现：_hide_protocol_files — 隐藏 `protocol_files` 对应的数据与约束。"} {:block/title "内部实现：_load_record — 加载 `record` 对应的数据与约束。"} {:block/title "内部实现：_resolve_runtime — 解析并确定 `runtime` 对应的数据与约束。"} {:block/title "内部实现：_workspace_root_allowed — 判断 `workspace_root_allowed` 所表达的条件。"} {:block/title "内部实现：_write_workspace_root — 写入 `workspace_root` 对应的数据与约束。"} {:block/title "内部实现：project_agent_protocol — 投影 `agent_protocol` 对应的数据与约束。"} {:block/title "内部实现：install_agent_protocol — 安装 `agent_protocol` 对应的数据与约束。"} {:block/title "内部实现：_expected_internal — 处理 `internal` 对应的数据与约束。"} {:block/title "内部实现：_audit_note_storage — 审计 `note_storage` 对应的数据与约束。"} {:block/title "内部实现：agent_protocol_status — 汇总 `agent_protocol_status` 状态与计数。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：主要代码单元是 [[audit_agent_protocol]]。"} {:block/title "协作：实现时会用到 [[audit_feedback 与 _contains_chinese 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[record_note 与 page_tag 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve]]。"} {:block/title "协作：实现时会用到 [[start_scope_extension 与 _error 的协作实现]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：[[audit_agent_protocol]] 会使用这里提供的行为。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[run 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "audit_feedback"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`audit_feedback` 是 `scripts/ckb_core/feedback.py` 中负责检查反馈锚点、状态、镜像、归档与落实记录的一致性的函数。 它按源码所示的参数、条件分支和数据结构完成检查反馈锚点、状态、镜像、归档与落实记录的一致性，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当定位式人工反馈的创建、重定位、审计与归档的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/feedback.py 第 437 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/feedback.py:437:1)  `scripts/ckb_core/feedback.py:437-545`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_feedback 与 _contains_chinese 的协作实现]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[audit_agent_protocol]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_feedback 与 _contains_chinese 的协作实现]] 汇总了本页。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[serve_stdio 与 _write_line 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "audit_feedback 与 _contains_chinese 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/feedback.py` 是 `scripts/ckb_core/feedback.py` 中负责汇总并提供定位式人工反馈的创建、重定位、审计与归档的文件入口。 它按源码所示的参数、条件分支和数据结构完成汇总并提供定位式人工反馈的创建、重定位、审计与归档，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当定位式人工反馈的创建、重定位、审计与归档的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/feedback.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/feedback.py:1:1)  `scripts/ckb_core/feedback.py:1-595`"} {:block/title "内部实现：_contains_chinese — 判断 `contains_chinese` 所表达的条件。"} {:block/title "内部实现：prepare_feedback_store — 准备 `feedback_store` 对应的数据与约束。"} {:block/title "内部实现：_canonical_relative_target — 处理 `relative_target` 对应的数据与约束。"} {:block/title "内部实现：_read_chinese_body — 读取 `chinese_body` 对应的数据与约束。"} {:block/title "内部实现：_line_selection — 处理 `selection` 对应的数据与约束。"} {:block/title "内部实现：_next_feedback_id — 根据固定输入计算 `next_feedback_id` 稳定标识。"} {:block/title "内部实现：_title_of — 处理 `of` 对应的数据与约束。"} {:block/title "内部实现：_blockquote — 处理 `blockquote` 对应的数据与约束。"} {:block/title "内部实现：_visible_feedback — 处理 `feedback` 对应的数据与约束。"} {:block/title "内部实现：_record_path — 记录 `path` 对应的数据与约束。"} {:block/title "内部实现：_visible_relative — 处理 `relative` 对应的数据与约束。"} {:block/title "内部实现：_write_visible_mirrors — 写入 `visible_mirrors` 对应的数据与约束。"} {:block/title "内部实现：create_feedback — 创建 `feedback` 对应的数据与约束。"} {:block/title "内部实现：_load_feedback — 加载 `feedback` 对应的数据与约束。"} {:block/title "内部实现：_all_offsets — 处理 `offsets` 对应的数据与约束。"} {:block/title "内部实现：_offset_lines — 处理 `lines` 对应的数据与约束。"} {:block/title "内部实现：_locate_anchor — 定位 `anchor` 对应的数据与约束。"} {:block/title "内部实现：locate_feedback — 定位 `feedback` 对应的数据与约束。"} {:block/title "内部实现：_canonical_applied_record — 处理 `applied_record` 对应的数据与约束。"} {:block/title "内部实现：resolve_feedback — 解析并确定 `feedback` 对应的数据与约束。"} {:block/title "内部实现：list_feedback — 列出 `feedback` 对应的数据与约束。"} {:block/title "内部实现：_query_terms — 查询 `terms` 对应的数据与约束。"} {:block/title "内部实现：search_feedback — 处理 `feedback` 对应的数据与约束。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：主要代码单元是 [[audit_feedback]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_agent_protocol 与 _default_python 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_feedback]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "audit_gap_register"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`audit_gap_register` 是 `scripts/ckb_core/research_gaps.py` 第 233-272 行定义的函数，本页绑定该固定源码范围。 负责实现 `research_gaps.py` 中由固定源码定义的命令或知识库处理步骤。"} {:block/title "需要修改时：当 `audit_gap_register` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/research_gaps.py 第 233 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/research_gaps.py:233:1)  `scripts/ckb_core/research_gaps.py:233-272`"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_gap_register 与 _root 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[append 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[audit_gap_register 与 _root 的协作实现]] 汇总了本页。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[maintenance_check]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"}]}
{:page {:block/title "audit_gap_register 与 _root 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/research_gaps.py` 是 `scripts/ckb_core/research_gaps.py` 中负责汇总并提供研究缺口的确定性登记、状态转换、聚合与审计的文件入口。 它按源码所示的参数、条件分支和数据结构完成汇总并提供研究缺口的确定性登记、状态转换、聚合与审计，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当研究缺口的确定性登记、状态转换、聚合与审计的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/research_gaps.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/research_gaps.py:1:1)  `scripts/ckb_core/research_gaps.py:1-273`"} {:block/title "内部实现：_root — 处理 `root` 对应的数据与约束。"} {:block/title "内部实现：_records_root — 处理 `root` 对应的数据与约束。"} {:block/title "内部实现：_narrative — 处理 `narrative` 对应的数据与约束。"} {:block/title "内部实现：_evidence — 处理 `evidence` 对应的数据与约束。"} {:block/title "内部实现：_record_path — 记录 `path` 对应的数据与约束。"} {:block/title "内部实现：gap_records — 处理 `records` 对应的数据与约束。"} {:block/title "内部实现：_index_value — 处理 `value` 对应的数据与约束。"} {:block/title "内部实现：_write_index — 写入 `index` 对应的数据与约束。"} {:block/title "内部实现：gap_navigation_counts — 汇总 `gap_navigation_counts` 状态与计数。"} {:block/title "内部实现：_sync_indexes — 同步 `indexes` 对应的数据与约束。"} {:block/title "内部实现：create_gap — `create_gap` 是第 123-149 行的函数，供所属页面定位实现。"} {:block/title "内部实现：resolve_gap — 解析并确定 `gap` 对应的数据与约束。"} {:block/title "内部实现：list_gaps — 列出 `gaps` 对应的数据与约束。"} {:block/title "内部实现：gap_machine_records — 处理 `machine_records` 对应的数据与约束。"} {:block/title "内部实现：_record_errors — 记录 `errors` 对应的数据与约束。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：主要代码单元是 [[audit_gap_register]]。"} {:block/title "协作：实现时会用到 [[audit_work_record_index 与 _contains_chinese 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve 与 _tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[append 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests]] 会使用这里提供的行为。"} {:block/title "协作：[[ScopeExtensionTest]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_gap_register]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_work_record_index 与 _contains_chinese 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "audit_migration"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`audit_migration` 是 `scripts/ckb_core/migration.py` 第 367-479 行定义的函数，本页绑定该固定源码范围。 负责把已审计知识库增量迁移到新快照，并保留可变层和复用证明。"} {:block/title "需要修改时：当 `audit_migration` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/migration.py 第 367 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/migration.py:367:1)  `scripts/ckb_core/migration.py:367-479`"} {:block/title "内部实现：audit_migration.check — `audit_migration.check` 是第 373-374 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_migration 与 _entity_key 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：[[MigrationTest]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 汇总了本页。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"}]}
{:page {:block/title "audit_migration 与 _entity_key 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/migration.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责把已审计知识库增量迁移到新快照，并保留可变层和复用证明。"} {:block/title "需要修改时：当 `scripts/ckb_core/migration.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/migration.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/migration.py:1:1)  `scripts/ckb_core/migration.py:1-604`"} {:block/title "内部实现：_entity_key — `_entity_key` 是第 24-34 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_review_shape — `_review_shape` 是第 37-38 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_review_for_new_entity — `_review_for_new_entity` 是第 41-62 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_copy_file — `_copy_file` 是第 65-74 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_mutable_target — `_mutable_target` 是第 77-84 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_add_mutable_baseline — `_add_mutable_baseline` 是第 87-104 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_generated_paths — `_generated_paths` 是第 107-111 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_preserve_mutable_layers — `_preserve_mutable_layers` 是第 114-181 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_selected_entities — `_selected_entities` 是第 184-196 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_replace_review_packs — `_replace_review_packs` 是第 199-250 行的函数，供所属页面定位实现。"} {:block/title "内部实现：migrate_output — `migrate_output` 是第 253-364 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_semantic_page_key — `_semantic_page_key` 是第 482-484 行的函数，供所属页面定位实现。"} {:block/title "内部实现：relink_preserved_notes — `relink_preserved_notes` 是第 487-581 行的函数，供所属页面定位实现。"} {:block/title "内部实现：relink_preserved_notes.replace_links — `relink_preserved_note...` 是第 515-520 行的函数，供所属页面定位实现。"} {:block/title "内部实现：migration_status — `migration_status` 是第 584-603 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：主要代码单元是 [[audit_migration]]。"} {:block/title "协作：实现时会用到 [[ingest_event 与 default_registry_path 的协作实现]]。"} {:block/title "协作：实现时会用到 [[initialize]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[module_name 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[preflight 与 git 的协作实现]]。"} {:block/title "协作：实现时会用到 [[render_integration 与 _looks_windows 的协作实现]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[MigrationTest]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[start_scope_extension]] 会使用这里提供的行为。"} {:block/title "协作：[[start_scope_extension 与 _error 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "audit_obsidian"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`audit_obsidian` 是 `scripts/ckb_core/obsidian.py` 中负责检查 Obsidian 配置、样式、所有权清单与页面投影约束的函数。 它按源码所示的参数、条件分支和数据结构完成检查 Obsidian 配置、样式、所有权清单与页面投影约束，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当Obsidian vault 配置、页面投影和本地编辑器集成的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/obsidian.py 第 118 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/obsidian.py:118:1)  `scripts/ckb_core/obsidian.py:118-165`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_output_contract]]。"} {:block/title "协作：实现时会用到 [[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[run 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[audit_obsidian 与 prepare_vault 的协作实现]] 汇总了本页。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "audit_obsidian 与 prepare_vault 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/obsidian.py` 是 `scripts/ckb_core/obsidian.py` 中负责汇总并提供Obsidian vault 配置、页面投影和本地编辑器集成的文件入口。 它按源码所示的参数、条件分支和数据结构完成汇总并提供Obsidian vault 配置、页面投影和本地编辑器集成，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当Obsidian vault 配置、页面投影和本地编辑器集成的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/obsidian.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/obsidian.py:1:1)  `scripts/ckb_core/obsidian.py:1-166`"} {:block/title "内部实现：prepare_vault — 准备 `vault` 对应的数据与约束。"} {:block/title "内部实现：install_obsidian — 安装 `obsidian` 对应的数据与约束。"} {:block/title "内部实现：write_generated_ownership — 写入 `generated_ownership` 对应的数据与约束。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：主要代码单元是 [[audit_obsidian]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[run 等测试场景]] 覆盖相关行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "audit_operation_journal"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`audit_operation_journal` 是 `scripts/ckb_core/operation_journal.py` 第 383-452 行定义的函数，本页绑定该固定源码范围。 负责追加和审计有界、机器可读的操作日志。"} {:block/title "需要修改时：当 `audit_operation_journal` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/operation_journal.py 第 383 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/operation_journal.py:383:1)  `scripts/ckb_core/operation_journal.py:383-452`"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_operation_journal 与 _root 的协作实现]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[append 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[audit_operation_journal 与 _root 的协作实现]] 汇总了本页。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[maintenance_check]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"}]}
{:page {:block/title "audit_operation_journal 与 _root 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/operation_journal.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责追加和审计有界、机器可读的操作日志。"} {:block/title "需要修改时：当 `scripts/ckb_core/operation_journal.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/operation_journal.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/operation_journal.py:1:1)  `scripts/ckb_core/operation_journal.py:1-453`"} {:block/title "内部实现：_root — `_root` 是第 50-51 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_journal_lock — `_journal_lock` 是第 55-79 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_read_shard — `_read_shard` 是第 82-96 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_serialized_lines — `_serialized_lines` 是第 99-100 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_write_shard — `_write_shard` 是第 103-107 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_state — `_state` 是第 110-122 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_retention_cutoff — `_retention_cutoff` 是第 125-126 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_prune_expired — `_prune_expired` 是第 129-139 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_all_events — `_all_events` 是第 142-147 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_latest_summary — `_latest_summary` 是第 150-184 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_relative_evidence — `_relative_evidence` 是第 187-205 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_command_name — `_command_name` 是第 208-228 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_operation_type — `_operation_type` 是第 231-258 行的函数，供所属页面定位实现。"} {:block/title "内部实现：record_operation — `record_operation` 是第 261-309 行的函数，供所属页面定位实现。"} {:block/title "内部实现：record_cli_operation — `record_cli_operation` 是第 312-327 行的函数，供所属页面定位实现。"} {:block/title "内部实现：list_operations — `list_operations` 是第 330-348 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_event_errors — `_event_errors` 是第 351-380 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：主要代码单元是 [[audit_operation_journal]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[refresh]]。"} {:block/title "协作：实现时会用到 [[retrieve]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_operation_journal]] 会使用这里提供的行为。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[emit]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "audit_output_contract"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`audit_output_contract` 是 `scripts/ckb_core/output_contract.py` 第 111-143 行定义的函数，本页绑定该固定源码范围。 负责投影并校验面向 Agent 的输出契约。"} {:block/title "需要修改时：当 `audit_output_contract` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/output_contract.py 第 111 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/output_contract.py:111:1)  `scripts/ckb_core/output_contract.py:111-143`"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_output_contract 与 _default_ckb 的协作实现]]。"} {:block/title "协作：实现时会用到 [[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[AgentProtocolBatchApplyTests]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_agent_protocol]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_obsidian]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_output_contract 与 _default_ckb 的协作实现]] 汇总了本页。"} {:block/title "协作：[[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "audit_output_contract 与 _default_ckb 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/output_contract.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责投影并校验面向 Agent 的输出契约。"} {:block/title "需要修改时：当 `scripts/ckb_core/output_contract.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/output_contract.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/output_contract.py:1:1)  `scripts/ckb_core/output_contract.py:1-144`"} {:block/title "内部实现：_default_ckb — `_default_ckb` 是第 19-20 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_runtime_binding — `_runtime_binding` 是第 23-28 行的函数，供所属页面定位实现。"} {:block/title "内部实现：expected_output_contract — `expected_output_contract` 是第 31-35 行的函数，供所属页面定位实现。"} {:block/title "内部实现：output_contract_for_runtime — `output_contract_for_r...` 是第 38-58 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_update_ownership — `_update_ownership` 是第 61-73 行的函数，供所属页面定位实现。"} {:block/title "内部实现：project_output_contract — `project_output_contract` 是第 76-97 行的函数，供所属页面定位实现。"} {:block/title "内部实现：remove_output_contract — `remove_output_contract` 是第 100-108 行的函数，供所属页面定位实现。"} {:block/title "协作：主要代码单元是 [[audit_output_contract]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[audit_output_contract]] 会使用这里提供的行为。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[sync_human_layer]] 会使用这里提供的行为。"}]}
{:page {:block/title "audit_references"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`audit_references` 是 `scripts/ckb_core/reference_documents.py` 中负责核对资料原文、许可证、逐项引用、中文主张、镜像和机器索引的函数。 它按源码所示的参数、条件分支和数据结构完成核对资料原文、许可证、逐项引用、中文主张、镜像和机器索引，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当审阅文本资料的归档、逐项核对、投影、索引与回滚的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/reference_documents.py 第 470 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/reference_documents.py:470:1)  `scripts/ckb_core/reference_documents.py:470-558`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_references 与 _root 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[append 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[audit_references 与 _root 的协作实现]] 汇总了本页。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[maintenance_check]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"}]}
{:page {:block/title "audit_references 与 _root 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/reference_documents.py` 是 `scripts/ckb_core/reference_documents.py` 中负责汇总并提供审阅文本资料的归档、逐项核对、投影、索引与回滚的文件入口。 它按源码所示的参数、条件分支和数据结构完成汇总并提供审阅文本资料的归档、逐项核对、投影、索引与回滚，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当审阅文本资料的归档、逐项核对、投影、索引与回滚的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/reference_documents.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/reference_documents.py:1:1)  `scripts/ckb_core/reference_documents.py:1-604`"} {:block/title "内部实现：_root — 处理 `root` 对应的数据与约束。"} {:block/title "内部实现：_manifests — 处理 `manifests` 对应的数据与约束。"} {:block/title "内部实现：_manifest — 处理 `manifest` 对应的数据与约束。"} {:block/title "内部实现：_validate_output — 验证 `output` 对应的数据与约束。"} {:block/title "内部实现：_validate_license — 验证 `license` 对应的数据与约束。"} {:block/title "内部实现：_decode_source — 处理 `source` 对应的数据与约束。"} {:block/title "内部实现：_set_marker — 处理 `marker` 对应的数据与约束。"} {:block/title "内部实现：ingest_reference — 接收并写入 `reference` 对应的数据与约束。"} {:block/title "内部实现：write_reference_review_template — 写入 `reference_review_template` 对应的数据与约束。"} {:block/title "内部实现：_source_lines — 处理 `lines` 对应的数据与约束。"} {:block/title "内部实现：_validate_review — 验证 `review` 对应的数据与约束。"} {:block/title "内部实现：_source_uri — 处理 `uri` 对应的数据与约束。"} {:block/title "内部实现：_render_reference_page — 渲染 `reference_page` 对应的数据与约束。"} {:block/title "内部实现：_active_reviewed — 处理 `reviewed` 对应的数据与约束。"} {:block/title "内部实现：project_references — 投影 `references` 对应的数据与约束。"} {:block/title "内部实现：submit_reference_review — 处理 `reference_review` 对应的数据与约束。"} {:block/title "内部实现：reference_machine_records — 处理 `machine_records` 对应的数据与约束。"} {:block/title "内部实现：_reference_sections — 处理 `sections` 对应的数据与约束。"} {:block/title "内部实现：list_references — 列出 `references` 对应的数据与约束。"} {:block/title "内部实现：rollback_reference — 回滚 `reference` 对应的数据与约束。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：主要代码单元是 [[audit_references]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[render_integration 与 _looks_windows 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve 与 _tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[audit_references]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "audit_work_record_index"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`audit_work_record_index` 是 `scripts/ckb_core/work_record_index.py` 第 230-241 行定义的函数，本页绑定该固定源码范围。 负责实现 `work_record_index.py` 中由固定源码定义的命令或知识库处理步骤。"} {:block/title "需要修改时：当 `audit_work_record_index` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/work_record_index.py 第 230 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/work_record_index.py:230:1)  `scripts/ckb_core/work_record_index.py:230-241`"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_work_record_index 与 _contains_chinese 的协作实现]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：[[audit_agent_protocol]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_work_record_index 与 _contains_chinese 的协作实现]] 汇总了本页。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[maintenance_check]] 会使用这里提供的行为。"}]}
{:page {:block/title "audit_work_record_index 与 _contains_chinese 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/work_record_index.py` 是 `scripts/ckb_core/work_record_index.py` 中负责汇总并提供工作记录的完整分组、中文摘要、导航生成与审计的文件入口。 它按源码所示的参数、条件分支和数据结构完成汇总并提供工作记录的完整分组、中文摘要、导航生成与审计，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当工作记录的完整分组、中文摘要、导航生成与审计的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/work_record_index.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/work_record_index.py:1:1)  `scripts/ckb_core/work_record_index.py:1-242`"} {:block/title "内部实现：_contains_chinese — 判断 `contains_chinese` 所表达的条件。"} {:block/title "内部实现：_title — 处理 `title` 对应的数据与约束。"} {:block/title "内部实现：_plain_text — 处理 `text` 对应的数据与约束。"} {:block/title "内部实现：_first_narrative — 处理 `narrative` 对应的数据与约束。"} {:block/title "内部实现：collect_work_records — 处理 `work_records` 对应的数据与约束。"} {:block/title "内部实现：render_work_record_index — 渲染 `work_record_index` 对应的数据与约束。"} {:block/title "内部实现：write_work_record_index — 写入 `work_record_index` 对应的数据与约束。"} {:block/title "内部实现：refresh_work_record_index — `refresh_work_record_i...` 是第 173-185 行的函数，供所属页面定位实现。"} {:block/title "内部实现：audit_work_record_root — 审计 `work_record_root` 对应的数据与约束。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_gap_register 与 _root 的协作实现]]。"} {:block/title "协作：主要代码单元是 [[audit_work_record_index]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[audit_gap_register 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_work_record_index]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "bind_conversation"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`bind_conversation` 是 `scripts/ckb_core/management_agent.py` 第 435-550 行定义的函数，本页绑定该固定源码范围。 负责管理 Agent 的会话绑定、执行队列、开发任务交接与合并审计状态。"} {:block/title "需要修改时：当 `bind_conversation` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/management_agent.py 第 435 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/management_agent.py:435:1)  `scripts/ckb_core/management_agent.py:435-550`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[append 等测试场景]]。"} {:block/title "协作：实现时会用到 [[bind_conversation 与 default_management_registry_path 的协作实现]]。"} {:block/title "协作：实现时会用到 [[preflight]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[append 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[main（generate_large_fixture 测试）]] 覆盖相关行为。"} {:block/title "协作：由 [[main（session_stdio_harness_probe 测试）]] 覆盖相关行为。"} {:block/title "协作：[[append 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "bind_conversation 与 default_management_registry_path 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/management_agent.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责管理 Agent 的会话绑定、执行队列、开发任务交接与合并审计状态。"} {:block/title "需要修改时：当 `scripts/ckb_core/management_agent.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/management_agent.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/management_agent.py:1:1)  `scripts/ckb_core/management_agent.py:1-1337`"} {:block/title "内部实现：default_management_registry_path — `default_management_re...` 是第 51-53 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_path_key — `_path_key` 是第 56-58 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_normalized_path — `_normalized_path` 是第 61-62 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_empty_registry — `_empty_registry` 是第 65-71 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_read_registry — `_read_registry` 是第 74-85 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_registry_lock — `_registry_lock` 是第 89-113 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_locked_registry — `_locked_registry` 是第 117-127 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_audit_event — `_audit_event` 是第 130-140 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_capability — `_capability` 是第 143-144 行的函数，供所属页面定位实现。"} {:block/title "内部实现：harness_capabilities — `harness_capabilities` 是第 147-161 行的函数，供所属页面定位实现。"} {:block/title "内部实现：binding_schema — `binding_schema` 是第 164-217 行的函数，供所属页面定位实现。"} {:block/title "内部实现：canonical_binding_input — `canonical_binding_input` 是第 220-248 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_privacy_errors — `_privacy_errors` 是第 251-262 行的函数，供所属页面定位实现。"} {:block/title "内部实现：audit_manager_registry — `audit_manager_registry` 是第 265-352 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_git — `_git` 是第 355-362 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_is_within — `_is_within` 是第 365-368 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_git_preflight — `_git_preflight` 是第 371-414 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_binding_identity — `_binding_identity` 是第 417-418 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_binding_project — `_binding_project` 是第 421-422 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_binding_id — `_binding_id` 是第 425-432 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_find_binding — `_find_binding` 是第 553-559 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_runtime_state — `_runtime_state` 是第 562-602 行的函数，供所属页面定位实现。"} {:block/title "内部实现：binding_status — `binding_status` 是第 605-625 行的函数，供所属页面定位实现。"} {:block/title "内部实现：unbind_conversation — `unbind_conversation` 是第 628-669 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_sqlite_integrity — `_sqlite_integrity` 是第 672-683 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_feedback_snapshot — `_feedback_snapshot` 是第 686-704 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_knowledge_snapshot — `_knowledge_snapshot` 是第 707-743 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_single_quote — `_single_quote` 是第 746-747 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_manager_commands — `_manager_commands` 是第 750-767 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_management_prompt — `_management_prompt` 是第 770-843 行的函数，供所属页面定位实现。"} {:block/title "内部实现：audit_management_prompt — `audit_management_prompt` 是第 846-883 行的函数，供所属页面定位实现。"} {:block/title "内部实现：management_context — `management_context` 是第 886-932 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_task_id — `_task_id` 是第 935-936 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_task_artifact_root — `_task_artifact_root` 是第 939-940 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_find_task — `_find_task` 是第 943-944 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_bounded_values — `_bounded_values` 是第 947-957 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_task_prompt — `_task_prompt` 是第 960-1011 行的函数，供所属页面定位实现。"} {:block/title "内部实现：create_management_task — `create_management_task` 是第 1014-1153 行的函数，供所属页面定位实现。"} {:block/title "内部实现：management_task_status — `management_task_status` 是第 1156-1237 行的函数，供所属页面定位实现。"} {:block/title "内部实现：review_management_task — `review_management_task` 是第 1240-1336 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[_Transport.close 与 _StartGate 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[append 等测试场景]]。"} {:block/title "协作：实现时会用到 [[audit_gap_register 与 _root 的协作实现]]。"} {:block/title "协作：主要代码单元是 [[bind_conversation]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[maintenance_check]]。"} {:block/title "协作：实现时会用到 [[maintenance_check 与 capability_matrix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[append]] 会使用这里提供的行为。"} {:block/title "协作：[[append 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[bind_conversation]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "bind_reference"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`bind_reference` 是 `tests/fixtures/cpp-parser-scons/reference-direct-init-valid.cpp` 第 3-6 行定义的函数，本页绑定该固定源码范围。 该函数作为可执行验证入口，检查标识符 `bind_reference` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `bind_reference` 的说明。"} {:block/title "源码入口：[打开源码：tests/fixtures/cpp-parser-scons/reference-direct-init-valid.cpp 第 3 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/fixtures/cpp-parser-scons/reference-direct-init-valid.cpp:3:1)  `tests/fixtures/cpp-parser-scons/reference-direct-init-valid.cpp:3-6`"} {:block/title "内部实现：bind_reference.x — `bind_reference.x` 是第 5-5 行的源码实体，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[bind_reference 等测试场景]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[bind_reference 等测试场景]] 汇总了本页。"}]}
{:page {:block/title "bind_reference 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/fixtures/cpp-parser-scons/reference-direct-init-valid.cpp` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `reference-direct-init-valid.cpp` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/fixtures/cpp-parser-scons/reference-direct-init-valid.cpp` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/fixtures/cpp-parser-scons/reference-direct-init-valid.cpp 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/fixtures/cpp-parser-scons/reference-direct-init-valid.cpp:1:1)  `tests/fixtures/cpp-parser-scons/reference-direct-init-valid.cpp:1-7`"} {:block/title "内部实现：T — `T` 是第 1-1 行的类，供所属页面定位实现。"} {:block/title "内部实现：T.T — `T.T` 是第 1-1 行的函数，供所属页面定位实现。"} {:block/title "内部实现：source_value — `source_value` 是第 2-2 行的函数，供所属页面定位实现。"} {:block/title "协作：主要代码单元是 [[bind_reference]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[bind_reference]] 覆盖相关行为。"} {:block/title "协作：[[bind_reference]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "build 的协作边界"} :blocks [{:block/title "标签：#类型/边界"} {:block/title "页面说明：这里汇集当前扫描范围之外、但与所选功能直接协作的代码。"} {:block/title "边界协作：run — 位于 plugins/obsidian-code-knowledge-builder/scripts/build.py:26-45，本次未继续展开。"} {:block/title "边界协作：validate_selection_provider_routing — 位于 plugins/obsidian-code-knowledge-builder/scripts/build.py:58-180，本次未继续展开。"} {:block/title "边界协作：main — 位于 plugins/obsidian-code-knowledge-builder/scripts/build.py:187-313，本次未继续展开。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：实现时会用到 [[retrieve]]。"}]}
{:page {:block/title "command"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`command` 是 `tests/e2e_knowledge_batch_migration.py` 第 42-65 行定义的函数，本页绑定该固定源码范围。 该函数作为可执行验证入口，检查标识符 `command` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `command` 的说明。"} {:block/title "源码入口：[打开源码：tests/e2e_knowledge_batch_migration.py 第 42 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/e2e_knowledge_batch_migration.py:42:1)  `tests/e2e_knowledge_batch_migration.py:42-65`"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.register 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[CkbError 与 DependencyError 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests]] 会使用这里提供的行为。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[_Transport.close 与 _StartGate 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_obsidian 与 prepare_vault 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_operation_journal 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_output_contract 与 _default_ckb 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[build 的协作边界]] 会使用这里提供的行为。"} {:block/title "协作：[[command 等测试场景]] 汇总了本页。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[deploy 的协作边界]] 会使用这里提供的行为。"} {:block/title "协作：[[doctor_report]] 会使用这里提供的行为。"} {:block/title "协作：[[doctor_report 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[execute]] 会使用这里提供的行为。"} {:block/title "协作：[[execute 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[keyword_provider_config]] 会使用这里提供的行为。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[normalize 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[preflight 与 git 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[run 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[run_keyword_provider]] 会使用这里提供的行为。"} {:block/title "协作：[[run_keyword_provider 与 KeywordProviderConfig 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[serve_stdio 与 _write_line 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "command 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/e2e_knowledge_batch_migration.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `e2e_knowledge_batch_migration.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/e2e_knowledge_batch_migration.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/e2e_knowledge_batch_migration.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/e2e_knowledge_batch_migration.py:1:1)  `tests/e2e_knowledge_batch_migration.py:1-318`"} {:block/title "内部实现：historical_source — `historical_source` 是第 68-76 行的函数，供所属页面定位实现。"} {:block/title "内部实现：fixture_repository — `fixture_repository` 是第 79-93 行的函数，供所属页面定位实现。"} {:block/title "内部实现：historical_output — `historical_output` 是第 96-152 行的函数，供所属页面定位实现。"} {:block/title "内部实现：project_manifest — `project_manifest` 是第 155-206 行的函数，供所属页面定位实现。"} {:block/title "内部实现：run_e2e — `run_e2e` 是第 209-302 行的函数，供所属页面定位实现。"} {:block/title "内部实现：main — `main` 是第 305-313 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：主要代码单元是 [[command]]。"} {:block/title "协作：实现时会用到 [[create_knowledge_batch_plan]]。"} {:block/title "协作：实现时会用到 [[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：实现时会用到 [[preflight]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[start_scope_extension 与 _error 的协作实现]]。"} {:block/title "协作：[[AutomationTest.register]] 关联到这里的验证场景。"} {:block/title "协作：[[_Transport.close 与 _StartGate 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[append 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_feedback]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_gap_register]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_obsidian]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_obsidian 与 prepare_vault 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_operation_journal]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_operation_journal 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_output_contract]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_output_contract 与 _default_ckb 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_references 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_work_record_index 与 _contains_chinese 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[bind_conversation]] 关联到这里的验证场景。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[create_knowledge_batch_plan]] 关联到这里的验证场景。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config]] 关联到这里的验证场景。"} {:block/title "协作：[[maintenance_check]] 关联到这里的验证场景。"} {:block/title "协作：[[maintenance_check 与 capability_matrix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[parser]] 关联到这里的验证场景。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note]] 关联到这里的验证场景。"} {:block/title "协作：[[refresh]] 关联到这里的验证场景。"} {:block/title "协作：[[refresh 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine]] 关联到这里的验证场景。"} {:block/title "协作：[[run_keyword_provider]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer]] 关联到这里的验证场景。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "create_batch_plan"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`create_batch_plan` 是 `scripts/ckb_core/agent_protocol_batch.py` 第 657-714 行定义的函数，本页绑定该固定源码范围。 负责批量升级 Agent Protocol，包括计划、锁、备份、审计、切换和回滚。"} {:block/title "需要修改时：当 `create_batch_plan` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/agent_protocol_batch.py 第 657 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_protocol_batch.py:657:1)  `scripts/ckb_core/agent_protocol_batch.py:657-714`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[create_batch_plan 与 ProtocolRelease 的协作实现]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[AgentProtocolBatchApplyTests]] 会使用这里提供的行为。"} {:block/title "协作：[[AgentProtocolBatchApplyTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "create_batch_plan 与 ProtocolRelease 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/agent_protocol_batch.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责批量升级 Agent Protocol，包括计划、锁、备份、审计、切换和回滚。"} {:block/title "需要修改时：当 `scripts/ckb_core/agent_protocol_batch.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/agent_protocol_batch.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_protocol_batch.py:1:1)  `scripts/ckb_core/agent_protocol_batch.py:1-1714`"} {:block/title "内部实现：ProtocolRelease — `ProtocolRelease` 是第 68-73 行的类，供所属页面定位实现。"} {:block/title "内部实现：supported_upgrade_path — `supported_upgrade_path` 是第 112-126 行的函数，供所属页面定位实现。"} {:block/title "内部实现：version_matrix — `version_matrix` 是第 129-143 行的函数，供所属页面定位实现。"} {:block/title "内部实现：reject_unknown_fields — `reject_unknown_fields` 是第 146-149 行的函数，供所属页面定位实现。"} {:block/title "内部实现：require_absolute_path — `require_absolute_path` 是第 152-158 行的函数，供所属页面定位实现。"} {:block/title "内部实现：BatchProjectError — `BatchProjectError` 是第 161-164 行的类，供所属页面定位实现。"} {:block/title "内部实现：BatchProjectError.__init__ — `BatchProjectError.__i...` 是第 162-164 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_canonical_bytes — `_canonical_bytes` 是第 167-170 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_digest_value — `_digest_value` 是第 173-174 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_sha256_bytes — `_sha256_bytes` 是第 177-178 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_single_quote — `_single_quote` 是第 181-182 行的函数，供所属页面定位实现。"} {:block/title "内部实现：command_examples_for_version — `command_examples_for_...` 是第 185-224 行的函数，供所属页面定位实现。"} {:block/title "内部实现：protocol_text_for_version — `protocol_text_for_ver...` 是第 227-378 行的函数，供所属页面定位实现。"} {:block/title "内部实现：adapter_texts_for_version — `adapter_texts_for_ver...` 是第 381-382 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_record_roots — `_record_roots` 是第 385-391 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_tracked_paths — `_tracked_paths` 是第 394-419 行的函数，供所属页面定位实现。"} {:block/title "内部实现：snapshot_files — `snapshot_files` 是第 422-435 行的函数，供所属页面定位实现。"} {:block/title "内部实现：snapshot_digest — `snapshot_digest` 是第 438-439 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_normalized_managed_block — `_normalized_managed_b...` 是第 442-447 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_validate_workspace_managed — `_validate_workspace_m...` 是第 450-465 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_validate_internal_adapters — `_validate_internal_ad...` 是第 468-482 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_within_any — `_within_any` 是第 485-486 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_validate_structural_manifest — `_validate_structural_...` 是第 489-527 行的函数，供所属页面定位实现。"} {:block/title "内部实现：load_batch_manifest — `load_batch_manifest` 是第 530-538 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_inspect_project — `_inspect_project` 是第 541-654 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_load_batch_plan — `_load_batch_plan` 是第 717-732 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_json_bytes — `_json_bytes` 是第 735-736 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_replace_workspace_block_bytes — `_replace_workspace_bl...` 是第 739-759 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_target_record — `_target_record` 是第 762-811 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_desired_project_files — `_desired_project_files` 是第 814-863 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_state_file — `_state_file` 是第 866-873 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_write_bytes_atomic — `_write_bytes_atomic` 是第 876-881 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_desired_inventory — `_desired_inventory` 是第 884-897 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_create_backup — `_create_backup` 是第 900-932 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_restore_backup — `_restore_backup` 是第 935-950 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_commit_desired — `_commit_desired` 是第 953-964 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_descriptor_lock — `_descriptor_lock` 是第 967-984 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_descriptor_unlock — `_descriptor_unlock` 是第 987-996 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_descriptor_bytes — `_descriptor_bytes` 是第 999-1006 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_write_lock_descriptor — `_write_lock_descriptor` 是第 1009-1016 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_process_start_identity — `_process_start_identity` 是第 1019-1075 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_new_output_lock_record — `_new_output_lock_record` 是第 1078-1092 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_parse_output_lock — `_parse_output_lock` 是第 1095-1111 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_lock_owner_state — `_lock_owner_state` 是第 1114-1124 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_legacy_lock_owner_state — `_legacy_lock_owner_state` 是第 1127-1139 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_same_lock_file — `_same_lock_file` 是第 1142-1148 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_release_output_lock — `_release_output_lock` 是第 1151-1174 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_output_lock — `_output_lock` 是第 1178-1256 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_append_state_event — `_append_state_event` 是第 1259-1271 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_save_state — `_save_state` 是第 1274-1276 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_load_state — `_load_state` 是第 1279-1290 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_new_state — `_new_state` 是第 1293-1345 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_current_digest — `_current_digest` 是第 1348-1351 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_recovery_matches — `_recovery_matches` 是第 1354-1367 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_write_project_evidence — `_write_project_evidence` 是第 1370-1392 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_journal_batch_result — `_journal_batch_result` 是第 1395-1400 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_summarize_state — `_summarize_state` 是第 1403-1411 行的函数，供所属页面定位实现。"} {:block/title "内部实现：apply_batch_plan — `apply_batch_plan` 是第 1414-1516 行的函数，供所属页面定位实现。"} {:block/title "内部实现：batch_status — `batch_status` 是第 1519-1560 行的函数，供所属页面定位实现。"} {:block/title "内部实现：audit_batch_state — `audit_batch_state` 是第 1563-1623 行的函数，供所属页面定位实现。"} {:block/title "内部实现：rollback_batch_state — `rollback_batch_state` 是第 1626-1713 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_agent_protocol]]。"} {:block/title "协作：实现时会用到 [[audit_agent_protocol 与 _default_python 的协作实现]]。"} {:block/title "协作：实现时会用到 [[audit_operation_journal 与 _root 的协作实现]]。"} {:block/title "协作：实现时会用到 [[audit_output_contract 与 _default_ckb 的协作实现]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：主要代码单元是 [[create_batch_plan]]。"} {:block/title "协作：实现时会用到 [[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve]]。"} {:block/title "协作：实现时会用到 [[start_scope_extension 与 _error 的协作实现]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[run]] 覆盖相关行为。"} {:block/title "协作：由 [[run 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[AgentProtocolBatchApplyTests]] 会使用这里提供的行为。"} {:block/title "协作：[[AgentProtocolBatchApplyTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[create_batch_plan]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[run]] 会使用这里提供的行为。"} {:block/title "协作：[[run 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "create_knowledge_batch_plan"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`create_knowledge_batc...` 是 `scripts/ckb_core/knowledge_batch_migration.py` 第 888-956 行定义的函数，本页绑定该固定源码范围。 负责完整知识库批量迁移的计划、隔离构建、审计、切换和精确回滚。"} {:block/title "需要修改时：当 `create_knowledge_batch_plan` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/knowledge_batch_migration.py 第 888 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/knowledge_batch_migration.py:888:1)  `scripts/ckb_core/knowledge_batch_migration.py:888-956`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[create_batch_plan 与 ProtocolRelease 的协作实现]]。"} {:block/title "协作：实现时会用到 [[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]]。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[command 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 汇总了本页。"} {:block/title "协作：[[refresh 等测试场景]] 会使用这里提供的行为。"}]}
{:page {:block/title "create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/knowledge_batch_migration.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责完整知识库批量迁移的计划、隔离构建、审计、切换和精确回滚。"} {:block/title "需要修改时：当 `scripts/ckb_core/knowledge_batch_migration.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/knowledge_batch_migration.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/knowledge_batch_migration.py:1:1)  `scripts/ckb_core/knowledge_batch_migration.py:1-2076`"} {:block/title "内部实现：KnowledgeRelease — `KnowledgeRelease` 是第 113-121 行的类，供所属页面定位实现。"} {:block/title "内部实现：_canonical_bytes — `_canonical_bytes` 是第 191-192 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_digest_value — `_digest_value` 是第 195-196 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_reject_unknown — `_reject_unknown` 是第 199-202 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_object — `_object` 是第 205-208 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_absolute — `_absolute` 是第 211-217 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_within_any — `_within_any` 是第 220-221 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_normalized_protocol — `_normalized_protocol` 是第 224-227 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_release_for — `_release_for` 是第 230-240 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_release_chain — `_release_chain` 是第 243-252 行的函数，供所属页面定位实现。"} {:block/title "内部实现：knowledge_version_matrix — `knowledge_version_matrix` 是第 255-287 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_validate_version — `_validate_version` 是第 290-302 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_validate_snapshot — `_validate_snapshot` 是第 305-311 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_validate_tree_summary — `_validate_tree_summary` 是第 314-324 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_validate_structural_manifest — `_validate_structural_...` 是第 327-373 行的函数，供所属页面定位实现。"} {:block/title "内部实现：load_knowledge_batch_manifest — `load_knowledge_batch_...` 是第 376-384 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_file_record — `_file_record` 是第 387-394 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_origin_health — `_origin_health` 是第 397-491 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_origin_health.check — `_origin_health.check` 是第 400-401 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_complete_layer_inventory — `_complete_layer_inven...` 是第 494-523 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_sqlite_schema_versions — `_sqlite_schema_versions` 是第 526-542 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_normalized_scope — `_normalized_scope` 是第 545-561 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_path_risks — `_path_risks` 是第 564-569 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_operation_token — `_operation_token` 是第 572-573 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_project_operation_id — `_project_operation_id` 是第 576-586 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_paths_overlap — `_paths_overlap` 是第 589-590 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_validate_recovery_topology — `_validate_recovery_to...` 是第 593-634 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_validate_origin_record_keys — `_validate_origin_reco...` 是第 637-660 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_inspect_knowledge_project — `_inspect_knowledge_pr...` 是第 663-885 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_load_knowledge_batch_plan — `_load_knowledge_batch...` 是第 959-972 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_verify_plan_manifest_binding — `_verify_plan_manifest...` 是第 975-990 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_save_knowledge_state — `_save_knowledge_state` 是第 993-995 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_load_knowledge_state — `_load_knowledge_state` 是第 998-1007 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_state_event — `_state_event` 是第 1010-1029 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_new_knowledge_state — `_new_knowledge_state` 是第 1032-1068 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_summarize_knowledge_state — `_summarize_knowledge_...` 是第 1071-1092 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_manifest_matches — `_manifest_matches` 是第 1095-1096 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_manifest_delta — `_manifest_delta` 是第 1099-1113 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_verify_plan_bindings — `_verify_plan_bindings` 是第 1116-1126 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_knowledge_output_lock — `_knowledge_output_lock` 是第 1129-1139 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_detach_staging_workspace_roots — `_detach_staging_works...` 是第 1142-1157 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_cold_build — `_cold_build` 是第 1160-1232 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_project_record — `_project_record` 是第 1235-1260 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_pending_reviews — `_pending_reviews` 是第 1263-1265 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_mutable_preservation_check — `_mutable_preservation...` 是第 1268-1280 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_old_entity_id_errors — `_old_entity_id_errors` 是第 1283-1289 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_knowledge_project_audit — `_knowledge_project_audit` 是第 1292-1424 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_knowledge_project_audit.check — `_knowledge_project_au...` 是第 1297-1298 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_apply_one_project — `_apply_one_project` 是第 1427-1503 行的函数，供所属页面定位实现。"} {:block/title "内部实现：apply_knowledge_batch_plan — `apply_knowledge_batch...` 是第 1506-1552 行的函数，供所属页面定位实现。"} {:block/title "内部实现：resume_knowledge_batch_state — `resume_knowledge_batc...` 是第 1555-1561 行的函数，供所属页面定位实现。"} {:block/title "内部实现：knowledge_batch_status — `knowledge_batch_status` 是第 1564-1620 行的函数，供所属页面定位实现。"} {:block/title "内部实现：audit_knowledge_batch_state — `audit_knowledge_batch...` 是第 1623-1679 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_control_path — `_control_path` 是第 1682-1683 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_control_records — `_control_records` 是第 1686-1695 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_active_control — `_active_control` 是第 1698-1709 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_protocol_digest — `_protocol_digest` 是第 1712-1718 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_cutover_one — `_cutover_one` 是第 1721-1880 行的函数，供所属页面定位实现。"} {:block/title "内部实现：cutover_knowledge_batch_state — `cutover_knowledge_bat...` 是第 1883-1923 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_rollback_one — `_rollback_one` 是第 1926-2032 行的函数，供所属页面定位实现。"} {:block/title "内部实现：rollback_knowledge_batch_state — `rollback_knowledge_ba...` 是第 2035-2075 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_agent_protocol]]。"} {:block/title "协作：实现时会用到 [[audit_agent_protocol 与 _default_python 的协作实现]]。"} {:block/title "协作：实现时会用到 [[audit_gap_register]]。"} {:block/title "协作：实现时会用到 [[audit_migration]]。"} {:block/title "协作：实现时会用到 [[audit_migration 与 _entity_key 的协作实现]]。"} {:block/title "协作：实现时会用到 [[audit_operation_journal]]。"} {:block/title "协作：实现时会用到 [[audit_references]]。"} {:block/title "协作：实现时会用到 [[create_batch_plan 与 ProtocolRelease 的协作实现]]。"} {:block/title "协作：主要代码单元是 [[create_knowledge_batch_plan]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[initialize]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[refresh 等测试场景]]。"} {:block/title "协作：实现时会用到 [[start_scope_extension 与 _error 的协作实现]]。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[command 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[refresh 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "debug_value 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/fixtures/cpp-parser-scons/conditional-valid.cpp` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `conditional-valid.cpp` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/fixtures/cpp-parser-scons/conditional-valid.cpp` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/fixtures/cpp-parser-scons/conditional-valid.cpp 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/fixtures/cpp-parser-scons/conditional-valid.cpp:1:1)  `tests/fixtures/cpp-parser-scons/conditional-valid.cpp:1-6`"} {:block/title "内部实现：debug_value — `debug_value` 是第 2-4 行的函数，供所属页面定位实现。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "deploy 的协作边界"} :blocks [{:block/title "标签：#类型/边界"} {:block/title "页面说明：这里汇集当前扫描范围之外、但与所选功能直接协作的代码。"} {:block/title "边界协作：_write_contract — 位于 plugins/obsidian-code-knowledge-builder/deploy.py:59-78，本次未继续展开。"} {:block/title "边界协作：deploy — 位于 plugins/obsidian-code-knowledge-builder/deploy.py:81-100，本次未继续展开。"} {:block/title "边界协作：status — 位于 plugins/obsidian-code-knowledge-builder/deploy.py:103-115，本次未继续展开。"} {:block/title "边界协作：remove — 位于 plugins/obsidian-code-knowledge-builder/deploy.py:118-132，本次未继续展开。"} {:block/title "边界协作：main — 位于 plugins/obsidian-code-knowledge-builder/deploy.py:135-148，本次未继续展开。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：实现时会用到 [[retrieve]]。"}]}
{:page {:block/title "deployment_plan"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`deployment_plan` 是 `scripts/ckb_core/runtime.py` 中负责根据锁定运行时清单生成所需组件、来源和部署动作的函数。 它按源码所示的参数、条件分支和数据结构完成根据锁定运行时清单生成所需组件、来源和部署动作，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当scripts/ckb_core/runtime.py 的职责的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/runtime.py 第 26 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/runtime.py:26:1)  `scripts/ckb_core/runtime.py:26-67`"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[deployment_plan 与 skill_root 的协作实现]]。"} {:block/title "协作：实现时会用到 [[doctor_report 与 _version_matches 的协作实现]]。"} {:block/title "协作：[[deployment_plan 与 skill_root 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "deployment_plan 与 skill_root 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现私有离线运行时部署、复用检查和移除。 它是 Code Knowledge Builder 中承载私有离线运行时部署、复用检查和移除的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当私有离线运行时部署、复用检查和移除的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/runtime.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/runtime.py:1:1)  `scripts/ckb_core/runtime.py:1-153`"} {:block/title "内部实现：skill_root — 该附属代码负责私有离线运行时部署、复用检查和移除，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：lock_document — 该附属代码负责私有离线运行时部署、复用检查和移除，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：payload_path — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：deploy — 该附属代码负责管理隔离离线运行时及其回滚，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：remove — 移除 `remove` 对应的数据与约束。"} {:block/title "内部实现：_host_snapshot — 该附属代码负责建立并验证与固定提交一致的源码快照，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：主要代码单元是 [[deployment_plan]]。"} {:block/title "协作：实现时会用到 [[doctor_report 与 _version_matches 的协作实现]]。"} {:block/title "协作：[[deployment_plan]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "doctor_report"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`doctor_report` 是 `scripts/ckb_core/providers.py` 第 76-240 行定义的函数，本页绑定该固定源码范围。 负责语义提供器调用、结果校验和固定实体证据绑定。"} {:block/title "需要修改时：当 `doctor_report` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/providers.py 第 76 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/providers.py:76:1)  `scripts/ckb_core/providers.py:76-240`"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[doctor_report 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[query_graph 与 _networkx_modules 的协作实现]]。"} {:block/title "协作：[[doctor_report 与 _version_matches 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "doctor_report 与 _version_matches 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/providers.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责语义提供器调用、结果校验和固定实体证据绑定。"} {:block/title "需要修改时：当 `scripts/ckb_core/providers.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/providers.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/providers.py:1:1)  `scripts/ckb_core/providers.py:1-619`"} {:block/title "内部实现：_version_matches — `_version_matches` 是第 32-34 行的函数，供所属页面定位实现。"} {:block/title "内部实现：private_runtime_root — `private_runtime_root` 是第 37-41 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_runtime_bin_candidates — `_runtime_bin_candidates` 是第 44-54 行的函数，供所属页面定位实现。"} {:block/title "内部实现：resolve_executable — `resolve_executable` 是第 57-73 行的函数，供所属页面定位实现。"} {:block/title "内部实现：path_to_uri — `path_to_uri` 是第 243-247 行的函数，供所属页面定位实现。"} {:block/title "内部实现：uri_to_path — `uri_to_path` 是第 250-255 行的函数，供所属页面定位实现。"} {:block/title "内部实现：LspClient — `LspClient` 是第 258-368 行的类，供所属页面定位实现。"} {:block/title "内部实现：LspClient.__init__ — `LspClient.__init__` 是第 259-268 行的函数，供所属页面定位实现。"} {:block/title "内部实现：LspClient.start — `LspClient.start` 是第 270-281 行的函数，供所属页面定位实现。"} {:block/title "内部实现：LspClient._read_stdout — `LspClient._read_stdout` 是第 283-302 行的函数，供所属页面定位实现。"} {:block/title "内部实现：LspClient._read_stderr — `LspClient._read_stderr` 是第 304-307 行的函数，供所属页面定位实现。"} {:block/title "内部实现：LspClient.send — `LspClient.send` 是第 309-315 行的函数，供所属页面定位实现。"} {:block/title "内部实现：LspClient.notify — `LspClient.notify` 是第 317-318 行的函数，供所属页面定位实现。"} {:block/title "内部实现：LspClient.request — `LspClient.request` 是第 320-350 行的函数，供所属页面定位实现。"} {:block/title "内部实现：LspClient.stop — `LspClient.stop` 是第 352-368 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_provider_spec — `_provider_spec` 是第 371-430 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_fallback_flags — `_fallback_flags` 是第 433-460 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_flatten_symbols — `_flatten_symbols` 是第 463-472 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_provider_status — `_provider_status` 是第 475-486 行的函数，供所属页面定位实现。"} {:block/title "内部实现：collect_semantics — `collect_semantics` 是第 489-618 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：主要代码单元是 [[doctor_report]]。"} {:block/title "协作：实现时会用到 [[initialize]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[render_integration 与 _looks_windows 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.register 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests]] 会使用这里提供的行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[SessionStdioLifecycleTests]] 会使用这里提供的行为。"} {:block/title "协作：[[_Transport.close 与 _StartGate 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[deployment_plan]] 会使用这里提供的行为。"} {:block/title "协作：[[deployment_plan 与 skill_root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[doctor_report]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（fake_logseq 测试）]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "emit"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`emit` 是 `scripts/ckb.py` 第 749-752 行定义的函数，本页绑定该固定源码范围。 负责注册 CKB 命令、校验参数，并把子命令分派到对应的知识库实现。"} {:block/title "需要修改时：当 `emit` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb.py 第 749 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb.py:749:1)  `scripts/ckb.py:749-752`"} {:block/title "协作：实现时会用到 [[audit_operation_journal 与 _root 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 汇总了本页。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "execute"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`execute` 是 `tests/provider_integration.py` 第 19-24 行定义的函数，本页绑定该固定源码范围。 该函数作为可执行验证入口，检查标识符 `execute` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `execute` 的说明。"} {:block/title "源码入口：[打开源码：tests/provider_integration.py 第 19 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/provider_integration.py:19:1)  `tests/provider_integration.py:19-24`"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[MigrationTest]] 会使用这里提供的行为。"} {:block/title "协作：[[ScopeExtensionTest]] 会使用这里提供的行为。"} {:block/title "协作：[[_Transport.close 与 _StartGate 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_agent_protocol 与 _default_python 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_gap_register]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_references]] 会使用这里提供的行为。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[doctor_report]] 会使用这里提供的行为。"} {:block/title "协作：[[execute 等测试场景]] 汇总了本页。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（fake_logseq 测试）]] 会使用这里提供的行为。"} {:block/title "协作：[[normalize 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[start_scope_extension 与 _error 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "execute 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/provider_integration.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `provider_integration.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/provider_integration.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/provider_integration.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/provider_integration.py:1:1)  `tests/provider_integration.py:1-325`"} {:block/title "内部实现：git — `git` 是第 27-30 行的函数，供所属页面定位实现。"} {:block/title "内部实现：provider_case — `provider_case` 是第 33-65 行的函数，供所属页面定位实现。"} {:block/title "内部实现：clangd_case — `clangd_case` 是第 68-156 行的函数，供所属页面定位实现。"} {:block/title "内部实现：csharp_case — `csharp_case` 是第 159-207 行的函数，供所属页面定位实现。"} {:block/title "内部实现：main — `main` 是第 210-320 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[Box 等测试场景（main 测试）]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：主要代码单元是 [[execute]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：[[Box 等测试场景（main 测试）]] 关联到这里的验证场景。"} {:block/title "协作：[[_Transport.close 与 _StartGate 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[append 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[bind_conversation]] 关联到这里的验证场景。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[parser]] 关联到这里的验证场景。"} {:block/title "协作：[[refresh]] 关联到这里的验证场景。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "ingest_event"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`ingest_event` 是 `scripts/ckb_core/automation.py` 第 1394-1504 行定义的函数，本页绑定该固定源码范围。 负责跨 Harness 自动化事件、会话状态、任务队列和 SQLite 状态的确定性处理。"} {:block/title "需要修改时：当 `ingest_event` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/automation.py 第 1394 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation.py:1394:1)  `scripts/ckb_core/automation.py:1394-1504`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[_Transport.close 与 _StartGate 的协作实现]]。"} {:block/title "协作：实现时会用到 [[ingest_event 与 default_registry_path 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[SessionStdioLifecycleTests]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.register 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[SessionStdioLifecycleTests]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "ingest_event 与 default_registry_path 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/automation.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责跨 Harness 自动化事件、会话状态、任务队列和 SQLite 状态的确定性处理。"} {:block/title "需要修改时：当 `scripts/ckb_core/automation.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/automation.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation.py:1:1)  `scripts/ckb_core/automation.py:1-1744`"} {:block/title "内部实现：default_registry_path — `default_registry_path` 是第 127-129 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_path_key — `_path_key` 是第 132-135 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_is_within — `_is_within` 是第 138-141 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_read_registry — `_read_registry` 是第 144-171 行的函数，供所属页面定位实现。"} {:block/title "内部实现：register_project — `register_project` 是第 174-246 行的函数，供所属页面定位实现。"} {:block/title "内部实现：unregister_project — `unregister_project` 是第 249-261 行的函数，供所属页面定位实现。"} {:block/title "内部实现：registry_status — `registry_status` 是第 264-273 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_registration_for_event — `_registration_for_event` 是第 276-293 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_event_path_text — `_event_path_text` 是第 296-324 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_walk_values — `_walk_values` 是第 327-335 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_first_scalar — `_first_scalar` 是第 338-355 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_text_content — `_text_content` 是第 358-373 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_event_name — `_event_name` 是第 376-384 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_message_role — `_message_role` 是第 387-388 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_normalized_skill_name — `_normalized_skill_name` 是第 391-393 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_skill_name — `_skill_name` 是第 396-413 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_canonical_type — `_canonical_type` 是第 416-463 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_extract_paths — `_extract_paths` 是第 466-476 行的函数，供所属页面定位实现。"} {:block/title "内部实现：normalize_event — `normalize_event` 是第 479-549 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_redact_text — `_redact_text` 是第 552-576 行的函数，供所属页面定位实现。"} {:block/title "内部实现：redact_event — `redact_event` 是第 579-602 行的函数，供所属页面定位实现。"} {:block/title "内部实现：redact_event.redact — `redact_event.redact` 是第 583-597 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_automation_root — `_automation_root` 是第 605-609 行的函数，供所属页面定位实现。"} {:block/title "内部实现：initialize_automation_database — `initialize_automation...` 是第 612-724 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_git_status_paths — `_git_status_paths` 是第 727-747 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_change_path_allowed — `_change_path_allowed` 是第 750-752 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_working_file_state — `_working_file_state` 是第 755-770 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_relative_changed_paths — `_relative_changed_paths` 是第 773-804 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_drain_lock — `_drain_lock` 是第 808-835 行的函数，供所属页面定位实现。"} {:block/title "内部实现：enqueue_event — `enqueue_event` 是第 838-845 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_spool_events — `_spool_events` 是第 848-849 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_session_key — `_session_key` 是第 852-853 行的函数，供所属页面定位实现。"} {:block/title "内部实现：default_session_id — `default_session_id` 是第 856-861 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_activation_key — `_activation_key` 是第 864-865 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_record_skill_activation — `_record_skill_activation` 是第 868-905 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_skill_activation — `_skill_activation` 是第 908-922 行的函数，供所属页面定位实现。"} {:block/title "内部实现：activate_skill_session — `activate_skill_session` 是第 925-967 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_remove_skill_activation — `_remove_skill_activation` 是第 970-982 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_explicit_skill_application — `_explicit_skill_appli...` 是第 985-995 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_ensure_session — `_ensure_session` 是第 998-1027 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_resolve_turn — `_resolve_turn` 是第 1030-1083 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_event_id — `_event_id` 是第 1086-1107 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_pending_review_content — `_pending_review_content` 是第 1110-1136 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_create_pending_review — `_create_pending_review` 是第 1139-1212 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_process_event — `_process_event` 是第 1215-1301 行的函数，供所属页面定位实现。"} {:block/title "内部实现：drain_automation — `drain_automation` 是第 1304-1350 行的函数，供所属页面定位实现。"} {:block/title "内部实现：retry_failed_automation — `retry_failed_automation` 是第 1353-1367 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_hook_context — `_hook_context` 是第 1370-1376 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_hook_output — `_hook_output` 是第 1379-1391 行的函数，供所属页面定位实现。"} {:block/title "内部实现：automation_status — `automation_status` 是第 1507-1534 行的函数，供所属页面定位实现。"} {:block/title "内部实现：pending_automation_reviews — `pending_automation_re...` 是第 1537-1559 行的函数，供所属页面定位实现。"} {:block/title "内部实现：write_automation_review_template — `write_automation_revi...` 是第 1562-1602 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_heading_errors — `_heading_errors` 是第 1605-1613 行的函数，供所属页面定位实现。"} {:block/title "内部实现：review_automation — `review_automation` 是第 1616-1690 行的函数，供所属页面定位实现。"} {:block/title "内部实现：search_automation — `search_automation` 是第 1693-1719 行的函数，供所属页面定位实现。"} {:block/title "内部实现：automation_documents — `automation_documents` 是第 1722-1743 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[_Transport.close 与 _StartGate 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[doctor_report 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：主要代码单元是 [[ingest_event]]。"} {:block/title "协作：实现时会用到 [[record_note]]。"} {:block/title "协作：实现时会用到 [[record_note 与 page_tag 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.register]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.register]] 会使用这里提供的行为。"} {:block/title "协作：[[AutomationTest.register 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[SessionStdioLifecycleTests]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "initialize"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`initialize` 是 `scripts/ckb_core/pipeline.py` 第 676-922 行定义的函数，本页绑定该固定源码范围。 负责初始化、解析、Agent 审阅、全局审计和最终知识层投影。"} {:block/title "需要修改时：当 `initialize` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/pipeline.py 第 676 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/pipeline.py:676:1)  `scripts/ckb_core/pipeline.py:676-922`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[load_page_config]]。"} {:block/title "协作：实现时会用到 [[load_page_config 与 _merge_known 的协作实现]]。"} {:block/title "协作：实现时会用到 [[module_name 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[parse_file]]。"} {:block/title "协作：实现时会用到 [[parse_file 与 _language 的协作实现]]。"} {:block/title "协作：实现时会用到 [[preflight]]。"} {:block/title "协作：实现时会用到 [[preflight 与 git 的协作实现]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[append 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[MigrationTest]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[doctor_report 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 汇总了本页。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[preflight 与 git 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[run 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[start_scope_extension]] 会使用这里提供的行为。"}]}
{:page {:block/title "initialize 与 _replace_output_prefix 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/pipeline.py` 是 `scripts/ckb_core/pipeline.py` 中负责汇总并提供固定快照构建、审阅、合并、最终审计和完成状态机的文件入口。 它按源码所示的参数、条件分支和数据结构完成汇总并提供固定快照构建、审阅、合并、最终审计和完成状态机，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当固定快照构建、审阅、合并、最终审计和完成状态机的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/pipeline.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/pipeline.py:1:1)  `scripts/ckb_core/pipeline.py:1-3482`"} {:block/title "内部实现：_replace_output_prefix — 替换 `output_prefix` 对应的数据与约束。"} {:block/title "内部实现：_relocate_completed_output — 处理 `completed_output` 对应的数据与约束。"} {:block/title "内部实现：_load_state — 加载 `state` 对应的数据与约束。"} {:block/title "内部实现：_module — 处理 `module` 对应的数据与约束。"} {:block/title "内部实现：_parse_entry — 解析 `entry` 对应的数据与约束。"} {:block/title "内部实现：_resolve_entries — 解析并确定 `entries` 对应的数据与约束。"} {:block/title "内部实现：_expand_entries — 处理 `entries` 对应的数据与约束。"} {:block/title "内部实现：_chunks — 处理 `chunks` 对应的数据与约束。"} {:block/title "内部实现：_chunks.flush — 处理 `flush` 对应的数据与约束。"} {:block/title "内部实现：_chunks.units_for_file — 处理 `for_file` 对应的数据与约束。"} {:block/title "内部实现：_write_review_pack_templates — 写入 `review_pack_templates` 对应的数据与约束。"} {:block/title "内部实现：_normalize_repo_selector — 规范化 `repo_selector` 对应的数据与约束。"} {:block/title "内部实现：_resolve_csharp_workspace — 解析并确定 `csharp_workspace` 对应的数据与约束。"} {:block/title "内部实现：_prepare_csharp_restore — 准备 `csharp_restore` 对应的数据与约束。"} {:block/title "内部实现：_prepare_csharp_fallback_workspace — 准备 `csharp_fallback_workspace` 对应的数据与约束。"} {:block/title "内部实现：_rekey_reused_file_parse — 处理 `reused_file_parse` 对应的数据与约束。"} {:block/title "内部实现：_selected_catalog — 处理 `catalog` 对应的数据与约束。"} {:block/title "内部实现：_chunk — 处理 `chunk` 对应的数据与约束。"} {:block/title "内部实现：_review_pack — 处理 `pack` 对应的数据与约束。"} {:block/title "内部实现：_invalidate_after_build — 处理 `after_build` 对应的数据与约束。"} {:block/title "内部实现：build_chunk — 构建 `chunk` 对应的数据与约束。"} {:block/title "内部实现：_substantive_chinese — 处理 `chinese` 对应的数据与约束。"} {:block/title "内部实现：_single_chinese_sentence — 处理 `chinese_sentence` 对应的数据与约束。"} {:block/title "内部实现：_source_check — 处理 `check` 对应的数据与约束。"} {:block/title "内部实现：_partial_fragment_source_errors — 处理 `fragment_source_errors` 对应的数据与约束。"} {:block/title "内部实现：audit_chunk — 审计 `chunk` 对应的数据与约束。"} {:block/title "内部实现：audit_chunk.gate — 处理 `gate` 对应的数据与约束。"} {:block/title "内部实现：review_chunk — 处理 `chunk` 对应的数据与约束。"} {:block/title "内部实现：review_pack — 处理 `pack` 对应的数据与约束。"} {:block/title "内部实现：merge — 合并 `merge` 对应的数据与约束。"} {:block/title "内部实现：_repository_name — 处理 `name` 对应的数据与约束。"} {:block/title "内部实现：_short_code_unit_name — 处理 `code_unit_name` 对应的数据与约束。"} {:block/title "内部实现：_source_role — 处理 `role` 对应的数据与约束。"} {:block/title "内部实现：_human_page_base_title — 处理 `page_base_title` 对应的数据与约束。"} {:block/title "内部实现：_assign_human_titles — 处理 `human_titles` 对应的数据与约束。"} {:block/title "内部实现：_logical_projection — 处理 `projection` 对应的数据与约束。"} {:block/title "内部实现：_logical_projection.new_page — 处理 `page` 对应的数据与约束。"} {:block/title "内部实现：_logical_projection.relation_category — 处理 `category` 对应的数据与约束。"} {:block/title "内部实现：_source_manifest — 处理 `manifest` 对应的数据与约束。"} {:block/title "内部实现：_page_sections — 处理 `sections` 对应的数据与约束。"} {:block/title "内部实现：_overview_text — 处理 `text` 对应的数据与约束。"} {:block/title "内部实现：_aggregate_overview — 处理 `overview` 对应的数据与约束。"} {:block/title "内部实现：_canonical_page_context — 处理 `page_context` 对应的数据与约束。"} {:block/title "内部实现：_logical_context_budgets — 处理 `context_budgets` 对应的数据与约束。"} {:block/title "内部实现：_relation_phrase — 处理 `phrase` 对应的数据与约束。"} {:block/title "内部实现：_human_relation_sentences — 处理 `relation_sentences` 对应的数据与约束。"} {:block/title "内部实现：_normalized_edn_document — 处理 `edn_document` 对应的数据与约束。"} {:block/title "内部实现：_render_markdown_page — 渲染 `markdown_page` 对应的数据与约束。"} {:block/title "内部实现：_logseq_file_graph_config_bytes — 处理 `file_graph_config_bytes` 对应的数据与约束。"} {:block/title "内部实现：_install_logseq_file_graph_config — 安装 `logseq_file_graph_config` 对应的数据与约束。"} {:block/title "内部实现：_audit_logseq_file_graph_config — 审计 `logseq_file_graph_config` 对应的数据与约束。"} {:block/title "内部实现：_human_page_filenames — 处理 `page_filenames` 对应的数据与约束。"} {:block/title "内部实现：_index_document — 处理 `document` 对应的数据与约束。"} {:block/title "内部实现：_wiki_document — 处理 `document` 对应的数据与约束。"} {:block/title "内部实现：_readability_report — 处理 `report` 对应的数据与约束。"} {:block/title "内部实现：project_markdown — 投影 `markdown` 对应的数据与约束。"} {:block/title "内部实现：_preserved_human_bytes — 处理 `human_bytes` 对应的数据与约束。"} {:block/title "内部实现：refresh_human_navigation — 刷新 `human_navigation` 对应的数据与约束。"} {:block/title "内部实现：_logseq — 处理 `logseq` 对应的数据与约束。"} {:block/title "内部实现：_logseq_count — 处理 `count` 对应的数据与约束。"} {:block/title "内部实现：_logseq_count.visit — 处理 `visit` 对应的数据与约束。"} {:block/title "内部实现：_logseq_count.contains_null_result — 判断 `contains_null_result` 所表达的条件。"} {:block/title "内部实现：project_logseq — 投影 `logseq` 对应的数据与约束。"} {:block/title "内部实现：_audit_markdown — 审计 `markdown` 对应的数据与约束。"} {:block/title "内部实现：audit_global — `audit_global` 是第 2891-3204 行的函数，供所属页面定位实现。"} {:block/title "内部实现：finalize — 完成并封存 `finalize` 对应的数据与约束。"} {:block/title "内部实现：relink_sources — 重写链接 `sources` 对应的数据与约束。"} {:block/title "内部实现：build_context — 构建 `context` 对应的数据与约束。"} {:block/title "内部实现：status — 汇总状态 `status` 对应的数据与约束。"} {:block/title "内部实现：run_fast — 执行 `fast` 对应的数据与约束。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_migration 与 _entity_key 的协作实现]]。"} {:block/title "协作：实现时会用到 [[audit_obsidian]]。"} {:block/title "协作：实现时会用到 [[audit_references]]。"} {:block/title "协作：实现时会用到 [[audit_work_record_index]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：主要代码单元是 [[initialize]]。"} {:block/title "协作：实现时会用到 [[load_page_config 与 _merge_known 的协作实现]]。"} {:block/title "协作：实现时会用到 [[preflight 与 git 的协作实现]]。"} {:block/title "协作：实现时会用到 [[record_note 与 page_tag 的协作实现]]。"} {:block/title "协作：实现时会用到 [[refresh]]。"} {:block/title "协作：实现时会用到 [[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]]。"} {:block/title "协作：实现时会用到 [[render_integration 与 _looks_windows 的协作实现]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests]] 会使用这里提供的行为。"} {:block/title "协作：[[MigrationTest]] 会使用这里提供的行为。"} {:block/title "协作：[[MigrationTest 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[ScopeExtensionTest 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[_Transport.close 与 _StartGate 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[append 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_operation_journal 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_references 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[command 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[doctor_report 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[run 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[serve_stdio]] 会使用这里提供的行为。"} {:block/title "协作：[[start_scope_extension]] 会使用这里提供的行为。"} {:block/title "协作：[[start_scope_extension 与 _error 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session]] 会使用这里提供的行为。"}]}
{:page {:block/title "keyword_provider 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/fixtures/keyword_provider.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `keyword_provider.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/fixtures/keyword_provider.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/fixtures/keyword_provider.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/fixtures/keyword_provider.py:1:1)  `tests/fixtures/keyword_provider.py:1-61`"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "keyword_provider_config"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`keyword_provider_config` 是 `scripts/ckb.py` 第 237-257 行定义的函数，本页绑定该固定源码范围。 负责注册 CKB 命令、校验参数，并把子命令分派到对应的知识库实现。"} {:block/title "需要修改时：当 `keyword_provider_config` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb.py 第 237 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb.py:237:1)  `scripts/ckb.py:237-257`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[run_keyword_provider 与 KeywordProviderConfig 的协作实现]]。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "keyword_provider_config 与 parser 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责注册 CKB 命令、校验参数，并把子命令分派到对应的知识库实现。"} {:block/title "需要修改时：当 `scripts/ckb.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb.py:1:1)  `scripts/ckb.py:1-1405`"} {:block/title "内部实现：add_initial_arguments — `add_initial_arguments` 是第 184-199 行的函数，供所属页面定位实现。"} {:block/title "内部实现：add_csharp_arguments — `add_csharp_arguments` 是第 202-205 行的函数，供所属页面定位实现。"} {:block/title "内部实现：add_git_bootstrap_arguments — `add_git_bootstrap_arg...` 是第 208-216 行的函数，供所属页面定位实现。"} {:block/title "内部实现：add_keyword_provider_arguments — `add_keyword_provider_...` 是第 219-228 行的函数，供所属页面定位实现。"} {:block/title "内部实现：add_keyword_fallback_arguments — `add_keyword_fallback_...` 是第 231-234 行的函数，供所属页面定位实现。"} {:block/title "内部实现：keyword_fallback_options — `keyword_fallback_options` 是第 260-267 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_session_query — `_session_query` 是第 755-777 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[_Transport.close 与 _StartGate 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：主要代码单元是 [[emit]]。"} {:block/title "协作：实现时会用到 [[initialize]]。"} {:block/title "协作：主要代码单元是 [[keyword_provider_config]]。"} {:block/title "协作：主要代码单元是 [[main（ckb 实现）]]。"} {:block/title "协作：主要代码单元是 [[parser]]。"} {:block/title "协作：实现时会用到 [[run_keyword_provider 与 KeywordProviderConfig 的协作实现]]。"} {:block/title "协作：实现时会用到 [[start_scope_extension 与 _error 的协作实现]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[main（generate_large_fixture 测试）]] 覆盖相关行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "load_page_config"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`load_page_config` 是源码中负责规范化并校验不可漂移的页面配置的命名代码单元。 它在所属模块内执行规范化并校验不可漂移的页面配置，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当规范化并校验不可漂移的页面配置所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/page_config.py 第 209 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/page_config.py:209:1)  `scripts/ckb_core/page_config.py:209-219`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[load_page_config 与 _merge_known 的协作实现]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[initialize]] 会使用这里提供的行为。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "load_page_config 与 _merge_known 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现页面配置合并、规范化、校验和固定。 它是 Code Knowledge Builder 中承载页面配置合并、规范化、校验和固定的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当页面配置合并、规范化、校验和固定的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/page_config.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/page_config.py:1:1)  `scripts/ckb_core/page_config.py:1-244`"} {:block/title "内部实现：_merge_known — 该附属代码负责合并批次实体和跨批次关系，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_integer — 该附属代码负责页面配置合并、规范化、校验和固定，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_section_list — 该附属代码负责页面配置合并、规范化、校验和固定，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：normalize_page_config — 该附属代码负责规范化并校验不可漂移的页面配置，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：page_config_bytes — 该附属代码负责规范化并校验不可漂移的页面配置，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：page_config_sha256 — 该附属代码负责规范化并校验不可漂移的页面配置，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：write_page_config — 该附属代码负责规范化并校验不可漂移的页面配置，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：主要代码单元是 [[load_page_config]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：[[initialize]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[load_page_config]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "main 与 sha256 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现离线运行时清单、可复现压缩和归档校验。 它是 Code Knowledge Builder 中承载离线运行时清单、可复现压缩和归档校验的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当离线运行时清单、可复现压缩和归档校验的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/build_runtime_payload.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/build_runtime_payload.py:1:1)  `scripts/build_runtime_payload.py:1-117`"} {:block/title "内部实现：sha256 — 该附属代码负责计算机器交付物的完整性摘要，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：relative_files — 该附属代码负责离线运行时清单、可复现压缩和归档校验，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：build — `build` 是第 32-98 行的函数，供所属页面定位实现。"} {:block/title "协作：主要代码单元是 [[main（build_runtime_payload 实现）]]。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "main 相关实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该代码页汇总从空基线生成 Skill 文本统一补丁的实现。 它遍历可交付 UTF-8 文本文件，排除 `.git`、第三方 vendored 源码、运行时资产、缓存和二进制文件，再为每个文件生成新增补丁段。"} {:block/title "需要修改时：当源码目录结构、文本交付边界或补丁格式变化时，需要修改本页并检查输出中不含仓库元数据。"} {:block/title "源码入口：[打开源码：scripts/make_source_patch.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/make_source_patch.py:1:1)  `scripts/make_source_patch.py:1-47`"} {:block/title "协作：主要代码单元是 [[main（make_source_patch 实现）]]。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "main 等测试场景（fake_logseq 测试）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现Logseq CLI 命令形状和投影计数的确定性测试替身。 它是 Code Knowledge Builder 中承载Logseq CLI 命令形状和投影计数的确定性测试替身的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当Logseq CLI 命令形状和投影计数的确定性测试替身的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：tests/fake_logseq.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/fake_logseq.py:1:1)  `tests/fake_logseq.py:1-80`"} {:block/title "内部实现：option — 该附属代码负责Logseq CLI 命令形状和投影计数的确定性测试替身，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：主要代码单元是 [[main（fake_logseq 测试）]]。"} {:block/title "协作：由 [[main（fake_logseq 测试）]] 覆盖相关行为。"} {:block/title "协作：[[main（fake_logseq 测试）]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "main 等测试场景（generate_large_fixture 测试）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现大规模混合语言仓库样例生成。 它是 Code Knowledge Builder 中承载大规模混合语言仓库样例生成的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当大规模混合语言仓库样例生成的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：tests/generate_large_fixture.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/generate_large_fixture.py:1:1)  `tests/generate_large_fixture.py:1-42`"} {:block/title "协作：主要代码单元是 [[main（generate_large_fixture 测试）]]。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "main 等测试场景（session_stdio_harness_probe 测试）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/session_stdio_harness_probe.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `session_stdio_harness_probe.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/session_stdio_harness_probe.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/session_stdio_harness_probe.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/session_stdio_harness_probe.py:1:1)  `tests/session_stdio_harness_probe.py:1-35`"} {:block/title "协作：主要代码单元是 [[main（session_stdio_harness_probe 测试）]]。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "main 等测试场景（session_stdio_reactivation_probe 测试）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/session_stdio_reactivation_probe.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `session_stdio_reactivation_probe.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/session_stdio_reactivation_probe.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/session_stdio_reactivation_probe.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/session_stdio_reactivation_probe.py:1:1)  `tests/session_stdio_reactivation_probe.py:1-117`"} {:block/title "协作：主要代码单元是 [[main（session_stdio_reactivation_probe 测试）]]。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "maintenance_check"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`maintenance_check` 是 `scripts/ckb_core/llm_wiki_capabilities.py` 第 408-458 行定义的函数，本页绑定该固定源码范围。 负责能力矩阵、紧凑 brief 与聚合 maintain 检查的生成和审计。"} {:block/title "需要修改时：当 `maintenance_check` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/llm_wiki_capabilities.py 第 408 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/llm_wiki_capabilities.py:408:1)  `scripts/ckb_core/llm_wiki_capabilities.py:408-458`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_agent_protocol]]。"} {:block/title "协作：实现时会用到 [[audit_gap_register]]。"} {:block/title "协作：实现时会用到 [[audit_operation_journal]]。"} {:block/title "协作：实现时会用到 [[audit_references]]。"} {:block/title "协作：实现时会用到 [[audit_work_record_index]]。"} {:block/title "协作：实现时会用到 [[maintenance_check 与 capability_matrix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve 与 _tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run_keyword_provider 与 KeywordProviderConfig 的协作实现]]。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[append 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[maintenance_check 与 capability_matrix 的协作实现]] 汇总了本页。"} {:block/title "协作：[[start_scope_extension 与 _error 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "maintenance_check 与 capability_matrix 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/llm_wiki_capabilities.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责能力矩阵、紧凑 brief 与聚合 maintain 检查的生成和审计。"} {:block/title "需要修改时：当 `scripts/ckb_core/llm_wiki_capabilities.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/llm_wiki_capabilities.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/llm_wiki_capabilities.py:1:1)  `scripts/ckb_core/llm_wiki_capabilities.py:1-459`"} {:block/title "内部实现：capability_matrix — `capability_matrix` 是第 275-286 行的函数，供所属页面定位实现。"} {:block/title "内部实现：render_capability_matrix_markdown — `render_capability_mat...` 是第 289-343 行的函数，供所属页面定位实现。"} {:block/title "内部实现：write_capability_matrix — `write_capability_matrix` 是第 346-355 行的函数，供所属页面定位实现。"} {:block/title "内部实现：compact_agent_brief — `compact_agent_brief` 是第 358-405 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：主要代码单元是 [[maintenance_check]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[append 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[maintenance_check]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[serve_stdio]] 会使用这里提供的行为。"}]}
{:page {:block/title "main（build_runtime_payload 实现）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`main` 是 `scripts/build_runtime_payload.py` 第 101-112 行定义的函数，本页绑定该固定源码范围。 负责生成可复现的 Windows 运行时载荷，并核对归档成员、清单与校验结果。"} {:block/title "需要修改时：当 `main` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/build_runtime_payload.py 第 101 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/build_runtime_payload.py:101:1)  `scripts/build_runtime_payload.py:101-112`"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：[[main 与 sha256 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "main（ckb 实现）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`main` 是 `scripts/ckb.py` 第 780-1393 行定义的函数，本页绑定该固定源码范围。 负责注册 CKB 命令、校验参数，并把子命令分派到对应的知识库实现。"} {:block/title "需要修改时：当 `main` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb.py 第 780 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb.py:780:1)  `scripts/ckb.py:780-1393`"} {:block/title "协作：实现时会用到 [[AutomationTest.register]]。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[_Transport.close 与 _StartGate 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_agent_protocol]]。"} {:block/title "协作：实现时会用到 [[audit_feedback]]。"} {:block/title "协作：实现时会用到 [[audit_gap_register]]。"} {:block/title "协作：实现时会用到 [[audit_migration]]。"} {:block/title "协作：实现时会用到 [[audit_operation_journal]]。"} {:block/title "协作：实现时会用到 [[audit_operation_journal 与 _root 的协作实现]]。"} {:block/title "协作：实现时会用到 [[audit_references]]。"} {:block/title "协作：实现时会用到 [[audit_references 与 _root 的协作实现]]。"} {:block/title "协作：实现时会用到 [[bind_conversation 与 default_management_registry_path 的协作实现]]。"} {:block/title "协作：实现时会用到 [[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]]。"} {:block/title "协作：实现时会用到 [[ingest_event 与 default_registry_path 的协作实现]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[start_scope_extension 与 _error 的协作实现]]。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "main（fake_logseq 测试）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`main` 是源码中负责接收命令参数并把请求路由到对应实现的命名代码单元。 它在所属模块内执行接收命令参数并把请求路由到对应实现，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当接收命令参数并把请求路由到对应实现所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：tests/fake_logseq.py 第 22 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/fake_logseq.py:22:1)  `tests/fake_logseq.py:22-75`"} {:block/title "协作：实现时会用到 [[doctor_report 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[main 等测试场景（fake_logseq 测试）]]。"} {:block/title "协作：[[main 等测试场景（fake_logseq 测试）]] 汇总了本页。"}]}
{:page {:block/title "main（generate_large_fixture 测试）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`main` 是源码中负责接收命令参数并把请求路由到对应实现的命名代码单元。 它在所属模块内执行接收命令参数并把请求路由到对应实现，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当接收命令参数并把请求路由到对应实现所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：tests/generate_large_fixture.py 第 8 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/generate_large_fixture.py:8:1)  `tests/generate_large_fixture.py:8-37`"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：[[append 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[bind_conversation]] 关联到这里的验证场景。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[main 等测试场景（generate_large_fixture 测试）]] 汇总了本页。"} {:block/title "协作：[[parser]] 关联到这里的验证场景。"}]}
{:page {:block/title "main（make_source_patch 实现）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`main` 解析补丁输出路径并生成从空目录到完整 Skill 的文本统一差异。 它确定性过滤 `.git`、第三方 vendored 源码与二进制资产，按路径排序拼接新增文件补丁并以 UTF-8/LF 写入。"} {:block/title "需要修改时：当排除集合、编码策略、排序规则或统一差异格式变化时，需要修改该函数。"} {:block/title "源码入口：[打开源码：scripts/make_source_patch.py 第 19 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/make_source_patch.py:19:1)  `scripts/make_source_patch.py:19-42`"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：[[main 相关实现]] 汇总了本页。"}]}
{:page {:block/title "main（session_stdio_harness_probe 测试）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`main` 是 `tests/session_stdio_harness_probe.py` 第 16-30 行定义的函数，本页绑定该固定源码范围。 该函数作为可执行验证入口，检查标识符 `main` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `main` 的说明。"} {:block/title "源码入口：[打开源码：tests/session_stdio_harness_probe.py 第 16 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/session_stdio_harness_probe.py:16:1)  `tests/session_stdio_harness_probe.py:16-30`"} {:block/title "协作：实现时会用到 [[_Transport.close 与 _StartGate 的协作实现]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：[[bind_conversation]] 关联到这里的验证场景。"} {:block/title "协作：[[main 等测试场景（session_stdio_harness_probe 测试）]] 汇总了本页。"}]}
{:page {:block/title "main（session_stdio_reactivation_probe 测试）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`main` 是 `tests/session_stdio_reactivation_probe.py` 第 16-112 行定义的函数，本页绑定该固定源码范围。 该函数作为可执行验证入口，检查标识符 `main` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `main` 的说明。"} {:block/title "源码入口：[打开源码：tests/session_stdio_reactivation_probe.py 第 16 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/session_stdio_reactivation_probe.py:16:1)  `tests/session_stdio_reactivation_probe.py:16-112`"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[_Transport.close 与 _StartGate 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：[[main 等测试场景（session_stdio_reactivation_probe 测试）]] 汇总了本页。"}]}
{:page {:block/title "module_name"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`module_name` 是源码中负责页面配额、实体归属、关系预算和上下文预算的确定性决策的命名代码单元。 它在所属模块内执行页面配额、实体归属、关系预算和上下文预算的确定性决策，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当页面配额、实体归属、关系预算和上下文预算的确定性决策所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/navigation.py 第 37 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/navigation.py:37:1)  `scripts/ckb_core/navigation.py:37-39`"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[run]] 覆盖相关行为。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "module_name 与 estimated_tokens 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现页面配额、实体归属、关系预算和上下文预算的确定性决策。 它是 Code Knowledge Builder 中承载页面配额、实体归属、关系预算和上下文预算的确定性决策的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当页面配额、实体归属、关系预算和上下文预算的确定性决策的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/navigation.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/navigation.py:1:1)  `scripts/ckb_core/navigation.py:1-456`"} {:block/title "内部实现：estimated_tokens — 该附属代码负责页面配额、实体归属、关系预算和上下文预算的确定性决策，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_test_path — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_graph_facts — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_ancestors — 该附属代码负责页面配额、实体归属、关系预算和上下文预算的确定性决策，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_eligible — 该附属代码负责页面配额、实体归属、关系预算和上下文预算的确定性决策，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_rank — 该附属代码负责页面配额、实体归属、关系预算和上下文预算的确定性决策，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：build_navigation_plan — 该附属代码负责按固定排序计算页面、附录、归属和审阅预算，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：apply_navigation_plan — 该附属代码负责按固定排序计算页面、附录、归属和审阅预算，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：page_limit — 该附属代码负责按固定排序计算页面、附录、归属和审阅预算，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：context_budget_record — 该附属代码负责页面配额、实体归属、关系预算和上下文预算的确定性决策，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：build_review_packs — 该附属代码负责按固定排序计算页面、附录、归属和审阅预算，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：build_review_packs.partition — 该附属代码负责按固定排序计算页面、附录、归属和审阅预算，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：build_review_packs.partition.flush — 该附属代码负责按固定排序计算页面、附录、归属和审阅预算，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：主要代码单元是 [[module_name]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "normalize"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`normalize` 是 `tests/benchmark_chinese_retrieval.py` 第 211-245 行定义的函数，本页绑定该固定源码范围。 该函数作为可执行验证入口，检查标识符 `normalize` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `normalize` 的说明。"} {:block/title "源码入口：[打开源码：tests/benchmark_chinese_retrieval.py 第 211 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/benchmark_chinese_retrieval.py:211:1)  `tests/benchmark_chinese_retrieval.py:211-245`"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：[[normalize 等测试场景]] 汇总了本页。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[run_keyword_provider 与 KeywordProviderConfig 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[search_terms 与 _split_camel 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "normalize 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/benchmark_chinese_retrieval.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `benchmark_chinese_retrieval.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/benchmark_chinese_retrieval.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/benchmark_chinese_retrieval.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/benchmark_chinese_retrieval.py:1:1)  `tests/benchmark_chinese_retrieval.py:1-569`"} {:block/title "内部实现：json_load — `json_load` 是第 43-44 行的函数，供所属页面定位实现。"} {:block/title "内部实现：json_write — `json_write` 是第 47-49 行的函数，供所属页面定位实现。"} {:block/title "内部实现：sha256 — `sha256` 是第 52-53 行的函数，供所属页面定位实现。"} {:block/title "内部实现：percentile — `percentile` 是第 56-65 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_sqlite_backup — `_sqlite_backup` 是第 68-71 行的函数，供所属页面定位实现。"} {:block/title "内部实现：copy_corpus — `copy_corpus` 是第 74-99 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_title_and_links — `_title_and_links` 是第 102-108 行的函数，供所属页面定位实现。"} {:block/title "内部实现：build_manual_index — `build_manual_index` 是第 111-154 行的函数，供所属页面定位实现。"} {:block/title "内部实现：manual_scan — `manual_scan` 是第 157-208 行的函数，供所属页面定位实现。"} {:block/title "内部实现：invoke — `invoke` 是第 248-261 行的函数，供所属页面定位实现。"} {:block/title "内部实现：result_signature — `result_signature` 是第 264-271 行的函数，供所属页面定位实现。"} {:block/title "内部实现：validate_protocol — `validate_protocol` 是第 274-282 行的函数，供所属页面定位实现。"} {:block/title "内部实现：run_benchmark — `run_benchmark` 是第 285-386 行的函数，供所属页面定位实现。"} {:block/title "内部实现：summarize — `summarize` 是第 389-499 行的函数，供所属页面定位实现。"} {:block/title "内部实现：verify_runs — `verify_runs` 是第 502-544 行的函数，供所属页面定位实现。"} {:block/title "内部实现：main — `main` 是第 547-564 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：主要代码单元是 [[normalize]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[search_terms]]。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "one_cycle"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`one_cycle` 是 `tests/session_stdio_stress.py` 第 22-45 行定义的函数，本页绑定该固定源码范围。 该函数作为可执行验证入口，检查标识符 `one_cycle` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `one_cycle` 的说明。"} {:block/title "源码入口：[打开源码：tests/session_stdio_stress.py 第 22 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/session_stdio_stress.py:22:1)  `tests/session_stdio_stress.py:22-45`"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[_Transport.close 与 _StartGate 的协作实现]]。"} {:block/title "协作：由 [[one_cycle 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[one_cycle 等测试场景]] 汇总了本页。"}]}
{:page {:block/title "one_cycle 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/session_stdio_stress.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `session_stdio_stress.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/session_stdio_stress.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/session_stdio_stress.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/session_stdio_stress.py:1:1)  `tests/session_stdio_stress.py:1-95`"} {:block/title "内部实现：main — `main` 是第 48-90 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[_Transport.close 与 _StartGate 的协作实现]]。"} {:block/title "协作：主要代码单元是 [[one_cycle]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：[[one_cycle]] 关联到这里的验证场景。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "package_showcase"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`package_showcase` 是源码中负责构建可复现发行归档并复核成员集合的命名代码单元。 它在所属模块内执行构建可复现发行归档并复核成员集合，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当构建可复现发行归档并复核成员集合所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/showcase.py 第 66 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/showcase.py:66:1)  `scripts/ckb_core/showcase.py:66-172`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[package_showcase 与 _parse_sample 的协作实现]]。"} {:block/title "协作：[[package_showcase 与 _parse_sample 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "package_showcase 与 _parse_sample 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现人类可读样例集合、中文 Wiki 和展示归档。 它是 Code Knowledge Builder 中承载人类可读样例集合、中文 Wiki 和展示归档的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当人类可读样例集合、中文 Wiki 和展示归档的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/showcase.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/showcase.py:1:1)  `scripts/ckb_core/showcase.py:1-173`"} {:block/title "内部实现：_parse_sample — 该附属代码负责解析命名声明并保存稳定源码范围，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_root_wiki — 该附属代码负责人类可读样例集合、中文 Wiki 和展示归档，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：主要代码单元是 [[package_showcase]]。"} {:block/title "协作：[[package_showcase]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "parse_file"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`parse_file` 是 `scripts/ckb_core/parsers.py` 第 272-430 行定义的函数，本页绑定该固定源码范围。 负责按语言解析源码，并为不完整 C++ 与 SCons 场景提供受控回退。"} {:block/title "需要修改时：当 `parse_file` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/parsers.py 第 272 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/parsers.py:272:1)  `scripts/ckb_core/parsers.py:272-430`"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[doctor_report 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[parse_file 与 _language 的协作实现]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "parse_file 与 _language 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/parsers.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责按语言解析源码，并为不完整 C++ 与 SCons 场景提供受控回退。"} {:block/title "需要修改时：当 `scripts/ckb_core/parsers.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/parsers.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/parsers.py:1:1)  `scripts/ckb_core/parsers.py:1-538`"} {:block/title "内部实现：_language — `_language` 是第 65-78 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_walk — `_walk` 是第 81-86 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_node_text — `_node_text` 是第 89-90 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_byte_position — `_byte_position` 是第 93-96 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_find_name — `_find_name` 是第 99-115 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_public_evidence — `_public_evidence` 是第 118-123 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_single_statement_shape — `_single_statement_shape` 是第 145-168 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_csharp_fallback_name — `_csharp_fallback_name` 是第 171-187 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_has_ancestor — `_has_ancestor` 是第 190-196 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_cpp_function_declarator_kind — `_cpp_function_declara...` 是第 199-209 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_cpp_explicit_class_instantiation_recovery — `_cpp_explicit_class_i...` 是第 212-235 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_parse_diagnostics — `_parse_diagnostics` 是第 238-269 行的函数，供所属页面定位实现。"} {:block/title "内部实现：merge_csharp_partials — `merge_csharp_partials` 是第 433-489 行的函数，供所属页面定位实现。"} {:block/title "内部实现：lexical_links — `lexical_links` 是第 492-537 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：主要代码单元是 [[parse_file]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[initialize]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "parser"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`parser` 是 `scripts/ckb.py` 第 270-743 行定义的函数，本页绑定该固定源码范围。 负责注册 CKB 命令、校验参数，并把子命令分派到对应的知识库实现。"} {:block/title "需要修改时：当 `parser` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb.py 第 270 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb.py:270:1)  `scripts/ckb.py:270-743`"} {:block/title "协作：实现时会用到 [[AutomationTest.register]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[append 等测试场景]]。"} {:block/title "协作：实现时会用到 [[bind_conversation 与 default_management_registry_path 的协作实现]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[ingest_event 与 default_registry_path 的协作实现]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[keyword_provider_config 与 parser 的协作实现]]。"} {:block/title "协作：实现时会用到 [[normalize]]。"} {:block/title "协作：实现时会用到 [[refresh]]。"} {:block/title "协作：实现时会用到 [[retrieve]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[start_scope_extension 与 _error 的协作实现]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[main（generate_large_fixture 测试）]] 覆盖相关行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[build 的协作边界]] 会使用这里提供的行为。"} {:block/title "协作：[[command 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[deploy 的协作边界]] 会使用这里提供的行为。"} {:block/title "协作：[[execute 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 汇总了本页。"} {:block/title "协作：[[main（build_runtime_payload 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[main（generate_large_fixture 测试）]] 会使用这里提供的行为。"} {:block/title "协作：[[main（make_source_patch 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[main（session_stdio_harness_probe 测试）]] 会使用这里提供的行为。"} {:block/title "协作：[[main（session_stdio_reactivation_probe 测试）]] 会使用这里提供的行为。"} {:block/title "协作：[[normalize 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[one_cycle 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file]] 会使用这里提供的行为。"} {:block/title "协作：[[preflight 与 git 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[run 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[source_files 与 sha256 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "preflight"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`preflight` 是 `scripts/ckb_core/gitrepo.py` 第 194-217 行定义的函数，本页绑定该固定源码范围。 负责验证固定 Git 快照，并读取提交、树、文件模式和对象内容。"} {:block/title "需要修改时：当 `preflight` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/gitrepo.py 第 194 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/gitrepo.py:194:1)  `scripts/ckb_core/gitrepo.py:194-217`"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[preflight 与 git 的协作实现]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[append 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[bind_conversation]] 会使用这里提供的行为。"} {:block/title "协作：[[command 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize]] 会使用这里提供的行为。"} {:block/title "协作：[[preflight 与 git 的协作实现]] 汇总了本页。"} {:block/title "协作：[[start_scope_extension]] 会使用这里提供的行为。"} {:block/title "协作：[[start_scope_extension 与 _error 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "preflight 与 git 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/gitrepo.py` 是 `scripts/ckb_core/gitrepo.py` 中负责汇总并提供固定 Git 提交、blob、工作树与源码快照读取的文件入口。 它按源码所示的参数、条件分支和数据结构完成汇总并提供固定 Git 提交、blob、工作树与源码快照读取，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当固定 Git 提交、blob、工作树与源码快照读取的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/gitrepo.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/gitrepo.py:1:1)  `scripts/ckb_core/gitrepo.py:1-419`"} {:block/title "内部实现：git — 处理 `git` 对应的数据与约束。"} {:block/title "内部实现：_git_probe — 处理 `probe` 对应的数据与约束。"} {:block/title "内部实现：_identity_value — 处理 `value` 对应的数据与约束。"} {:block/title "内部实现：prepare_git_repository — 准备 `git_repository` 对应的数据与约束。"} {:block/title "内部实现：assert_unchanged — 处理 `unchanged` 对应的数据与约束。"} {:block/title "内部实现：create_source_snapshot — 创建 `source_snapshot` 对应的数据与约束。"} {:block/title "内部实现：assert_source_snapshot — 处理 `source_snapshot` 对应的数据与约束。"} {:block/title "内部实现：tracked_sources — 处理 `sources` 对应的数据与约束。"} {:block/title "内部实现：tracked_csharp_project_files — 处理 `csharp_project_files` 对应的数据与约束。"} {:block/title "内部实现：blob_bytes — 处理 `bytes` 对应的数据与约束。"} {:block/title "内部实现：blob_bytes_many — `blob_bytes_many` 是第 363-393 行的函数，供所属页面定位实现。"} {:block/title "内部实现：object_exists — 判断 `object_exists` 所表达的条件。"} {:block/title "内部实现：resolve_scope_paths — 解析并确定 `scope_paths` 对应的数据与约束。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[initialize]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：主要代码单元是 [[preflight]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[preflight]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "query_graph"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`query_graph` 是源码中负责构造职责关系图并提供职责群或路径查询的命名代码单元。 它在所属模块内执行构造职责关系图并提供职责群或路径查询，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当构造职责关系图并提供职责群或路径查询所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/graphify_core.py 第 526 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/graphify_core.py:526:1)  `scripts/ckb_core/graphify_core.py:526-583`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[query_graph 与 _networkx_modules 的协作实现]]。"} {:block/title "协作：实现时会用到 [[source_files]]。"} {:block/title "协作：实现时会用到 [[start_scope_extension 与 _error 的协作实现]]。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "query_graph 与 _networkx_modules 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现Graphify 兼容图、确定性职责群、路径查询和关系报告。 它是 Code Knowledge Builder 中承载Graphify 兼容图、确定性职责群、路径查询和关系报告的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当Graphify 兼容图、确定性职责群、路径查询和关系报告的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/graphify_core.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/graphify_core.py:1:1)  `scripts/ckb_core/graphify_core.py:1-676`"} {:block/title "内部实现：_networkx_modules — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_canonical_json_sha256 — 该附属代码负责稳定读取或写入机器状态记录，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_source_location — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_node_label — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_description — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_confidence — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_graphify_node — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_graphify_link — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_build_networkx — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_community_records — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_report — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：project_graphify — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：audit_graphify — 该附属代码负责执行确定性完整性与来源真实性检查，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_load_projected_graph — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_terms — 该附属代码负责维护旧版页面索引兼容接口并生成受预算约束的阅读包，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_seed_scores — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_adjacency — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_query_size — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_resolve_node — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：shortest_path — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：explain_node — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：主要代码单元是 [[query_graph]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[doctor_report]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "record_note"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`record_note` 是 `scripts/ckb_core/workspace_notes.py` 中负责校验中文正文与知识页回链，写入指定记录类型并更新镜像和索引的函数。 它按源码所示的参数、条件分支和数据结构完成校验中文正文与知识页回链，写入指定记录类型并更新镜像和索引，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当analysis、change、pitfall、experiment、session 笔记写入与镜像的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/workspace_notes.py 第 107 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/workspace_notes.py:107:1)  `scripts/ckb_core/workspace_notes.py:107-185`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_work_record_index 与 _contains_chinese 的协作实现]]。"} {:block/title "协作：实现时会用到 [[record_note 与 page_tag 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve 与 _tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[sync_human_layer 与 _source_manifest 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 汇总了本页。"} {:block/title "协作：[[start_session 与 _session_directory 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "record_note 与 page_tag 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/workspace_notes.py` 是 `scripts/ckb_core/workspace_notes.py` 中负责汇总并提供analysis、change、pitfall、experiment、session 笔记写入与镜像的文件入口。 它按源码所示的参数、条件分支和数据结构完成汇总并提供analysis、change、pitfall、experiment、session 笔记写入与镜像，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当analysis、change、pitfall、experiment、session 笔记写入与镜像的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/workspace_notes.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/workspace_notes.py:1:1)  `scripts/ckb_core/workspace_notes.py:1-376`"} {:block/title "内部实现：page_tag — 处理 `tag` 对应的数据与约束。"} {:block/title "内部实现：_markdown_root — 处理 `root` 对应的数据与约束。"} {:block/title "内部实现：_resolve_page_titles — 解析并确定 `page_titles` 对应的数据与约束。"} {:block/title "内部实现：_source_links_for_titles — 处理 `links_for_titles` 对应的数据与约束。"} {:block/title "内部实现：queue_pending_note — 处理 `pending_note` 对应的数据与约束。"} {:block/title "内部实现：selectors_for_changed_paths — 处理 `for_changed_paths` 对应的数据与约束。"} {:block/title "内部实现：materialize_pending_notes — 物化 `pending_notes` 对应的数据与约束。"} {:block/title "内部实现：audit_notes — 审计 `notes` 对应的数据与约束。"} {:block/title "内部实现：sync_workspace — 同步 `workspace` 对应的数据与约束。"} {:block/title "内部实现：workspace_status — 汇总 `workspace_status` 状态与计数。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：主要代码单元是 [[record_note]]。"} {:block/title "协作：实现时会用到 [[retrieve 与 _tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：[[audit_agent_protocol 与 _default_python 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[start_session]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session 与 _session_directory 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "refresh"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`KnowledgeBatchWorkflo...` 是 `tests/test_knowledge_batch_migration.py` 第 468-476 行定义的函数，本页绑定该固定源码范围。 该函数作为可执行验证入口，检查标识符 `refresh` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `refresh` 的说明。"} {:block/title "源码入口：[打开源码：tests/test_knowledge_batch_migration.py 第 468 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_knowledge_batch_migration.py:468:1)  `tests/test_knowledge_batch_migration.py:468-476`"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[start_scope_extension 与 _error 的协作实现]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[audit_operation_journal 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[refresh 等测试场景]] 汇总了本页。"}]}
{:page {:block/title "refresh 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/test_knowledge_batch_migration.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `test_knowledge_batch_migration.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/test_knowledge_batch_migration.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/test_knowledge_batch_migration.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_knowledge_batch_migration.py:1:1)  `tests/test_knowledge_batch_migration.py:1-706`"} {:block/title "内部实现：KnowledgeBatchVersionMatrixTests — `KnowledgeBatchVersion...` 是第 25-124 行的类，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchVersionMatrixTests.test_git_common_dir_resolves_worktree_and_ordinary_clone — `KnowledgeBatchVersion...` 是第 26-75 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchVersionMatrixTests.test_matrix_uses_real_historical_releases — `KnowledgeBatchVersion...` 是第 77-101 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchVersionMatrixTests.test_reference_matrix_matches_runtime_matrix — `KnowledgeBatchVersion...` 是第 103-109 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchVersionMatrixTests.test_historical_output_fixture_recipes_are_not_relabelled_current_outputs — `KnowledgeBatchVersion...` 是第 111-124 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests — `KnowledgeBatchWorkflo...` 是第 127-701 行的类，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests.setUp — `KnowledgeBatchWorkflo...` 是第 128-148 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests.tearDown — `KnowledgeBatchWorkflo...` 是第 150-161 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests._add_complete_mutable_fixture — `KnowledgeBatchWorkflo...` 是第 163-178 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests._build_additional_output — `KnowledgeBatchWorkflo...` 是第 180-197 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests._project_document — `KnowledgeBatchWorkflo...` 是第 199-254 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests._manifest — `KnowledgeBatchWorkflo...` 是第 256-269 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests.test_plan_apply_audit_cutover_and_exact_rollback — `KnowledgeBatchWorkflo...` 是第 271-307 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests.test_partial_apply_failure_can_resume_without_touching_origin — `KnowledgeBatchWorkflo...` 是第 309-327 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests.test_cold_build_has_zero_reuse_and_resume_obeys_review_gate — `KnowledgeBatchWorkflo...` 是第 329-359 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests.test_changed_target_commit_uses_delta_review_then_resumes_ready — `KnowledgeBatchWorkflo...` 是第 361-389 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests.test_two_projects_isolate_partial_apply_cutover_and_subset_rollback — `KnowledgeBatchWorkflo...` 是第 391-455 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests.test_plan_classifies_required_origin_version_and_path_failures — `KnowledgeBatchWorkflo...` 是第 457-575 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests.test_plan_classifies_required_origin_version_and_path_failures.case_project — `KnowledgeBatchWorkflo...` 是第 462-466 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests.test_plan_classifies_required_origin_version_and_path_failures.plan_for — `KnowledgeBatchWorkflo...` 是第 478-489 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests.test_origin_records_are_exact_fixed_keys_before_any_external_read — `KnowledgeBatchWorkflo...` 是第 577-617 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests.test_origin_records_are_exact_fixed_keys_before_any_external_read.guarded — `KnowledgeBatchWorkflo...` 是第 587-590 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests.test_recovery_topology_rejects_same_root_before_any_transaction_write — `KnowledgeBatchWorkflo...` 是第 619-637 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests.test_apply_rejects_manifest_origin_and_repository_drift_before_staging_write — `KnowledgeBatchWorkflo...` 是第 639-674 行的函数，供所属页面定位实现。"} {:block/title "内部实现：KnowledgeBatchWorkflowTests.test_owner_token_lock_serializes_apply_and_cutover_then_allows_retry — `KnowledgeBatchWorkflo...` 是第 676-701 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[ScopeExtensionTest]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[create_knowledge_batch_plan]]。"} {:block/title "协作：实现时会用到 [[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]]。"} {:block/title "协作：主要代码单元是 [[refresh]]。"} {:block/title "协作：实现时会用到 [[start_scope_extension 与 _error 的协作实现]]。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[ScopeExtensionTest]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_gap_register]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_obsidian]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_obsidian 与 prepare_vault 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_operation_journal]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_operation_journal 与 _root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_output_contract 与 _default_ckb 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[create_knowledge_batch_plan]] 关联到这里的验证场景。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[maintenance_check]] 关联到这里的验证场景。"} {:block/title "协作：[[maintenance_check 与 capability_matrix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note]] 关联到这里的验证场景。"} {:block/title "协作：[[resolve_checkout_git_dir]] 关联到这里的验证场景。"} {:block/title "协作：[[resolve_checkout_git_dir 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[source_files]] 关联到这里的验证场景。"} {:block/title "协作：[[start_scope_extension]] 关联到这里的验证场景。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "register_obsidian_plugin"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`register_obsidian_plugin` 是 `scripts/ckb_core/obsidian_plugin.py` 中负责验证并登记独立 Obsidian Companion 包及其可部署载荷的函数。 它按源码所示的参数、条件分支和数据结构完成验证并登记独立 Obsidian Companion 包及其可部署载荷，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当Obsidian Companion 包注册、部署、状态与移除的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/obsidian_plugin.py 第 88 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/obsidian_plugin.py:88:1)  `scripts/ckb_core/obsidian_plugin.py:88-111`"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：[[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/obsidian_plugin.py` 是 `scripts/ckb_core/obsidian_plugin.py` 中负责汇总并提供Obsidian Companion 包注册、部署、状态与移除的文件入口。 它按源码所示的参数、条件分支和数据结构完成汇总并提供Obsidian Companion 包注册、部署、状态与移除，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当Obsidian Companion 包注册、部署、状态与移除的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/obsidian_plugin.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/obsidian_plugin.py:1:1)  `scripts/ckb_core/obsidian_plugin.py:1-262`"} {:block/title "内部实现：default_obsidian_plugin_registry — 返回默认 `obsidian_plugin_registry` 对应的数据与约束。"} {:block/title "内部实现：obsidian_plugin_installation — 处理 `plugin_installation` 对应的数据与约束。"} {:block/title "内部实现：_payload_from_package — 处理 `from_package` 对应的数据与约束。"} {:block/title "内部实现：_validate_payload — 验证 `payload` 对应的数据与约束。"} {:block/title "内部实现：_registered_payload — 处理 `payload` 对应的数据与约束。"} {:block/title "内部实现：deploy_obsidian_plugin_to_vault — 部署 `obsidian_plugin_to_vault` 对应的数据与约束。"} {:block/title "内部实现：deploy_obsidian_plugin — 部署 `obsidian_plugin` 对应的数据与约束。"} {:block/title "内部实现：obsidian_plugin_status — 汇总 `obsidian_plugin_status` 状态与计数。"} {:block/title "内部实现：remove_obsidian_plugin — 移除 `obsidian_plugin` 对应的数据与约束。"} {:block/title "内部实现：deploy_registered_plugin_if_available — 部署 `registered_plugin_if_available` 对应的数据与约束。"} {:block/title "协作：实现时会用到 [[AutomationTest.register]]。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[audit_output_contract]]。"} {:block/title "协作：实现时会用到 [[audit_output_contract 与 _default_ckb 的协作实现]]。"} {:block/title "协作：主要代码单元是 [[register_obsidian_plugin]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_obsidian]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_obsidian 与 prepare_vault 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_output_contract]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_output_contract 与 _default_ckb 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[register_obsidian_plugin]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "render_integration"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`render_integration` 是 `scripts/ckb_core/automation_integrations.py` 第 432-574 行定义的函数，本页绑定该固定源码范围。 负责为不同 Harness 生成事件接入脚本和固定配置。"} {:block/title "需要修改时：当 `render_integration` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/automation_integrations.py 第 432 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation_integrations.py:432:1)  `scripts/ckb_core/automation_integrations.py:432-574`"} {:block/title "内部实现：render_integration.write — `render_integration.write` 是第 461-467 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[bind_conversation 与 default_management_registry_path 的协作实现]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[render_integration 与 _looks_windows 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "render_integration 与 _looks_windows 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/automation_integrations.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责为不同 Harness 生成事件接入脚本和固定配置。"} {:block/title "需要修改时：当 `scripts/ckb_core/automation_integrations.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/automation_integrations.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation_integrations.py:1:1)  `scripts/ckb_core/automation_integrations.py:1-575`"} {:block/title "内部实现：_looks_windows — `_looks_windows` 是第 19-21 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_wsl_launch_path — `_wsl_launch_path` 是第 24-32 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_commands — `_commands` 是第 35-41 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_powershell_command — `_powershell_command` 是第 44-62 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_powershell_command.quote — `_powershell_command.q...` 是第 45-46 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_codex_windows_bridge — `_codex_windows_bridge` 是第 65-78 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_handler — `_handler` 是第 81-90 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_codex_hooks — `_codex_hooks` 是第 93-121 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_claude_hooks — `_claude_hooks` 是第 124-143 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_claude_hooks.handler — `_claude_hooks.handler` 是第 125-126 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_gemini_hooks — `_gemini_hooks` 是第 146-159 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_gemini_hooks.handler — `_gemini_hooks.handler` 是第 147-148 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_copilot_hooks — `_copilot_hooks` 是第 162-188 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_copilot_hooks.handler — `_copilot_hooks.handler` 是第 163-172 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_cursor_hooks — `_cursor_hooks` 是第 191-211 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_cursor_hooks.handler — `_cursor_hooks.handler` 是第 192-196 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_opencode_stable_plugin — `_opencode_stable_plugin` 是第 214-292 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_opencode_v2_plugin — `_opencode_v2_plugin` 是第 295-388 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_generic_schema — `_generic_schema` 是第 391-429 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[doctor_report 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[emit]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：主要代码单元是 [[render_integration]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.register 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[SourceLinkRenderer.uri]] 会使用这里提供的行为。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_references 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[doctor_report 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[initialize 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "resolve_checkout_git_dir"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`resolve_checkout_git_dir` 是 `tests/git_checkout.py` 第 20-32 行定义的函数，本页绑定该固定源码范围。 该函数作为可执行验证入口，检查标识符 `resolve_checkout_git_dir` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `resolve_checkout_git_dir` 的说明。"} {:block/title "源码入口：[打开源码：tests/git_checkout.py 第 20 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/git_checkout.py:20:1)  `tests/git_checkout.py:20-32`"} {:block/title "协作：实现时会用到 [[resolve_checkout_git_dir 等测试场景]]。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[resolve_checkout_git_dir 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[resolve_checkout_git_dir 等测试场景]] 汇总了本页。"}]}
{:page {:block/title "resolve_checkout_git_dir 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/git_checkout.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `git_checkout.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/git_checkout.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/git_checkout.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/git_checkout.py:1:1)  `tests/git_checkout.py:1-64`"} {:block/title "内部实现：_host_path — `_host_path` 是第 9-17 行的函数，供所属页面定位实现。"} {:block/title "内部实现：resolve_git_common_dir — `resolve_git_common_dir` 是第 35-63 行的函数，供所属页面定位实现。"} {:block/title "协作：主要代码单元是 [[resolve_checkout_git_dir]]。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[resolve_checkout_git_dir]] 覆盖相关行为。"} {:block/title "协作：[[resolve_checkout_git_dir]] 关联到这里的验证场景。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "retrieve"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`retrieve` 是 `scripts/ckb_core/agent_index.py` 第 426-554 行定义的函数，本页绑定该固定源码范围。 负责构建兼容 Agent 索引，并按预算执行确定性检索与结果排序。"} {:block/title "需要修改时：当 `retrieve` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/agent_index.py 第 426 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_index.py:426:1)  `scripts/ckb_core/agent_index.py:426-554`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[module_name 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve 与 _tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[search_terms]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests]] 会使用这里提供的行为。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests]] 会使用这里提供的行为。"} {:block/title "协作：[[QueryTermsTests]] 会使用这里提供的行为。"} {:block/title "协作：[[_Transport.close 与 _StartGate 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_agent_protocol 与 _default_python 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_operation_journal 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[build 的协作边界]] 会使用这里提供的行为。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[deploy 的协作边界]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 汇总了本页。"} {:block/title "协作：[[serve_stdio]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session]] 会使用这里提供的行为。"}]}
{:page {:block/title "retrieve 与 _tokens 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/agent_index.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责构建兼容 Agent 索引，并按预算执行确定性检索与结果排序。"} {:block/title "需要修改时：当 `scripts/ckb_core/agent_index.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/agent_index.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_index.py:1:1)  `scripts/ckb_core/agent_index.py:1-555`"} {:block/title "内部实现：_tokens — `_tokens` 是第 24-25 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_note_documents — `_note_documents` 是第 28-44 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_projection — `_projection` 是第 47-60 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_page_documents — `_page_documents` 是第 63-160 行的函数，供所属页面定位实现。"} {:block/title "内部实现：build_agent_index — `build_agent_index` 是第 163-341 行的函数，供所属页面定位实现。"} {:block/title "内部实现：audit_agent_index — `audit_agent_index` 是第 344-390 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_agent_index_ready — `_agent_index_ready` 是第 393-405 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_fts_query — `_fts_query` 是第 408-409 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_next_pack_path — `_next_pack_path` 是第 412-423 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：主要代码单元是 [[retrieve]]。"} {:block/title "协作：实现时会用到 [[search_terms 与 _split_camel 的协作实现]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：[[audit_gap_register 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_references 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[maintenance_check]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "retrieve_machine"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`retrieve_machine` 是 `scripts/ckb_core/machine_knowledge.py` 第 1600-1709 行定义的函数，本页绑定该固定源码范围。 负责机器知识 SQLite 的构建、FTS5 检索、实体邻接和源码定位。"} {:block/title "需要修改时：当 `retrieve_machine` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/machine_knowledge.py 第 1600 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/machine_knowledge.py:1600:1)  `scripts/ckb_core/machine_knowledge.py:1600-1709`"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run_keyword_provider]]。"} {:block/title "协作：实现时会用到 [[run_keyword_provider 与 KeywordProviderConfig 的协作实现]]。"} {:block/title "协作：实现时会用到 [[search_terms]]。"} {:block/title "协作：实现时会用到 [[search_terms 与 _split_camel 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[append 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests]] 会使用这里提供的行为。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[bind_conversation 与 default_management_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[command 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[normalize 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 汇总了本页。"} {:block/title "协作：[[run_keyword_benchmark 与 _text_list 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[serve_stdio]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session]] 会使用这里提供的行为。"}]}
{:page {:block/title "retrieve_machine 与 estimated_tokens 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/machine_knowledge.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责机器知识 SQLite 的构建、FTS5 检索、实体邻接和源码定位。"} {:block/title "需要修改时：当 `scripts/ckb_core/machine_knowledge.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/machine_knowledge.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/machine_knowledge.py:1:1)  `scripts/ckb_core/machine_knowledge.py:1-1852`"} {:block/title "内部实现：estimated_tokens — `estimated_tokens` 是第 55-56 行的函数，供所属页面定位实现。"} {:block/title "内部实现：contains_chinese_narrative — `contains_chinese_narr...` 是第 59-63 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_fts_query — `_fts_query` 是第 66-67 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_fts_query_values — `_fts_query_values` 是第 70-74 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_human_projection — `_human_projection` 是第 77-85 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_note_documents — `_note_documents` 是第 88-115 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_review_paths — `_review_paths` 是第 118-130 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_source_texts — `_source_texts` 是第 133-142 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_description — `_description` 是第 145-152 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_human_map — `_human_map` 是第 155-158 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_sections_for_entity — `_sections_for_entity` 是第 161-179 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_create_schema — `_create_schema` 是第 182-351 行的函数，供所属页面定位实现。"} {:block/title "内部实现：build_machine_knowledge — `build_machine_knowledge` 是第 354-613 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_markdown_sections — `_markdown_sections` 是第 616-634 行的函数，供所属页面定位实现。"} {:block/title "内部实现：audit_machine_knowledge — `audit_machine_knowledge` 是第 637-718 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_adjacency — `_adjacency` 是第 721-731 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_fast_graph_scores — `_fast_graph_scores` 是第 734-754 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_deterministic_ppr — `_deterministic_ppr` 是第 757-789 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_next_pack_path — `_next_pack_path` 是第 792-801 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_sql_placeholders — `该实体` 是第 804-808 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_utf8_prefix — `_utf8_prefix` 是第 811-820 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_bulk_entity_context — `_bulk_entity_context` 是第 823-846 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_diverse_candidates — `_diverse_candidates` 是第 849-872 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_compact_entity_block — `_compact_entity_block` 是第 875-931 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_static_retrieval_key — `_static_retrieval_key` 是第 934-946 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_static_retrieval_context — `_static_retrieval_con...` 是第 949-1003 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_openers — `_openers` 是第 1006-1018 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_matching_documents — `_matching_documents` 是第 1021-1044 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_document_source_link — `_document_source_link` 是第 1047-1058 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_document_block — `_document_block` 是第 1061-1074 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_retrieve_machine_deterministic — `_retrieve_machine_det...` 是第 1077-1556 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_retrieve_machine_deterministic.add — `_retrieve_machine_det...` 是第 1129-1132 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_retrieve_machine_deterministic.entity_column — `_retrieve_machine_det...` 是第 1203-1204 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_provider_record — `_provider_record` 是第 1559-1578 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_attach_keyword_fallback — `_attach_keyword_fallback` 是第 1581-1597 行的函数，供所属页面定位实现。"} {:block/title "内部实现：coverage — `coverage` 是第 1712-1732 行的函数，供所属页面定位实现。"} {:block/title "内部实现：entity_lookup — `entity_lookup` 是第 1735-1746 行的函数，供所属页面定位实现。"} {:block/title "内部实现：neighbor_lookup — `neighbor_lookup` 是第 1749-1788 行的函数，供所属页面定位实现。"} {:block/title "内部实现：source_lookup — `source_lookup` 是第 1791-1808 行的函数，供所属页面定位实现。"} {:block/title "内部实现：change_documents — `change_documents` 是第 1811-1831 行的函数，供所属页面定位实现。"} {:block/title "内部实现：sync_workspace_changes — `sync_workspace_changes` 是第 1834-1851 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_feedback 与 _contains_chinese 的协作实现]]。"} {:block/title "协作：实现时会用到 [[audit_gap_register 与 _root 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[render_integration 与 _looks_windows 的协作实现]]。"} {:block/title "协作：主要代码单元是 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[search_terms]]。"} {:block/title "协作：实现时会用到 [[search_terms 与 _split_camel 的协作实现]]。"} {:block/title "协作：实现时会用到 [[source_files]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_gap_register 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_operation_journal 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_references]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_references 与 _root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[doctor_report 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[maintenance_check]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[serve_stdio]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session 与 _session_directory 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "run"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`run` 是 `tests/e2e_agent_protocol_batch.py` 第 147-305 行定义的函数，本页绑定该固定源码范围。 该函数作为可执行验证入口，检查标识符 `run` 所指的行为与失败边界。"} {:block/title "需要修改时：当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `run` 的说明。"} {:block/title "源码入口：[打开源码：tests/e2e_agent_protocol_batch.py 第 147 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/e2e_agent_protocol_batch.py:147:1)  `tests/e2e_agent_protocol_batch.py:147-305`"} {:block/title "内部实现：run.remove_readonly — `run.remove_readonly` 是第 149-151 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[create_batch_plan 与 ProtocolRelease 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run 等测试场景]]。"} {:block/title "协作：实现时会用到 [[start_scope_extension 与 _error 的协作实现]]。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name]] 关联到这里的验证场景。"} {:block/title "协作：[[run 等测试场景]] 汇总了本页。"}]}
{:page {:block/title "run 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`tests/e2e_agent_protocol_batch.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责以可执行样例验证 `e2e_agent_protocol_batch.py` 覆盖的成功行为、失败边界和回归约束。"} {:block/title "需要修改时：当 `tests/e2e_agent_protocol_batch.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：tests/e2e_agent_protocol_batch.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/e2e_agent_protocol_batch.py:1:1)  `tests/e2e_agent_protocol_batch.py:1-320`"} {:block/title "内部实现：git — `git` 是第 33-43 行的函数，供所属页面定位实现。"} {:block/title "内部实现：review_all — `review_all` 是第 46-67 行的函数，供所属页面定位实现。"} {:block/title "内部实现：build_output — `build_output` 是第 70-89 行的函数，供所属页面定位实现。"} {:block/title "内部实现：downgrade_protocol — `downgrade_protocol` 是第 92-109 行的函数，供所属页面定位实现。"} {:block/title "内部实现：manifest_project — `manifest_project` 是第 112-124 行的函数，供所属页面定位实现。"} {:block/title "内部实现：fixed_hashes — `fixed_hashes` 是第 127-129 行的函数，供所属页面定位实现。"} {:block/title "内部实现：invoke — `invoke` 是第 132-144 行的函数，供所属页面定位实现。"} {:block/title "内部实现：main — `main` 是第 308-315 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_agent_protocol 与 _default_python 的协作实现]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[create_batch_plan 与 ProtocolRelease 的协作实现]]。"} {:block/title "协作：实现时会用到 [[initialize]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：主要代码单元是 [[run]]。"} {:block/title "协作：由 [[run]] 覆盖相关行为。"} {:block/title "协作：[[audit_obsidian]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_obsidian 与 prepare_vault 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[run]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "run_keyword_benchmark"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`run_keyword_benchmark` 是 `scripts/ckb_core/keyword_benchmark.py` 第 139-227 行定义的函数，本页绑定该固定源码范围。 负责运行固定问题集上的检索词项基准并汇总质量与时延证据。"} {:block/title "需要修改时：当 `run_keyword_benchmark` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/keyword_benchmark.py 第 139 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/keyword_benchmark.py:139:1)  `scripts/ckb_core/keyword_benchmark.py:139-227`"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[run_keyword_benchmark 与 _text_list 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run_keyword_provider 与 KeywordProviderConfig 的协作实现]]。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[run_keyword_benchmark 与 _text_list 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "run_keyword_benchmark 与 _text_list 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/keyword_benchmark.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责运行固定问题集上的检索词项基准并汇总质量与时延证据。"} {:block/title "需要修改时：当 `scripts/ckb_core/keyword_benchmark.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/keyword_benchmark.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/keyword_benchmark.py:1:1)  `scripts/ckb_core/keyword_benchmark.py:1-228`"} {:block/title "内部实现：_text_list — `_text_list` 是第 26-31 行的函数，供所属页面定位实现。"} {:block/title "内部实现：load_keyword_benchmark — `load_keyword_benchmark` 是第 34-78 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_location — `_location` 是第 81-108 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_timed_retrieval — `_timed_retrieval` 是第 111-126 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_restore_cache — `_restore_cache` 是第 129-136 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：主要代码单元是 [[run_keyword_benchmark]]。"} {:block/title "协作：实现时会用到 [[run_keyword_provider 与 KeywordProviderConfig 的协作实现]]。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[run_keyword_benchmark]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "run_keyword_provider"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`run_keyword_provider` 是 `scripts/ckb_core/keyword_fallback.py` 第 380-461 行定义的函数，本页绑定该固定源码范围。 负责在确定性词项不足时执行受预算约束的 LLM 关键词备选慢路径。"} {:block/title "需要修改时：当 `run_keyword_provider` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/keyword_fallback.py 第 380 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/keyword_fallback.py:380:1)  `scripts/ckb_core/keyword_fallback.py:380-461`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[run_keyword_provider 与 KeywordProviderConfig 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[append 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests]] 会使用这里提供的行为。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine]] 会使用这里提供的行为。"} {:block/title "协作：[[run_keyword_provider 与 KeywordProviderConfig 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "run_keyword_provider 与 KeywordProviderConfig 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/keyword_fallback.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责在确定性词项不足时执行受预算约束的 LLM 关键词备选慢路径。"} {:block/title "需要修改时：当 `scripts/ckb_core/keyword_fallback.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/keyword_fallback.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/keyword_fallback.py:1:1)  `scripts/ckb_core/keyword_fallback.py:1-633`"} {:block/title "内部实现：KeywordProviderConfig — `KeywordProviderConfig` 是第 81-90 行的类，供所属页面定位实现。"} {:block/title "内部实现：KeywordFallbackOptions — `KeywordFallbackOptions` 是第 94-99 行的类，供所属页面定位实现。"} {:block/title "内部实现：keyword_input_hash — `keyword_input_hash` 是第 102-103 行的函数，供所属页面定位实现。"} {:block/title "内部实现：keyword_request_id — `keyword_request_id` 是第 106-107 行的函数，供所属页面定位实现。"} {:block/title "内部实现：keyword_cache_key — `keyword_cache_key` 是第 110-120 行的函数，供所属页面定位实现。"} {:block/title "内部实现：canonical_keyword_request — `canonical_keyword_req...` 是第 123-140 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_machine_token — `_machine_token` 是第 143-146 行的函数，供所属页面定位实现。"} {:block/title "内部实现：validate_provider_config — `validate_provider_config` 是第 149-161 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_bounded_strings — `_bounded_strings` 是第 164-193 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_usage — `_usage` 是第 196-216 行的函数，供所属页面定位实现。"} {:block/title "内部实现：validate_provider_response — `validate_provider_res...` 是第 219-300 行的函数，供所属页面定位实现。"} {:block/title "内部实现：parse_provider_json — `parse_provider_json` 是第 303-307 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_failure — `_failure` 是第 310-323 行的函数，供所属页面定位实现。"} {:block/title "内部实现：keyword_cache_path — `keyword_cache_path` 是第 326-327 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_cache_path — `_cache_path` 是第 330-331 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_cache_record — `_cache_record` 是第 334-346 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_read_cache — `_read_cache` 是第 349-366 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_write_cache — `_write_cache` 是第 369-373 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_transient — `_transient` 是第 376-377 行的函数，供所属页面定位实现。"} {:block/title "内部实现：audit_keyword_cache — `audit_keyword_cache` 是第 464-507 行的函数，供所属页面定位实现。"} {:block/title "内部实现：write_keyword_fallback_record — `write_keyword_fallbac...` 是第 510-549 行的函数，供所属页面定位实现。"} {:block/title "内部实现：audit_keyword_fallback — `audit_keyword_fallback` 是第 552-620 行的函数，供所属页面定位实现。"} {:block/title "内部实现：unique_casefold — `unique_casefold` 是第 623-632 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[normalize]]。"} {:block/title "协作：主要代码单元是 [[run_keyword_provider]]。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests]] 会使用这里提供的行为。"} {:block/title "协作：[[KeywordFallbackRetrievalWiringTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[keyword_provider_config]] 会使用这里提供的行为。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[maintenance_check]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine]] 会使用这里提供的行为。"} {:block/title "协作：[[run_keyword_benchmark]] 会使用这里提供的行为。"} {:block/title "协作：[[run_keyword_benchmark 与 _text_list 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[run_keyword_provider]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[serve_stdio 与 _write_line 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "scripts 职责导览"} :blocks [{:block/title "标签：#类型/职责"} {:block/title "页面说明：这里汇集 scripts 范围内彼此协作的类、函数和辅助实现。"} {:block/title "协作：继续浏览 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：继续浏览 [[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]]。"} {:block/title "协作：继续浏览 [[_Transport.close 与 _StartGate 的协作实现]]。"} {:block/title "协作：继续浏览 [[__init__ 实现概览]]。"} {:block/title "协作：继续浏览 [[audit_agent_protocol 与 _default_python 的协作实现]]。"} {:block/title "协作：继续浏览 [[audit_feedback 与 _contains_chinese 的协作实现]]。"} {:block/title "协作：继续浏览 [[audit_gap_register 与 _root 的协作实现]]。"} {:block/title "协作：继续浏览 [[audit_migration 与 _entity_key 的协作实现]]。"} {:block/title "协作：继续浏览 [[audit_obsidian 与 prepare_vault 的协作实现]]。"} {:block/title "协作：继续浏览 [[audit_operation_journal 与 _root 的协作实现]]。"} {:block/title "协作：继续浏览 [[audit_output_contract 与 _default_ckb 的协作实现]]。"} {:block/title "协作：继续浏览 [[audit_references 与 _root 的协作实现]]。"} {:block/title "协作：继续浏览 [[audit_work_record_index 与 _contains_chinese 的协作实现]]。"} {:block/title "协作：继续浏览 [[bind_conversation 与 default_management_registry_path 的协作实现]]。"} {:block/title "协作：继续浏览 [[create_batch_plan 与 ProtocolRelease 的协作实现]]。"} {:block/title "协作：继续浏览 [[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]]。"} {:block/title "协作：继续浏览 [[deployment_plan 与 skill_root 的协作实现]]。"} {:block/title "协作：继续浏览 [[doctor_report 与 _version_matches 的协作实现]]。"} {:block/title "协作：继续浏览 [[ingest_event 与 default_registry_path 的协作实现]]。"} {:block/title "协作：继续浏览 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：继续浏览 [[keyword_provider_config 与 parser 的协作实现]]。"} {:block/title "协作：继续浏览 [[load_page_config 与 _merge_known 的协作实现]]。"} {:block/title "协作：继续浏览 [[main 与 sha256 的协作实现]]。"} {:block/title "协作：继续浏览 [[main 相关实现]]。"} {:block/title "协作：继续浏览 [[maintenance_check 与 capability_matrix 的协作实现]]。"} {:block/title "协作：继续浏览 [[module_name 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：继续浏览 [[package_showcase 与 _parse_sample 的协作实现]]。"} {:block/title "协作：继续浏览 [[parse_file 与 _language 的协作实现]]。"} {:block/title "协作：继续浏览 [[preflight 与 git 的协作实现]]。"} {:block/title "协作：继续浏览 [[query_graph 与 _networkx_modules 的协作实现]]。"} {:block/title "协作：继续浏览 [[record_note 与 page_tag 的协作实现]]。"} {:block/title "协作：继续浏览 [[register_obsidian_plugin 与 default_obsidian_plugin_registry 的协作实现]]。"} {:block/title "协作：继续浏览 [[render_integration 与 _looks_windows 的协作实现]]。"} {:block/title "协作：继续浏览 [[retrieve 与 _tokens 的协作实现]]。"} {:block/title "协作：继续浏览 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：继续浏览 [[run_keyword_benchmark 与 _text_list 的协作实现]]。"} {:block/title "协作：继续浏览 [[run_keyword_provider 与 KeywordProviderConfig 的协作实现]]。"} {:block/title "协作：继续浏览 [[search_terms 与 _split_camel 的协作实现]]。"} {:block/title "协作：继续浏览 [[serve_stdio 与 _write_line 的协作实现]]。"} {:block/title "协作：继续浏览 [[source_files 与 sha256 的协作实现]]。"} {:block/title "协作：继续浏览 [[start_scope_extension 与 _error 的协作实现]]。"} {:block/title "协作：继续浏览 [[start_session 与 _session_directory 的协作实现]]。"} {:block/title "协作：继续浏览 [[sync_human_layer 与 _source_manifest 的协作实现]]。"} {:block/title "协作：可从 [[source 代码导览]] 进入本页。"}]}
{:page {:block/title "search_terms"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`search_terms` 是 `scripts/ckb_core/query_terms.py` 第 65-69 行定义的函数，本页绑定该固定源码范围。 负责从自然语言构造确定性中文及标识符检索词项。"} {:block/title "需要修改时：当 `search_terms` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/query_terms.py 第 65 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/query_terms.py:65:1)  `scripts/ckb_core/query_terms.py:65-69`"} {:block/title "协作：实现时会用到 [[search_terms 与 _split_camel 的协作实现]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：[[QueryTermsTests]] 会使用这里提供的行为。"} {:block/title "协作：[[normalize 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[search_terms 与 _split_camel 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "search_terms 与 _split_camel 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/query_terms.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责从自然语言构造确定性中文及标识符检索词项。"} {:block/title "需要修改时：当 `scripts/ckb_core/query_terms.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/query_terms.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/query_terms.py:1:1)  `scripts/ckb_core/query_terms.py:1-106`"} {:block/title "内部实现：_split_camel — `_split_camel` 是第 23-28 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_ranked_terms — `_ranked_terms` 是第 31-62 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_ranked_terms.add — `_ranked_terms.add` 是第 35-37 行的函数，供所属页面定位实现。"} {:block/title "内部实现：index_terms — `index_terms` 是第 72-74 行的函数，供所属页面定位实现。"} {:block/title "内部实现：fts_query_terms — `fts_query_terms` 是第 77-81 行的函数，供所属页面定位实现。"} {:block/title "内部实现：build_fts_query — `build_fts_query` 是第 84-88 行的函数，供所属页面定位实现。"} {:block/title "内部实现：explicit_anchors — `explicit_anchors` 是第 91-105 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[normalize]]。"} {:block/title "协作：主要代码单元是 [[search_terms]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[QueryTermsTests]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[search_terms]] 会使用这里提供的行为。"}]}
{:page {:block/title "serve_stdio"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`serve_stdio` 是 `scripts/ckb_core/stdio_server.py` 第 202-391 行定义的函数，本页绑定该固定源码范围。 负责实现 `stdio_server.py` 中由固定源码定义的命令或知识库处理步骤。"} {:block/title "需要修改时：当 `serve_stdio` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/stdio_server.py 第 202 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/stdio_server.py:202:1)  `scripts/ckb_core/stdio_server.py:202-391`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[maintenance_check 与 capability_matrix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[serve_stdio 与 _write_line 的协作实现]]。"} {:block/title "协作：实现时会用到 [[start_scope_extension 与 _error 的协作实现]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：[[serve_stdio 与 _write_line 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "serve_stdio 与 _write_line 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/stdio_server.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责实现 `stdio_server.py` 中由固定源码定义的命令或知识库处理步骤。"} {:block/title "需要修改时：当 `scripts/ckb_core/stdio_server.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/stdio_server.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/stdio_server.py:1:1)  `scripts/ckb_core/stdio_server.py:1-392`"} {:block/title "内部实现：_write_line — `_write_line` 是第 25-29 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_integer — `_integer` 是第 32-35 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_utf8_safe — `_utf8_safe` 是第 38-40 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_required_text — `_required_text` 是第 43-50 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_keyword_fallback_options — `_keyword_fallback_opt...` 是第 53-104 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_record_explanation — `_record_explanation` 是第 107-199 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[audit_agent_protocol]]。"} {:block/title "协作：实现时会用到 [[audit_feedback]]。"} {:block/title "协作：实现时会用到 [[command]]。"} {:block/title "协作：实现时会用到 [[run_keyword_provider 与 KeywordProviderConfig 的协作实现]]。"} {:block/title "协作：主要代码单元是 [[serve_stdio]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[serve_stdio]] 会使用这里提供的行为。"}]}
{:page {:block/title "source 代码导览"} :blocks [{:block/title "标签：#类型/职责"} {:block/title "页面说明：从这里按职责进入项目中的类、函数和相关实现。"} {:block/title "协作：继续浏览 [[scripts 职责导览]]。"} {:block/title "协作：继续浏览 [[tests 职责导览]]。"}]}
{:page {:block/title "source_files"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`source_files` 是 `scripts/package_release.py` 第 42-63 行定义的函数，本页绑定该固定源码范围。 负责实现 `package_release.py` 中由固定源码定义的命令或知识库处理步骤。"} {:block/title "需要修改时：当 `source_files` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/package_release.py 第 42 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/package_release.py:42:1)  `scripts/package_release.py:42-63`"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[PackageReleaseTests]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[PackageReleaseTests]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[source_files 与 sha256 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "source_files 与 sha256 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/package_release.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责实现 `package_release.py` 中由固定源码定义的命令或知识库处理步骤。"} {:block/title "需要修改时：当 `scripts/package_release.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/package_release.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/package_release.py:1:1)  `scripts/package_release.py:1-307`"} {:block/title "内部实现：sha256 — `sha256` 是第 34-39 行的函数，供所属页面定位实现。"} {:block/title "内部实现：core_capabilities — `core_capabilities` 是第 66-79 行的函数，供所属页面定位实现。"} {:block/title "内部实现：validate_core_boundary — `validate_core_boundary` 是第 82-97 行的函数，供所属页面定位实现。"} {:block/title "内部实现：validate_full_payload — `validate_full_payload` 是第 100-119 行的函数，供所属页面定位实现。"} {:block/title "内部实现：build_core — `build_core` 是第 122-185 行的函数，供所属页面定位实现。"} {:block/title "内部实现：validate_plugin_dist — `validate_plugin_dist` 是第 188-210 行的函数，供所属页面定位实现。"} {:block/title "内部实现：build_plugin — `build_plugin` 是第 213-270 行的函数，供所属页面定位实现。"} {:block/title "内部实现：build — `build` 是第 273-276 行的函数，供所属页面定位实现。"} {:block/title "内部实现：main — `main` 是第 279-302 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[AutomationTest.register]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：主要代码单元是 [[source_files]]。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "start_scope_extension"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`start_scope_extension` 是 `scripts/ckb_core/scope_extension.py` 第 261-451 行定义的函数，本页绑定该固定源码范围。 负责实现 `scope_extension.py` 中由固定源码定义的命令或知识库处理步骤。"} {:block/title "需要修改时：当 `start_scope_extension` 的输入、输出、状态转换或失败返回变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/scope_extension.py 第 261 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/scope_extension.py:261:1)  `scripts/ckb_core/scope_extension.py:261-451`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_migration 与 _entity_key 的协作实现]]。"} {:block/title "协作：实现时会用到 [[initialize]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[preflight]]。"} {:block/title "协作：实现时会用到 [[start_scope_extension 与 _error 的协作实现]]。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[refresh 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[ScopeExtensionTest]] 会使用这里提供的行为。"} {:block/title "协作：[[start_scope_extension 与 _error 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "start_scope_extension 与 _error 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/scope_extension.py` 页面汇总该文件在固定提交中的职责、入口与可审阅源码范围。 负责实现 `scope_extension.py` 中由固定源码定义的命令或知识库处理步骤。"} {:block/title "需要修改时：当 `scripts/ckb_core/scope_extension.py` 的职责、命令入口、数据契约或主要符号变化时，应更新本页并重跑对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/scope_extension.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/scope_extension.py:1:1)  `scripts/ckb_core/scope_extension.py:1-909`"} {:block/title "内部实现：_error — `_error` 是第 34-35 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_canonical_entry — `_canonical_entry` 是第 38-54 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_tree_manifest — `_tree_manifest` 是第 57-72 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_same_manifest — `_same_manifest` 是第 75-76 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_sqlite_checks — `_sqlite_checks` 是第 79-100 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_release_audit_handles — `_release_audit_handles` 是第 103-107 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_candidate_graph — `_candidate_graph` 是第 110-118 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_dimension — `_dimension` 是第 121-131 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_page_ids — `_page_ids` 是第 134-135 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_replace_prefix — `_replace_prefix` 是第 138-145 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_rebind_preserved_json — `_rebind_preserved_json` 是第 148-167 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_layer_inventory — `_layer_inventory` 是第 170-195 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_preservation_errors — `_preservation_errors` 是第 198-216 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_recomputed_review_sets — `_recomputed_review_sets` 是第 219-246 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_load_extension_state — `_load_extension_state` 是第 249-258 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_control_records — `_control_records` 是第 454-521 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_control_records.depth — `_control_records.depth` 是第 504-517 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_control_selection — `_control_selection` 是第 524-548 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_public_control — `_public_control` 是第 551-552 行的函数，供所属页面定位实现。"} {:block/title "内部实现：extension_status — `extension_status` 是第 555-595 行的函数，供所属页面定位实现。"} {:block/title "内部实现：audit_scope_extension — `audit_scope_extension` 是第 598-692 行的函数，供所属页面定位实现。"} {:block/title "内部实现：audit_scope_extension.check — `audit_scope_extension...` 是第 604-605 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_control_path — `_control_path` 是第 695-696 行的函数，供所属页面定位实现。"} {:block/title "内部实现：cutover_scope_extension — `cutover_scope_extension` 是第 699-804 行的函数，供所属页面定位实现。"} {:block/title "内部实现：_active_control — `_active_control` 是第 807-818 行的函数，供所属页面定位实现。"} {:block/title "内部实现：rollback_scope_extension — `rollback_scope_extension` 是第 821-908 行的函数，供所属页面定位实现。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[_Transport.close]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_migration 与 _entity_key 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[maintenance_check]]。"} {:block/title "协作：实现时会用到 [[preflight]]。"} {:block/title "协作：主要代码单元是 [[start_scope_extension]]。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests]] 覆盖相关行为。"} {:block/title "协作：由 [[AgentProtocolBatchApplyTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：[[ScopeExtensionTest]] 会使用这里提供的行为。"} {:block/title "协作：[[_Transport.close 与 _StartGate 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_agent_protocol 与 _default_python 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[command 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[create_batch_plan 与 ProtocolRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[create_knowledge_batch_plan 与 KnowledgeRelease 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[keyword_provider_config 与 parser 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph]] 会使用这里提供的行为。"} {:block/title "协作：[[refresh]] 会使用这里提供的行为。"} {:block/title "协作：[[refresh 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[run]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[serve_stdio]] 会使用这里提供的行为。"} {:block/title "协作：[[start_scope_extension]] 会使用这里提供的行为。"}]}
{:page {:block/title "start_session"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`start_session` 是源码中负责管理 Agent 会话、构建中记录和修改总结落页的命名代码单元。 它在所属模块内执行管理 Agent 会话、构建中记录和修改总结落页，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当管理 Agent 会话、构建中记录和修改总结落页所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/agent_maintenance.py 第 70 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_maintenance.py:70:1)  `scripts/ckb_core/agent_maintenance.py:70-142`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[initialize 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：实现时会用到 [[record_note 与 page_tag 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[start_session 与 _session_directory 的协作实现]]。"} {:block/title "协作：[[start_session 与 _session_directory 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "start_session 与 _session_directory 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现Agent 任务会话、构建中笔记排队、修改总结和生命周期状态。 它是 Code Knowledge Builder 中承载Agent 任务会话、构建中笔记排队、修改总结和生命周期状态的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当Agent 任务会话、构建中笔记排队、修改总结和生命周期状态的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/agent_maintenance.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_maintenance.py:1:1)  `scripts/ckb_core/agent_maintenance.py:1-255`"} {:block/title "内部实现：_session_directory — 该附属代码负责管理 Agent 会话、构建中记录和修改总结落页，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_new_session_id — 该附属代码负责管理 Agent 会话、构建中记录和修改总结落页，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_record_or_queue — 该附属代码负责Agent 任务会话、构建中笔记排队、修改总结和生命周期状态，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_summary_heading_errors — 该附属代码负责Agent 任务会话、构建中笔记排队、修改总结和生命周期状态，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：finish_session — 该附属代码负责管理 Agent 会话、构建中记录和修改总结落页，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：sessions_status — 该附属代码负责管理 Agent 会话、构建中记录和修改总结落页，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[record_note]]。"} {:block/title "协作：实现时会用到 [[record_note 与 page_tag 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：主要代码单元是 [[start_session]]。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[start_session]] 会使用这里提供的行为。"}]}
{:page {:block/title "sync_human_layer"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`sync_human_layer` 是 `scripts/ckb_core/knowledge_layers.py` 中负责把已审计 Markdown 投影同步到 human 层，并保持生成文件清单和镜像一致的函数。 它按源码所示的参数、条件分支和数据结构完成把已审计 Markdown 投影同步到 human 层，并保持生成文件清单和镜像一致，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当机器事实到 human/markdown 双层投影和镜像审计的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/knowledge_layers.py 第 126 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/knowledge_layers.py:126:1)  `scripts/ckb_core/knowledge_layers.py:126-192`"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[audit_output_contract 与 _default_ckb 的协作实现]]。"} {:block/title "协作：实现时会用到 [[sync_human_layer 与 _source_manifest 的协作实现]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：由 [[command 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "sync_human_layer 与 _source_manifest 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`scripts/ckb_core/knowledge_layers.py` 是 `scripts/ckb_core/knowledge_layers.py` 中负责汇总并提供机器事实到 human/markdown 双层投影和镜像审计的文件入口。 它按源码所示的参数、条件分支和数据结构完成汇总并提供机器事实到 human/markdown 双层投影和镜像审计，并把确定结果交给调用方或后续审计、索引、投影阶段。"} {:block/title "需要修改时：当机器事实到 human/markdown 双层投影和镜像审计的输入格式、状态规则、错误处理或输出契约变化时，需要修改该代码单元并复核对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/knowledge_layers.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/knowledge_layers.py:1:1)  `scripts/ckb_core/knowledge_layers.py:1-262`"} {:block/title "内部实现：_source_manifest — 处理 `manifest` 对应的数据与约束。"} {:block/title "内部实现：build_facts_layer — 构建 `facts_layer` 对应的数据与约束。"} {:block/title "内部实现：audit_facts_layer — 审计 `facts_layer` 对应的数据与约束。"} {:block/title "内部实现：_copy_file — 复制 `file` 对应的数据与约束。"} {:block/title "内部实现：_generated_files — 处理 `files` 对应的数据与约束。"} {:block/title "内部实现：audit_human_layer — 审计 `human_layer` 对应的数据与约束。"} {:block/title "内部实现：mirror_note — 镜像 `note` 对应的数据与约束。"} {:block/title "协作：实现时会用到 [[CkbError]]。"} {:block/title "协作：实现时会用到 [[CkbError 与 DependencyError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[append]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：主要代码单元是 [[sync_human_layer]]。"} {:block/title "协作：由 [[AutomationTest.register 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[KeywordFallbackRetrievalWiringTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[ScopeExtensionTest]] 覆盖相关行为。"} {:block/title "协作：[[record_note]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[sync_human_layer]] 会使用这里提供的行为。"}]}
{:page {:block/title "tests 职责导览"} :blocks [{:block/title "标签：#类型/职责"} {:block/title "页面说明：这里汇集 tests 范围内彼此协作的类、函数和辅助实现。"} {:block/title "协作：继续浏览 [[AgentProtocolBatchApplyTests 等测试场景]]。"} {:block/title "协作：继续浏览 [[AutomationTest.register 等测试场景]]。"} {:block/title "协作：继续浏览 [[Box 等测试场景（explicit-instantiation-valid 测试）]]。"} {:block/title "协作：继续浏览 [[Box 等测试场景（main 测试）]]。"} {:block/title "协作：继续浏览 [[ChineseRetrievalFixtureTests 等测试场景]]。"} {:block/title "协作：继续浏览 [[CodeKnowledgeBuilderTests 等测试场景]]。"} {:block/title "协作：继续浏览 [[KeywordFallbackRetrievalWiringTests 等测试场景]]。"} {:block/title "协作：继续浏览 [[MigrationTest 等测试场景]]。"} {:block/title "协作：继续浏览 [[PackageReleaseTests 等测试场景]]。"} {:block/title "协作：继续浏览 [[QueryTermsTests 等测试场景]]。"} {:block/title "协作：继续浏览 [[ScopeExtensionTest 等测试场景]]。"} {:block/title "协作：继续浏览 [[SessionStdioLifecycleTests 等测试场景]]。"} {:block/title "协作：继续浏览 [[append 等测试场景]]。"} {:block/title "协作：继续浏览 [[bind_reference 等测试场景]]。"} {:block/title "协作：继续浏览 [[command 等测试场景]]。"} {:block/title "协作：继续浏览 [[debug_value 等测试场景]]。"} {:block/title "协作：继续浏览 [[execute 等测试场景]]。"} {:block/title "协作：继续浏览 [[keyword_provider 等测试场景]]。"} {:block/title "协作：继续浏览 [[main 等测试场景（fake_logseq 测试）]]。"} {:block/title "协作：继续浏览 [[main 等测试场景（generate_large_fixture 测试）]]。"} {:block/title "协作：继续浏览 [[main 等测试场景（session_stdio_harness_probe 测试）]]。"} {:block/title "协作：继续浏览 [[main 等测试场景（session_stdio_reactivation_probe 测试）]]。"} {:block/title "协作：继续浏览 [[normalize 等测试场景]]。"} {:block/title "协作：继续浏览 [[one_cycle 等测试场景]]。"} {:block/title "协作：继续浏览 [[refresh 等测试场景]]。"} {:block/title "协作：继续浏览 [[resolve_checkout_git_dir 等测试场景]]。"} {:block/title "协作：继续浏览 [[run 等测试场景]]。"} {:block/title "协作：可从 [[source 代码导览]] 进入本页。"}]}
]}
