# Agent 机器知识阅读包

问题：parse_file LspClient.start sync_human_layer create_source_snapshot retrieve_machine ingest_event audit_migration load_page_config

检索档位：fast

所有说明使用简体中文；代码标识符保持源码形式。

## LspClient.start

选择原因：保留显式标识符 `lspclient.start`；确定性词项 `client`；确定性词项 `lsp`；确定性词项 `lspclient`

源码：[打开源码：scripts/ckb_core/providers.py 第 270 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/providers.py:270:1)  `scripts/ckb_core/providers.py:270-280`

人类知识页：[[LspClient.start]]（page）

### 中文含义

`LspClient.start` 是源码中负责调用语言提供器并整理定义、符号与诊断证据的命名代码单元。

## sync_human_layer

选择原因：保留显式标识符 `sync_human_layer`；确定性词项 `human`；确定性词项 `layer`；确定性词项 `sync`

源码：[打开源码：scripts/ckb_core/knowledge_layers.py 第 126 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/knowledge_layers.py:126:1)  `scripts/ckb_core/knowledge_layers.py:126-181`

人类知识页：[[sync_human_layer]]（page）

### 中文含义

`sync_human_layer` 是源码中负责生成事实层与中文人类层并核对跨层一致性的命名代码单元。

### 中文职责

它在所属模块内执行生成

> 本节已按预算截断。

## load_page_config

选择原因：保留显式标识符 `load_page_config`；确定性词项 `config`；确定性词项 `load`；确定性词项 `load_page_config`

源码：[打开源码：scripts/ckb_core/page_config.py 第 209 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/page_config.py:209:1)  `scripts/ckb_core/page_config.py:209-219`

人类知识页：[[load_page_config]]（page）

## create_source_snapshot

选择原因：保留显式标识符 `create_source_snapshot`；确定性词项 `create`；确定性词项 `create_source_snapshot`；确定性词项 `snapshot`

源码：[打开源码：scripts/ckb_core/gitrepo.py 第 230 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/gitrepo.py:230:1)  `scripts/ckb_core/gitrepo.py:230-257`

人类知识页：[[create_source_snapshot]]（page）

## audit_migration

选择原因：保留显式标识符 `audit_migration`；确定性词项 `audit`；确定性词项 `audit_migration`；确定性词项 `migration`

源码：[打开源码：scripts/ckb_core/migration.py 第 353 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/migration.py:353:1)  `scripts/ckb_core/migration.py:353-460`

人类知识页：[[audit_migration]]（page）

## retrieve_machine

选择原因：保留显式标识符 `retrieve_machine`；确定性词项 `machine`；确定性词项 `retrieve`；确定性词项 `retrieve_machine`

源码：[打开源码：scripts/ckb_core/machine_knowledge.py 第 942 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/machine_knowledge.py:942:1)  `scripts/ckb_core/machine_knowledge.py:942-1315`

人类知识页：[[retrieve_machine]]（page）

## ingest_event

选择原因：保留显式标识符 `ingest_event`；确定性词项 `event`；确定性词项 `ingest`；确定性词项 `ingest_event`

源码：[打开源码：scripts/ckb_core/automation.py 第 1321 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation.py:1321:1)  `scripts/ckb_core/automation.py:1321-1392`

人类知识页：[[ingest_event]]（page）

### 中文含义

`ingest_event` 是所有 Harness Hook 进入 CKB 自动化核心的统一边界。

### 中文职责

它先匹配项目，再核对当前 session 的精确 Skill 激活；显式 prompt、原生 Skill 元数据或已登记激活通过后才脱敏、入队和写入数据库。

### 什么时候需要修改

当项目路由、激活判据、静默忽略语义、脱敏或 Hook 输出约定变化时，需要修改该函数并重跑正反向 canary。

## parse_file

选择原因：保留显式标识符 `parse_file`；确定性词项 `file`；确定性词项 `parse`；确定性词项 `parse_file`

源码：[打开源码：scripts/ckb_core/parsers.py 第 191 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/parsers.py:191:1)  `scripts/ckb_core/parsers.py:191-329`

人类知识页：[[parse_file]]（page）

### 中文含义

`parse_file` 是源码中负责解析命名声明并保存稳定源码范围的命名代码单元。

### 中文职责

它在所属模块内执行解析命名声明并保存稳定源码范围，并把确定结果交给调用方、审计门或投影阶段。

### 什么时候需要修改

当解析命名声明并保存稳定源码范围所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。

### 来源核对

Agent 已重新打开固定 Git 源码中 scripts/ckb_core/parsers.py 第 191 至 329 行，核对名称、签名和局部实现后写出本条中文说明。
