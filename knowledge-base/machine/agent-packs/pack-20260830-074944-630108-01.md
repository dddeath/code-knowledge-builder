# Agent 机器知识阅读包

问题：当前项目如何将自然语言查询转换为 SQLite FTS5 检索词项？请定位规范化、中文切词、标识符拆分、词项扩展、FTS MATCH 查询、精确锚点和排序相关实现。

检索档位：fast

所有说明使用简体中文；代码标识符保持源码形式。

## scripts/ckb_core/agent_index.py

选择原因：确定性词项 `sqlite`；确定性词项 `实现`；确定性词项 `检索`；机器实体全文命中

源码：[打开源码：scripts/ckb_core/agent_index.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_index.py:1:1)  `scripts/ckb_core/agent_index.py:1-569`

人类知识页：[[retrieve 与 _tokens 的协作实现]]（page）

### 中文职责

它是 Code Knowledge Builder 中承载旧版 SQLite 页面索引和检索接口兼容层的源码边界，并连接相邻模块或测试。

### 什么时候需要修改

当旧版 SQLite 页面索引和检索接口兼容层的输入、输出、状态门或验证契约发生变化时，需要同

> 本节已按预算截断。

## retrieve_machine

选择原因：确定性词项 `fts`；确定性词项 `中文`；确定性词项 `标识`；确定性词项 `标识符`

源码：[打开源码：scripts/ckb_core/machine_knowledge.py 第 942 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/machine_knowledge.py:942:1)  `scripts/ckb_core/machine_knowledge.py:942-1315`

人类知识页：[[retrieve_machine]]（page）

### 中文职责

它组合精确锚点、中文与标识符词项、实体和章节 FTS、元数据加权、测试折扣、固定图传播、路径多样化及缓存上下文，稳定保留最相关源码目标。

### 固定源码摘录

def retrieve_machine(
    output: Path,
    question: str,
    budget: int = 1500,

> 本节已按预算截断。

## scripts/ckb_core/automation.py

选择原因：确定性词项 `sqlite`；确定性词项 `精确`；确定性词项 `范化`；确定性词项 `规范`

源码：[打开源码：scripts/ckb_core/automation.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation.py:1:1)  `scripts/ckb_core/automation.py:1-1632`

人类知识页：[[ingest_event 与 default_registry_path 的协作实现]]（page）

### 什么时候需要修改

当激活证据、注册表 Schema、Harness 事件映射、SQLite 表、幂等键、路径过滤或晋升规则变化时，需要修改本页。

### 中文含义

该代码页汇总跨 Harness 会话与修改自动化，包括项目登记、session 级 Skill 激活、

> 本节已按预算截断。

## audit_global

选择原因：确定性词项 `sqlite`；确定性词项 `中文`；机器实体全文命中；中文章节全文命中

源码：[打开源码：scripts/ckb_core/pipeline.py 第 2739 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/pipeline.py:2739:1)  `scripts/ckb_core/pipeline.py:2739-3036`

人类知识页：[[status 与 _replace_output_prefix 的协作实现]]（appendix）

### 中文作用

重跑分段、审阅、迁移、来源、中文、投影、SQLite 和链接等全局完成门。

### 来源核对

Agent 已从目标固定提交对应的 Git blob 重新打开 scripts/ckb_core/pipeline.py 第 2739 至 3036 行，核对名称、签名、目录提升分支与调用后确认本条说明。

## _version_matches

选择原因：机器实体全文命中；中文章节全文命中；文件、标识符和中文职责确定性匹配

源码：[打开源码：scripts/ckb_core/providers.py 第 32 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/providers.py:32:1)  `scripts/ckb_core/providers.py:32-34`

人类知识页：[[LspClient.start 与 _version_matches 的协作实现]]（appendix）

### 固定源码摘录

def _version_matches(expected: str, output: str) -> bool:
    lowered = output.lower()
    return expected.lower() in lowered or (len(expected) == 40 and expected[:7].lower() in lowered)

### 中文作用

该附属代码负责Pyright、TypeScript、clangd 与 csharp-ls 的真实 LSP 证据采集，

> 本节已按预算截断。

## audit_migration

选择原因：确定性词项 `sqlite`；机器实体全文命中；中文章节全文命中；文件、标识符和中文职责确定性匹配

源码：[打开源码：scripts/ckb_core/migration.py 第 353 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/migration.py:353:1)  `scripts/ckb_core/migration.py:353-460`

人类知识页：[[audit_migration]]（page）

### 中文职责

它区分不可变的迁移基线与可继续写入的工作副本，验证 Markdown/JSON 可读性和 SQLite 完整性，并在全部门通过后提升迁移状态。

### 中文含义

`audit_migration` 对增量迁移计划、复用集合、可变层基线、审阅状态和目标图来源执行确定性复核。

## scripts/ckb_core/page_config.py

选择原因：确定性词项 `实现`；确定性词项 `范化`；确定性词项 `规范`；确定性词项 `规范化`

源码：[打开源码：scripts/ckb_core/page_config.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/page_config.py:1:1)  `scripts/ckb_core/page_config.py:1-244`

人类知识页：[[load_page_config 与 _merge_known 的协作实现]]（page）

### 中文含义

该文件集中实现页面配置合并、规范化、校验和固定。

### 中文职责

它是 Code Knowledge Builder 中承载页面配置合并、规范化、校验和固定的源码边界，并连接相邻模块或测试。

## AutomationTest.test_automation_fts_finds_pending_machine_record

选择原因：确定性词项 `fts`；确定性词项 `检索`；机器实体全文命中；中文章节全文命中

源码：[打开源码：tests/test_automation.py 第 746 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_automation.py:746:1)  `tests/test_automation.py:746-760`

人类知识页：[[AutomationTest.event 等测试场景]]（appendix）

### 中文作用

该测试验证自动化机器记录的 FTS 检索符合固定契约。

### 固定源码摘录

def test_automation_fts_finds_pending_machine_record(self) -> None:
        self.register("generic")
        ingest_event(
            "generic",
            {"canonical_type": "turn.prompt", "event_id": "p", "session_id": "fts", "turn_id": "t", "cwd": str(self.repo), "prompt": "$code-knowledge-builder 实现会话

> 本节已按预算截断。
