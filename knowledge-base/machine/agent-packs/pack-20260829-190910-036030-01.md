# Agent 机器知识阅读包

问题：为 Obsidian CKB 解释系统制定实现计划：在独立右侧边栏流式显示可审计的 AI 执行过程与解释正文，不暴露隐藏思维链；根据问题复杂度使用确定性快速路径或完整 Agent 检索、record、审计路径。定位 SelectionLearningService、SelectionLearningView、provider text_delta、CKB stdio retrieve、复杂度判别和完成门。

检索档位：fast

所有说明使用简体中文；代码标识符保持源码形式。

## scripts/ckb_core/obsidian.py

选择原因：确定性词项 `ckb`；确定性词项 `obsidian`；确定性词项 `实现`；确定性词项 `审计`

源码：[打开源码：scripts/ckb_core/obsidian.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/obsidian.py:1:1)  `scripts/ckb_core/obsidian.py:1-134`

人类知识页：[[prepare_vault 与 install_obsidian 的协作实现]]（page）

### 来源核对

Agent 已重新打开目标 5.1 固定 Git 源码中 scripts/ckb_core/obsidian.py 第 1 至 134 行，核对名称、签名、分支和调用后确认本条说明。

### 中文含义

该页面汇总 Obsidian vault 准备、最小配置安装、生成器所有权和审计实现。

## retrieve

选择原因：确定性词项 `agent`；确定性词项 `ckb`；确定性词项 `retrieve`；确定性词项 `审计`

源码：[打开源码：scripts/ckb_core/agent_index.py 第 440 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_index.py:440:1)  `scripts/ckb_core/agent_index.py:440-568`

人类知识页：[[retrieve]]（page）

### 中文含义

`retrieve` 是源码中负责维护旧版页面索引兼容接口并生成受预算约束的阅读包的命名代码单元。

### 中文职责

它在所属模块内执行维护旧版页面索引兼容接口并生成受预算约束的阅读包，并把确定结果交给调用方、审计门或投影阶段。

## _record_or_queue

选择原因：确定性词项 `agent`；确定性词项 `ckb`；确定性词项 `record`；确定性词项 `使用`

源码：[打开源码：scripts/ckb_core/agent_maintenance.py 第 37 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_maintenance.py:37:1)  `scripts/ckb_core/agent_maintenance.py:37-67`

人类知识页：[[start_session 与 _session_directory 的协作实现]]（appendix）

### 固定源码摘录

def _record_or_queue(
    output: Path,
    kind: str,
    title: str,
    body_path: Path,
    *,
    query_record: Path | None = None,
    changed_paths: list[str] | None = None,
) -> dict[str, Any]:
    if (output / "markdown/projection.json").is_file():
        selectors = selectors_for_changed_paths(output, changed_paths or [])
        return {

> 本节已按预算截断。

## retrieve_machine

选择原因：确定性词项 `agent`；确定性词项 `ckb`；确定性词项 `retrieve`；确定性词项 `据问`

源码：[打开源码：scripts/ckb_core/machine_knowledge.py 第 942 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/machine_knowledge.py:942:1)  `scripts/ckb_core/machine_knowledge.py:942-1315`

人类知识页：[[retrieve_machine]]（page）

### 中文含义

`retrieve_machine` 根据问题生成来源绑定且受预算约束的 Agent 阅读包。

### 中文职责

它组合精确锚点、中文与标识符词项、实体和章节 FTS、元数据加权、测试折扣、固定图传播、路径多样化及缓存上下文，稳定保留最相关源码目标。

## context_budget_record

选择原因：确定性词项 `ckb`；确定性词项 `record`；确定性词项 `使用`；确定性词项 `定性`

源码：[打开源码：scripts/ckb_core/navigation.py 第 358 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/navigation.py:358:1)  `scripts/ckb_core/navigation.py:358-378`

人类知识页：[[module_name 与 estimated_tokens 的协作实现]]（appendix）

### 中文作用

该附属代码负责页面配额、实体归属、关系预算和上下文预算的确定性决策，并把结果交给所属页面中的主流程使用。

### 来源核对

Agent 已重新打开固定 Git 源码中 scripts/ckb_core/navigation.py 第 358 至 378 行，核对名称、签名和局部实现后写出本条中文说明。

## record_note

选择原因：确定性词项 `ckb`；确定性词项 `record`；确定性词项 `审计`；确定性词项 `执行`

源码：[打开源码：scripts/ckb_core/workspace_notes.py 第 106 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/workspace_notes.py:106:1)  `scripts/ckb_core/workspace_notes.py:106-183`

人类知识页：[[record_note]]（page）

### 中文含义

`record_note` 是源码中负责保存实时工作区变化和双链知识记录的命名代码单元。

## _overview_text

选择原因：确定性词项 `agent`；确定性词项 `ckb`；确定性词项 `text`；文件、标识符和中文职责确定性匹配

源码：[打开源码：scripts/ckb_core/pipeline.py 第 1819 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/pipeline.py:1819:1)  `scripts/ckb_core/pipeline.py:1819-1825`

人类知识页：[[status 与 _replace_output_prefix 的协作实现]]（appendix）

### 中文作用

从 Agent 审阅字段组合代码页的中文概览。

### 来源核对

Agent 已从目标固定提交对应的 Git blob 重新打开 scripts/ckb_core/pipeline.py 第 1819 至 1825 行，核对名称、签名、目录提升分支与调用后确认本条说明。

### 固定源码摘录

def _overview_text(entity: dict[str, Any], page_config: dict[str, Any]) -> str:
    fields = page_config["content"]["overview_fields"]
    values = {
        "meaning": str(entity.get("meaning_zh", "")).strip(),
        "role": str(entity.get("role_zh", "")).strip(),
    }
    return " ".join(values[field] for field in fields if values[field]).strip()

## scripts/ckb_core/source_links.py

选择原因：确定性词项 `ckb`；确定性词项 `obsidian`；确定性词项 `检索`；确定性词项 `路径`

源码：[打开源码：scripts/ckb_core/source_links.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/source_links.py:1:1)  `scripts/ckb_core/source_links.py:1-184`

人类知识页：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]]（page）

### 中文含义

该文件生成指向本地源码精确位置和 Obsidian 笔记的可点击链接。

### 中文职责

它集中验证打开器配置、限制仓库路径边界，并让批量检索复用同一渲染器与路径缓存。

### 什么时候需要修改

支持的编辑器、URI 格式、路径边界或缓存行为变化时，需要修改该文件。

### 来源核对

Agent 已从目标固定提交对应的 Git blob 重新打开 scripts/ckb_core/source_links.py 第 1 至 184 行，核对声明名称、签名、关键分支与调用后确认本条说明。
