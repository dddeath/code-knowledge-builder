# Agent 机器知识阅读包

问题：那么是否可以加入llm慢路径，用于在召回质量不佳时使用精准召回，可以参考各类记忆系统设计
来源文档：analysis/Code Knowledge Builder 秋招面试官拷问手册.md
原始选中文本：精确符号召回为 40%
承接问题：为什么召回率这么低，与固定式脚本构建词项是否有关，尤其是在中文语境下
上一轮解释：核心含义：这里的“精确符号召回为 40%”表示，在预期应命中的源码符号中，检索只找到了约四成。它衡量的是精确符号命中能力，不等同于相关页面召回率，更不直接代表最终回答只有 40% 正确。

召回偏低很可能与固定式词项构建有关，中文语境会进一步放大问题。确定性脚本若主要依赖字面词项，就难以连接中文问题与英文代码标识符之间的语义。例如“机器知识检索”实际可能对应 `retrieve_machine`，而“召回率低”未必会直接出现在符号名或源码文本中。当前 Agent pack 也显示，候选文件多由 `builder`、`code`、`knowledge`、`中文`、`固定` 等宽泛词项选出：这些词能扩大候选范围，却不一定把真正相关的检索实现排到前面。

需要区分三个边界：

1. **确定性不等于低召回**：若维护了中文别名、符号拆词、同义词和实体关系，确定性检索同样可以有较高召回。
2. **中文问题不等于中文符号**：自然语言通常使用“召回、词项、固定式”等概念词，而源码使用 `retrieve_machine` 一类英文标识符，单纯字面匹配容易断裂。
3. **精确符号召回不等于整体检索效果**：系统可能没有直接命中目标符号，却通过其所在文件、知识页或邻接关系取得足够上下文；反过来，命中许多包含通用词的文件也不代表找到了正确符号。

简短例子：查询“固定式脚本构建词项是否导致中文召回低”时，固定词项可能只提取出“固定”“构建”“中文”，于是召回多个通用模块，却遗漏真正负责检索的 `retrieve_machine`。因此，40% 更像是在暴露“中文概念到英文符号的映射不足”，而不是单纯说明 SQLite 或确定性检索机制本身无效。

检索档位：fast

所有说明使用简体中文；代码标识符保持源码形式。

## retrieve_machine

选择原因：保留显式标识符 `retrieve_machine`；确定性词项 `agent`

源码：[打开源码：scripts/ckb_core/machine_knowledge.py 第 942 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/machine_knowledge.py:942:1)  `scripts/ckb_core/machine_knowledge.py:942-1315`

## scripts/ckb_core/agent_index.py

选择原因：确定性词项 `agent`；确定性词项 `builder`

源码：[打开源码：scripts/ckb_core/agent_index.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_index.py:1:1)  `scripts/ckb_core/agent_index.py:1-569`

### 中文含义

该文件集中实

> 本节已按预算截断。

## scripts/ckb_core/navigation.py

选择原因：确定性词项 `builder`；确定性词项 `code`

源码：[打开源码：scripts/ckb_core/navigation.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/navigation.py:1:1)  `scripts/ckb_core/navigation.py:1-456`

### 中文含义

该文件集中实现页

> 本节已按预算截断。

## scripts/ckb_core/agent_maintenance.py

选择原因：确定性词项 `agent`；确定性词项 `builder`

源码：[打开源码：scripts/ckb_core/agent_maintenance.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_maintenance.py:1:1)  `scripts/ckb_core/agent_maintenance.py:1-255`

## scripts/ckb_core/graphify_core.py

选择原因：确定性词项 `builder`；确定性词项 `code`

源码：[打开源码：scripts/ckb_core/graphify_core.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/graphify_core.py:1:1)  `scripts/ckb_core/graphify_core.py:1-676`

## scripts/ckb_core/knowledge_layers.py

选择原因：确定性词项 `builder`；确定性词项 `code`

源码：[打开源码：scripts/ckb_core/knowledge_layers.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/knowledge_layers.py:1:1)  `scripts/ckb_core/knowledge_layers.py:1-239`

## scripts/ckb_core/parsers.py

选择原因：确定性词项 `builder`；确定性词项 `code`；确定性词项 `knowledge`；确定性词项 `关系`

源码：[打开源码：scripts/ckb_core/parsers.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/parsers.py:1:1)  `scripts/ckb_core/parsers.py:1-437`

人类知识页：[[parse_file 与 _language 的协作实现]]（page）

## scripts/ckb_core/page_config.py

选择原因：确定性词项 `builder`；确定性词项 `code`；确定性词项 `knowledge`；确定性词项 `固定`

源码：[打开源码：scripts/ckb_core/page_config.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/page_config.py:1:1)  `scripts/ckb_core/page_config.py:1-244`

人类知识页：[[load_page_config 与 _merge_known 的协作实现]]（page）

### 中文含义

该

> 本节已按预算截断。
