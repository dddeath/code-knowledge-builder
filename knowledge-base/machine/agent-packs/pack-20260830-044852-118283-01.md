# Agent 机器知识阅读包

问题：诊断 Codex 在 WSL 中运行时 UserPromptSubmit、PreToolUse、Stop Ladder Hook 退出码 127，导致对话记录未自动更新并疑似出现 Codex 会话记录丢失；定位 Hook 命令、WSL/Windows 路径、插件配置、spool 和 automation.sqlite。

检索档位：fast

所有说明使用简体中文；代码标识符保持源码形式。

## scripts/ckb_core/automation.py

选择原因：确定性词项 `automation`；确定性词项 `spool`；确定性词项 `sqlite`；确定性词项 `会话`

源码：[打开源码：scripts/ckb_core/automation.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation.py:1:1)  `scripts/ckb_core/automation.py:1-1632`

人类知识页：[[ingest_event 与 default_registry_path 的协作实现]]（page）

### 中文含义

该代码页汇总跨 Harness 会话与修改自动化，包括项目登记、session 级 Skill 激活、事件规范化、脱敏、spool、SQLite 和 Agent 审阅。

## scripts/ckb_core/automation_integrations.py

选择原因：确定性词项 `automation`；确定性词项 `codex`；确定性词项 `hook`；确定性词项 `和`

源码：[打开源码：scripts/ckb_core/automation_integrations.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation_integrations.py:1:1)  `scripts/ckb_core/automation_integrations.py:1-502`

人类知识页：[[render_integration 与 _looks_windows 的协作实现]]（page）

### 中文含义

该代码页汇总 Codex、Claude Code、OpenCode、DSH、Gemini、Copilot、Cursor 和 generic 的 Hook/Plugin 配置生成器。

## scripts/ckb.py

选择原因：确定性词项 `automation`；确定性词项 `hook`；确定性词项 `会话`；确定性词项 `出码`

源码：[打开源码：scripts/ckb.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb.py:1:1)  `scripts/ckb.py:1-495`

人类知识页：[[add_initial_arguments 与 add_git_bootstrap_arguments 的协作实现]]（page）

### 中文含义

该代码页汇总 Code Knowledge Builder 的命令行入口、参数路由和退出状态。

### 中文职责

它把构建、迁移、检索、工作区记录与自动化命令连接到确定性核心，并新增会话级 `automation activate` 路由。

### 什么时候需要修改

当命令、参数、退出码、会话激活入口或子命令调度变化时

> 本节已按预算截断。

## LspClient.stop

选择原因：确定性词项 `stop`；确定性词项 `诊断`；机器实体全文命中；文件、标识符和中文职责确定性匹配

源码：[打开源码：scripts/ckb_core/providers.py 第 351 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/providers.py:351:1)  `scripts/ckb_core/providers.py:351-363`

人类知识页：[[LspClient.start 与 _version_matches 的协作实现]]（appendix）

### 中文作用

该附属代码负责调用语言提供器并整理定义、符号与诊断证据，并把结果交给所属页面中的主流程使用。

### 来源核对

Agent 已重新打开固定 Git 源码中 scripts/ckb_core/providers.py 第 351 至 363 行，核对名称、签名和局部实现后写出本条中文说明。

## _codex_hooks

选择原因：确定性词项 `automation`；确定性词项 `codex`；确定性词项 `hook`；机器实体全文命中

源码：[打开源码：scripts/ckb_core/automation_integrations.py 第 62 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation_integrations.py:62:1)  `scripts/ckb_core/automation_integrations.py:62-90`

人类知识页：[[render_integration 与 _looks_windows 的协作实现]]（appendix）

### 中文作用

生成 Codex/DSH 生命周期 Hook，并在说明中固定显式 Skill 应用后的同步边界。

### 固定源码摘录

def _codex_hooks(posix: str, windows: str, *, dsh_subset: bool = False) -> dict[str, Any]:
    hooks: dict[str, Any] = {
        "SessionStart": [
            {

> 本节已按预算截断。

## _claude_hooks

选择原因：确定性词项 `automation`；确定性词项 `prompt`；确定性词项 `user`；机器实体全文命中

源码：[打开源码：scripts/ckb_core/automation_integrations.py 第 93 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation_integrations.py:93:1)  `scripts/ckb_core/automation_integrations.py:93-112`

人类知识页：[[render_integration 与 _looks_windows 的协作实现]]（appendix）

### 中文作用

生成 Claude Hooks，并增加 UserPromptExpansion 与 Skill 工具的原生激活监听。

## ingest_event

选择原因：确定性词项 `automation`；确定性词项 `hook`；确定性词项 `prompt`；确定性词项 `自动`

源码：[打开源码：scripts/ckb_core/automation.py 第 1321 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation.py:1321:1)  `scripts/ckb_core/automation.py:1321-1392`

人类知识页：[[ingest_event]]（page）

### 什么时候需要修改

当项目路由、激活判据、静默忽略语义、脱敏或 Hook 输出约定变化时，需要修改该函数并重跑正反向 canary。

### 中文含义

`ingest_event` 是所有 Harness Hook 进入 CKB 自动化核心的统一边界。

### 中文职责

它先匹配项目，再核对当前 session 的精确 Skill 激活；显式 prompt、原生 Skill 元数据或已登记激活通过后才脱敏、入队和写入数据库。

## automation_status

选择原因：确定性词项 `automation`；确定性词项 `spool`；确定性词项 `sqlite`；确定性词项 `会话`

源码：[打开源码：scripts/ckb_core/automation.py 第 1395 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation.py:1395:1)  `scripts/ckb_core/automation.py:1395-1422`

人类知识页：[[ingest_event 与 default_registry_path 的协作实现]]（appendix）
