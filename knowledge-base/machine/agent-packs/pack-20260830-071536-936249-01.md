# Agent 机器知识阅读包

问题：更新 Code Knowledge Builder 秋招面试官拷问手册：围绕当前简历描述涉及的固定 Git 提交、Tree-sitter、LSP、事实/机器/人类三层知识架构、确定性脚本与审计门、Agent 来源复核、SQLite FTS5、固定权重图传播、token 预算、Flask 3.1.3 Recall@8、检索上下文占用、P95 与 git grep 基线，形成基础概念解释、连续追问、证据边界和回答主干

检索档位：fast

所有说明使用简体中文；代码标识符保持源码形式。

## scripts/ckb_core/machine_knowledge.py

选择原因：确定性词项 `agent`；确定性词项 `knowledge`；确定性词项 `sqlite`；确定性词项 `上下`

源码：[打开源码：scripts/ckb_core/machine_knowledge.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/machine_knowledge.py:1:1)  `scripts/ckb_core/machine_knowledge.py:1-1458`

人类知识页：[[retrieve_machine 与 estimated_tokens 的协作实现]]（page）

### 中文含义

该文件实现完整 SQLite 机器知识库及纯确定性 Agent 检索。

### 中文职责

它负责构建实体、关系、来源、中文章节和 FTS 索引，并以固定候选上限、批量读取、图传播和静态缓存生成低上下文阅读包。

## scripts/ckb_core/migration.py

选择原因：确定性词项 `builder`；确定性词项 `code`；确定性词项 `git`；确定性词项 `knowledge`

源码：[打开源码：scripts/ckb_core/migration.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/migration.py:1:1)  `scripts/ckb_core/migration.py:1-585`

人类知识页：[[audit_migration 与 _entity_key 的协作实现]]（page）

### 中文含义

该代码页汇总 Code Knowledge Builder 的增量迁移实现，覆盖精确 blob 复用、审阅重建、可变知识保留和迁移审计。

### 中文职责

它把已经审计的旧知识库迁移到新的固定 Git 提交，并用不可变基线证明用户笔记与自动化数据库已被保留，同时允许 Hook 后续继续追加内容。

### 什么时候需要修改

当复用判据、可变层范围、重链接规则、

> 本节已按预算截断。

## scripts/ckb_core/agent_index.py

选择原因：确定性词项 `agent`；确定性词项 `builder`；确定性词项 `code`；确定性词项 `knowledge`

源码：[打开源码：scripts/ckb_core/agent_index.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_index.py:1:1)  `scripts/ckb_core/agent_index.py:1-569`

人类知识页：[[retrieve 与 _tokens 的协作实现]]（page）

### 中文职责

它是 Code Knowledge Builder 中承载旧版 SQLite 页面索引和检索接口兼容层的源码边界，并连接相邻模块或测试。

### 中文含义

该文件集中实现旧版 SQLite 页面索引和检索接口兼容层。

### 什么时候需要修改

当旧版 SQLite 页面索引和检索接口兼容层的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。

## scripts/ckb_core/parsers.py

选择原因：确定性词项 `builder`；确定性词项 `code`；确定性词项 `knowledge`；确定性词项 `sitter`

源码：[打开源码：scripts/ckb_core/parsers.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/parsers.py:1:1)  `scripts/ckb_core/parsers.py:1-437`

人类知识页：[[parse_file 与 _language 的协作实现]]（page）

### 什么时候需要修改

当Tree-sitter 实体提取、C# partial 合并和词法引用关系的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。

### 中文含义

该文件集中实现Tree-sitter 实体提取、C# partial 合并和词法引用关系。

### 中文职责

它是 Code Knowledge Builder 中承载Tree-sitter 实体提取、C# partial 合并和词法引用关系的源码边界，并连接相邻模块或测试。

## scripts/ckb_core/agent_maintenance.py

选择原因：确定性词项 `agent`；确定性词项 `builder`；确定性词项 `code`；确定性词项 `knowledge`

源码：[打开源码：scripts/ckb_core/agent_maintenance.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_maintenance.py:1:1)  `scripts/ckb_core/agent_maintenance.py:1-255`

人类知识页：[[start_session 与 _session_directory 的协作实现]]（page）

### 中文职责

它是 Code Knowledge Builder 中承载Agent 任务会话、构建中笔记排队、修改总结和生命周期状态的源码边界，并连接相邻模块或测试。

### 中文含义

该文件集中实现Agent 任务会话、构建中笔记排队、修改总结和生命周期状态。

### 什么时候需要修改

当Agent 任务会话、构建中笔记排队、修改总结和生命周期状态的输

> 本节已按预算截断。

## scripts/ckb_core/navigation.py

选择原因：确定性词项 `builder`；确定性词项 `code`；确定性词项 `knowledge`；确定性词项 `上下`

源码：[打开源码：scripts/ckb_core/navigation.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/navigation.py:1:1)  `scripts/ckb_core/navigation.py:1-456`

人类知识页：[[module_name 与 estimated_tokens 的协作实现]]（page）

### 中文含义

该文件集中实现页面配额、实体归属、关系预算和上下文预算的确定性决策。

### 中文职责

它是 Code Knowledge Builder 中承载页面配额、实体归属、关系预算和上下文预算的确定性决策的源码边界，并连接相邻模块或测试。

### 什么时候需要修改

当页面配额、实体归属、关系预算和上下文预算的确定性决策的输入、输出、状态门或验证

> 本节已按预算截断。

## scripts/ckb_core/knowledge_layers.py

选择原因：确定性词项 `builder`；确定性词项 `code`；确定性词项 `knowledge`；确定性词项 `事实`

源码：[打开源码：scripts/ckb_core/knowledge_layers.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/knowledge_layers.py:1:1)  `scripts/ckb_core/knowledge_layers.py:1-239`

人类知识页：[[sync_human_layer 与 _source_manifest 的协作实现]]（page）

### 中文含义

该文件集中实现可重建事实层、中文人类 vault 镜像与跨层一致性审计。

### 中文职责

它是 Code Knowledge Builder 中承载可重建事实层、中文人类 vault 镜像与跨层一致性审计的源码边界，并连接相邻模块或测试。

## scripts/ckb_core/automation.py

选择原因：确定性词项 `agent`；确定性词项 `builder`；确定性词项 `code`；确定性词项 `knowledge`

源码：[打开源码：scripts/ckb_core/automation.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation.py:1:1)  `scripts/ckb_core/automation.py:1-1632`

人类知识页：[[ingest_event 与 default_registry_path 的协作实现]]（page）

### 中文含义

该代码页汇总跨 Harness 会话与修改自动化，包括项目登记、session 级 Skill 激活、事件规范化、脱敏、spool、SQLite 和 Agent 审阅。

### 中文职责

它要求仓库登记与 `code-knowledge-builder` 精确激活同时成立；激活前事件静默忽略，激活后才记录会话、工具、修改与待审阅证据。

### 什么时候需要修改

当激活证据、注册表 Schema、Harness 事件映射、SQLite 表、幂等键、路径过滤或晋升规则变化时，需要修改本页。
