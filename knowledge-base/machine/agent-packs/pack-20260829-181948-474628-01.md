# Agent 机器知识阅读包

问题：明晰发行包边界：lite 保留基本代码知识库功能；full 只增加离线运行时且不包含 Obsidian 插件；Obsidian 插件作为独立发行类别、独立版本和独立 ZIP。定位现有 package、dist、toolchain lock、插件打包和文档入口。

检索档位：fast

所有说明使用简体中文；代码标识符保持源码形式。

## scripts/package_release.py

选择原因：确定性词项 `full`；确定性词项 `lite`；确定性词项 `package`；确定性词项 `zip`

源码：[打开源码：scripts/package_release.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/package_release.py:1:1)  `scripts/package_release.py:1-142`

人类知识页：[[source_files 与 sha256 的协作实现]]（page）

### 什么时候需要修改

版本号、载荷锁、发行文件规则或归档验证要求变化时，需要修改该文件。

### 来源核对

Agent 已从目标固定提交对应的 Git blob 重新打开 scripts/package_release.py 第 1 至 142 行，核对声明名称、签名、关键分支与调用后确认本条说明。

## scripts/ckb_core/obsidian.py

选择原因：确定性词项 `obsidian`；确定性词项 `保留`；机器实体全文命中；中文章节全文命中

源码：[打开源码：scripts/ckb_core/obsidian.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/obsidian.py:1:1)  `scripts/ckb_core/obsidian.py:1-134`

人类知识页：[[prepare_vault 与 install_obsidian 的协作实现]]（page）

### 中文职责

它让生成页面可重建，同时保留用户 workspace、笔记和未知文件。

### 来源核对

Agent 已重新打开目标 5.1 固定 Git 源码中 scripts/ckb_core/obsidian.py 第 1 至 134 行，核对名称、签名、分支和调用后确认本条说明。

## lock_document

选择原因：确定性词项 `lock`；确定性词项 `代码`；确定性词项 `离线`；确定性词项 `离线运`

源码：[打开源码：scripts/ckb_core/runtime.py 第 18 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/runtime.py:18:1)  `scripts/ckb_core/runtime.py:18-19`

人类知识页：[[remove 与 skill_root 的协作实现]]（appendix）

### 固定源码摘录

def lock_document() -> dict[str, Any]:
    return json_load(skill_root() / "toolchain.lock.json")

### 中文作用

该附属代码负责私有离线运行时部署、复用检查和移除，并把结果交给所属页面中的主流程使用。

## package_showcase

选择原因：确定性词项 `package`；确定性词项 `代码`；确定性词项 `发行`；机器实体全文命中

源码：[打开源码：scripts/ckb_core/showcase.py 第 66 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/showcase.py:66:1)  `scripts/ckb_core/showcase.py:66-172`

人类知识页：[[package_showcase]]（page）

### 中文含义

`package_showcase` 是源码中负责构建可复现发行归档并复核成员集合的命名代码单元。

## obsidian_open_uri

选择原因：确定性词项 `obsidian`；机器实体全文命中；中文章节全文命中；文件、标识符和中文职责确定性匹配

源码：[打开源码：scripts/ckb_core/source_links.py 第 168 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/source_links.py:168:1)  `scripts/ckb_core/source_links.py:168-169`

人类知识页：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]]（appendix）

### 中文作用

把本地 Markdown 路径编码为 Obsidian 打开链接。

### 固定源码摘录

def obsidian_open_uri(path: Path) -> str:
    return "obsidian://open?path=" + quote(str(path.resolve()), safe="")

### 来源核对

Agent 已从目标固定提交对应的 Git blob 重新打开 scripts/ckb_core/source_links.py 第 168 至 169 行，核对

> 本节已按预算截断。

## scripts/ckb_core/machine_knowledge.py

选择原因：确定性词项 `知识`；确定性词项 `知识库`；确定性词项 `识库`；机器实体全文命中

源码：[打开源码：scripts/ckb_core/machine_knowledge.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/machine_knowledge.py:1:1)  `scripts/ckb_core/machine_knowledge.py:1-1458`

人类知识页：[[retrieve_machine 与 estimated_tokens 的协作实现]]（page）

### 中文含义

该文件实现完整 SQLite 机器知识库及纯确定性 Agent 检索。

### 中文职责

它负责构建实体、关系、来源、中文章节和 FTS 索引，并以固定候选上限、批量读取、图传播和静态缓存生成低上下文阅读包。

### 什么时候需要修改

机器 Schema、检索召回规则、排序权重、

> 本节已按预算截断。

## scripts/build_runtime_payload.py

选择原因：确定性词项 `离线`；确定性词项 `离线运`；确定性词项 `线运`；确定性词项 `线运行`

源码：[打开源码：scripts/build_runtime_payload.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/build_runtime_payload.py:1:1)  `scripts/build_runtime_payload.py:1-117`

人类知识页：[[main 与 sha256 的协作实现]]（page）

### 中文含义

该文件集中实现离线运行时清单、可复现压缩和归档校验。

### 中文职责

它是 Code Knowledge Builder 中承载离线运行时清单、可复现压缩和归档校验的源码边界，并连接相邻模块或测试。

### 什么时候需要修改

当离线运行时清单、可复现压缩和归档校验的输入、输出、状态门或验证契约发生变化

> 本节已按预算截断。

## _drain_lock

选择原因：确定性词项 `lock`；机器实体全文命中；中文章节全文命中；文件、标识符和中文职责确定性匹配

源码：[打开源码：scripts/ckb_core/automation.py 第 760 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation.py:760:1)  `scripts/ckb_core/automation.py:760-787`

人类知识页：[[ingest_event 与 default_registry_path 的协作实现]]（appendix）
