# PackageReleaseTests

标签：#类型/代码

> `PackageReleaseTests` 是 `tests/test_package_release.py` 第 23-72 行定义的类，本页绑定该固定源码范围。 该类作为可执行验证入口，检查标识符 `PackageReleaseTests` 所指的行为与失败边界。

## 什么时候需要修改

当被测行为、输入夹具、断言或失败条件变化时，应同步更新 `PackageReleaseTests` 的说明。

## 在代码中的位置

[打开源码：tests/test_package_release.py 第 23 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_package_release.py:23:1)  `tests/test_package_release.py:23-72`

## 相关代码

- 实现时会用到 [[source_files]]。

## 谁会来到这里

- [[PackageReleaseTests 等测试场景]] 汇总了本页。
- [[source_files]] 关联到这里的验证场景。

## 内部细节

<details><summary>查看本页收纳的 3 个辅助实现</summary>

| 代码单元 | 一句话作用 |
|---|---|
| `PackageReleaseTests.test_core_packages_exclude_plugins_and_full_only_adds_runtime` | `PackageReleaseTests.t...` 是第 24-30 行的函数，供所属页面定位实现。 |
| `PackageReleaseTests.test_lite_manifest_retains_core_capabilities` | `PackageReleaseTests.t...` 是第 32-60 行的函数，供所属页面定位实现。 |
| `PackageReleaseTests.test_obsidian_plugin_is_independently_versioned` | `PackageReleaseTests.t...` 是第 62-72 行的函数，供所属页面定位实现。 |

</details>
