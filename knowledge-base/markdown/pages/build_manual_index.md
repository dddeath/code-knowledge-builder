# build_manual_index

标签：#类型/代码

> `build_manual_index` 位于 `tests/benchmark_chinese_retrieval.py` 第 111-154 行，本页用固定源码范围说明它如何验证目标行为、失败分类和回归边界。 `build_manual_index` 负责在冻结 benchmark 输入、观测和判定合同中验证目标行为、失败分类和回归边界。

## 什么时候需要修改

当 `tests/benchmark_chinese_retrieval.py` 中 `build_manual_index` 的输入合同、状态转换、输出字段或失败边界变化时，应更新本页并重跑对应测试。

## 在代码中的位置

[打开源码：tests/benchmark_chinese_retrieval.py 第 111 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/benchmark_chinese_retrieval.py:111:1)  `tests/benchmark_chinese_retrieval.py:111-154`

## 相关代码

- 实现时会用到 [[SourceLinkRenderer.uri]]。
- 实现时会用到 [[append]]。
- 实现时会用到 [[build_manual_index 等测试场景]]。
- 实现时会用到 [[execute]]。

## 谁会来到这里

- [[build_manual_index 等测试场景]] 汇总了本页。

## 相关测试

- [[build_manual_index 等测试场景]]
