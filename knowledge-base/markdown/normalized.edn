{:pages-and-blocks [
{:page {:block/title "AutomationTest.event"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`AutomationTest.event` 为测试构造统一的 Harness 事件夹具。 它默认附带精确 `applied_skills` 元数据，并允许测试显式关闭该字段以覆盖激活前静默分支。"} {:block/title "需要修改时：当测试事件公共字段或 Skill 激活证据结构变化时，需要修改该辅助函数。"} {:block/title "源码入口：[打开源码：tests/test_automation.py 第 80 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_automation.py:80:1)  `tests/test_automation.py:80-90`"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[main（generate_large_fixture 测试）]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.event 等测试场景]] 汇总了本页。"} {:block/title "协作：[[LspClient.start 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[MigrationTest]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "AutomationTest.event 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该测试页覆盖项目登记、session Skill 激活、跨 Harness 规范化、并发、脱敏、路径过滤、待审阅与人类晋升。 它同时验证普通提及保持零写入、显式应用开启记录、其他 session 隔离、CLI 激活幂等以及 schema 1/2 升级。"} {:block/title "需要修改时：当自动化 Schema、激活协议、适配器事件或审计门变化时，需要同步扩展该测试。"} {:block/title "源码入口：[打开源码：tests/test_automation.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_automation.py:1:1)  `tests/test_automation.py:1-801`"} {:block/title "内部实现：git — 执行测试仓库 Git 命令并在失败时保留 stderr。"} {:block/title "内部实现：AutomationTest — `AutomationTest` 汇集同一功能域的测试夹具、执行步骤和验收断言。"} {:block/title "内部实现：AutomationTest.setUp — 建立带 Git 源码、知识输出和注册表的自动化测试夹具。"} {:block/title "内部实现：AutomationTest.tearDown — 释放自动化测试创建的临时目录。"} {:block/title "内部实现：AutomationTest.register — `AutomationTest.register` 根据固定源码完成所属自动化或测试流程中的局部处理。"} {:block/title "内部实现：AutomationTest.test_project_opt_in_and_hook_output — 该测试验证对应功能的状态转换、输出和失败门符合固定契约。"} {:block/title "内部实现：AutomationTest.test_registered_session_stays_idle_until_skill_is_explicitly_applied — 验证已登记 session 在普通提及时零写入，并在精确调用后只激活当前 session。"} {:block/title "内部实现：AutomationTest.test_agent_activation_command_uses_harness_session_and_workspace — 验证 Agent 激活命令按 Harness session 与工作区匹配项目，且重复激活保持幂等。"} {:block/title "内部实现：AutomationTest.test_redaction_idempotency_change_capture_and_pending_review — 该测试验证敏感字段脱敏、幂等写入和待审阅生成符合固定契约。"} {:block/title "内部实现：AutomationTest.test_claude_without_turn_ids_allows_repeated_prompt_after_completion — 该测试验证对应功能的状态转换、输出和失败门符合固定契约。"} {:block/title "内部实现：AutomationTest.test_stop_detects_further_change_to_file_dirty_at_session_start — 该测试验证对应功能的状态转换、输出和失败门符合固定契约。"} {:block/title "内部实现：AutomationTest.test_nested_untracked_project_bounds_git_status_and_uses_project_relative_paths — 该测试验证Git 来源、初始化选择和工作树漂移门符合固定契约。"} {:block/title "内部实现：AutomationTest.test_workspace_root_maps_parent_task_to_nested_repository — 该测试验证workspace 根到源码仓库的路由与路径过滤符合固定契约。"} {:block/title "内部实现：AutomationTest.test_version_one_registry_is_read_compatibly — 该测试验证对应功能的状态转换、输出和失败门符合固定契约。"} {:block/title "内部实现：AutomationTest.test_version_two_registry_upgrades_to_session_activation_contract — 验证 schema 2 注册表读取后立即遵守 session Skill 激活门，并可通过规范激活事件继续工作。"} {:block/title "内部实现：AutomationTest.test_opencode_and_generic_normalization — 验证 OpenCode、generic 与 Claude Skill 事件可确定性规范化为统一事件和精确激活证据。"} {:block/title "内部实现：AutomationTest.test_gemini_copilot_and_cursor_normalization — 该测试验证Gemini、Copilot 和 Cursor 的事件规范化符合固定契约。"} {:block/title "内部实现：AutomationTest.test_concurrent_hooks_are_serialized_without_event_loss — 该测试验证并发 Hook 的串行导入与事件无损符合固定契约。"} {:block/title "内部实现：AutomationTest.test_failed_spool_is_retained_and_retryable — 该测试验证失败 spool 的保留和显式重试符合固定契约。"} {:block/title "内部实现：AutomationTest._prepare_human_projection — 建立自动化晋升测试所需的最小人类投影。"} {:block/title "内部实现：AutomationTest.test_agent_review_promotes_one_chinese_human_note — 该测试验证Agent 审阅集合、中文说明和来源检查符合固定契约。"} {:block/title "内部实现：AutomationTest.test_render_all_harness_integrations — 验证九种 Harness 适配包、激活清单字段、Claude Skill Hook 和 OpenCode V2 结构。"} {:block/title "内部实现：AutomationTest.test_automation_fts_finds_pending_machine_record — 该测试验证自动化机器记录的 FTS 检索符合固定契约。"} {:block/title "内部实现：AutomationTest.test_machine_retrieval_and_changes_include_pending_automation — 该测试验证对应功能的状态转换、输出和失败门符合固定契约。"} {:block/title "协作：主要代码单元是 [[AutomationTest.event]]。"} {:block/title "协作：实现时会用到 [[LspClient.start]]。"} {:block/title "协作：实现时会用到 [[LspClient.start 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[ingest_event]]。"} {:block/title "协作：实现时会用到 [[ingest_event 与 default_registry_path 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[run]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[main（generate_large_fixture 测试）]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.event]] 关联到这里的验证场景。"} {:block/title "协作：[[LspClient.start]] 关联到这里的验证场景。"} {:block/title "协作：[[LspClient.start 与 _version_matches 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[add_initial_arguments 与 add_git_bootstrap_arguments 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[create_source_snapshot]] 关联到这里的验证场景。"} {:block/title "协作：[[create_source_snapshot 与 git 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[execute]] 关联到这里的验证场景。"} {:block/title "协作：[[ingest_event]] 关联到这里的验证场景。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[render_integration]] 关联到这里的验证场景。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[run]] 关联到这里的验证场景。"} {:block/title "协作：[[run 与 CkbError 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[status]] 关联到这里的验证场景。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "CodeKnowledgeBuilderTests"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`CodeKnowledgeBuilderTests` 汇集 code-knowledge-builder 的端到端验收场景。 它统一管理临时 Git 夹具，验证初始化、范围、审阅、投影、运行时、C#、检索缓存和失败门。"} {:block/title "需要修改时：构建流水线验收条件、测试夹具结构或新增回归风险时，需要修改该测试类。"} {:block/title "源码入口：[打开源码：tests/test_ckb.py 第 171 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_ckb.py:171:1)  `tests/test_ckb.py:171-1147`"} {:block/title "内部实现：CodeKnowledgeBuilderTests.setUp — 为每个用例创建隔离临时目录和已提交的多语言仓库。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.tearDown — 在用例结束后清理隔离测试目录。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_non_git_path_reminds_then_opt_in_creates_one_initial_commit — 验证非 Git 目录先提示，再仅在显式选择后创建一次初始提交。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_unborn_git_repo_requires_opt_in_and_existing_dirty_repo_is_not_committed — 验证无提交仓库需要显式初始化，且已有脏仓库不会被自动提交。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_fast_run_can_bootstrap_non_git_source_and_stops_for_review — 验证快速入口可初始化非 Git 源码，并在首个 Agent 审阅门停止。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_markdown_whole_repository_and_completion_gate — 端到端验证 Markdown 构建、机器检索、缓存命中、源码链接与三项完成门。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_local_scope_has_one_hop_boundary — 验证局部路径扫描会为范围外关系生成一跳边界。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_entry_scope_uses_fixed_snapshot_while_live_worktree_changes — 验证入口扫描使用固定快照且允许后续工作树修改独立记录。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_review_set_mismatch_fails — 验证审阅实体集合与计划不一致时审计门失败。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_runtime_plan_lite — 验证 lite 运行时计划准确报告待部署依赖而不直接部署。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_required_format_duplicate_entry_and_syntax_stage — 验证必填格式、重名入口候选和单阶段语法构建契约。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_oversized_file_splits_on_declarations_without_duplicate_ids — 验证超大文件按声明边界分段且实体 ID 不重复。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_both_projection_parity_with_cli_contract_double — 验证 Markdown 与 Logseq DB 双投影具有同一逻辑页面和关系集合。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_logseq_only_projection_has_format_neutral_agent_index — 验证仅 Logseq DB 模式仍生成与格式无关的 Agent 索引。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_english_only_agent_review_is_rejected — 验证只有英文的 Agent 说明不能通过中文叙述门。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_navigation_page_quota_relation_budget_and_context_bundle — 验证页面配额、关系预算和上下文包均由确定性配置约束。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_page_configuration_controls_quotas_content_and_is_pinned — 验证页面内容与数量配置会被规范化、固定并影响最终投影。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_csharp_project_selection_partial_types_and_generated_exclusions — 验证 C# 项目选择、partial 类型合并和生成目录排除。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_csharp_property_and_enum_land_in_class_or_file_aggregation — 验证 C# 属性和枚举归入类页或文件聚合附录。"} {:block/title "内部实现：CodeKnowledgeBuilderTests.test_fallback_standard_derivation_and_stable_ids — 验证 C/C++ fallback 标准推导与稳定实体 ID。"} {:block/title "协作：实现时会用到 [[CodeKnowledgeBuilderTests 等测试场景]]。"} {:block/title "协作：实现时会用到 [[LspClient.start]]。"} {:block/title "协作：实现时会用到 [[audit_migration]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：实现时会用到 [[status 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：[[CodeKnowledgeBuilderTests 等测试场景]] 汇总了本页。"} {:block/title "协作：[[LspClient.start]] 关联到这里的验证场景。"} {:block/title "协作：[[LspClient.start 与 _version_matches 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[create_source_snapshot]] 关联到这里的验证场景。"} {:block/title "协作：[[create_source_snapshot 与 git 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[execute]] 关联到这里的验证场景。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[prepare_vault]] 关联到这里的验证场景。"} {:block/title "协作：[[prepare_vault 与 install_obsidian 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[run]] 关联到这里的验证场景。"} {:block/title "协作：[[run 与 CkbError 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[source_files]] 关联到这里的验证场景。"} {:block/title "协作：[[status]] 关联到这里的验证场景。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 关联到这里的验证场景。"}]}
{:page {:block/title "CodeKnowledgeBuilderTests 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件包含主构建、局部扫描、投影、配置、C#、机器检索和完成门的端到端回归测试。 它用隔离多语言 Git 夹具验证成功路径、失败门、检索确定性、静态缓存命中和跨格式一致性。"} {:block/title "需要修改时：主 CLI 契约、知识库 Schema、检索统计、页面配置或语言支持变化时，需要修改该文件。"} {:block/title "源码入口：[打开源码：tests/test_ckb.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_ckb.py:1:1)  `tests/test_ckb.py:1-1152`"} {:block/title "内部实现：invoke — 在固定测试环境中调用 ckb.py 并捕获 UTF-8 输出和退出状态。"} {:block/title "内部实现：write — 把测试夹具文本规范化为带结尾换行的 UTF-8 文件。"} {:block/title "内部实现：git — 在指定测试仓库中执行 Git 命令并把失败转换为测试异常。"} {:block/title "内部实现：make_repo — 创建含 Python、JavaScript、C、C++ 与排除目录的已提交测试仓库。"} {:block/title "内部实现：review_all — 为测试输出构建全部分段并提交逐实体中文审阅。"} {:block/title "协作：主要代码单元是 [[CodeKnowledgeBuilderTests]]。"} {:block/title "协作：实现时会用到 [[LspClient.start]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[run]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests]] 会使用这里提供的行为。"} {:block/title "协作：[[LspClient.start]] 关联到这里的验证场景。"} {:block/title "协作：[[LspClient.start 与 _version_matches 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[create_source_snapshot]] 关联到这里的验证场景。"} {:block/title "协作：[[create_source_snapshot 与 git 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[execute]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[run]] 关联到这里的验证场景。"} {:block/title "协作：[[run 与 CkbError 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[status]] 关联到这里的验证场景。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "LspClient.start"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`LspClient.start` 是源码中负责调用语言提供器并整理定义、符号与诊断证据的命名代码单元。 它在所属模块内执行调用语言提供器并整理定义、符号与诊断证据，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当调用语言提供器并整理定义、符号与诊断证据所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/providers.py 第 270 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/providers.py:270:1)  `scripts/ckb_core/providers.py:270-280`"} {:block/title "协作：实现时会用到 [[LspClient.start 与 _version_matches 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.event 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests]] 会使用这里提供的行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[LspClient.start 与 _version_matches 的协作实现]] 汇总了本页。"} {:block/title "协作：[[create_source_snapshot]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session]] 会使用这里提供的行为。"}]}
{:page {:block/title "LspClient.start 与 _version_matches 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现Pyright、TypeScript、clangd 与 csharp-ls 的真实 LSP 证据采集。 它是 Code Knowledge Builder 中承载Pyright、TypeScript、clangd 与 csharp-ls 的真实 LSP 证据采集的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当Pyright、TypeScript、clangd 与 csharp-ls 的真实 LSP 证据采集的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/providers.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/providers.py:1:1)  `scripts/ckb_core/providers.py:1-596`"} {:block/title "内部实现：_version_matches — 该附属代码负责Pyright、TypeScript、clangd 与 csharp-ls 的真实 LSP 证据采集，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：private_runtime_root — 该附属代码负责管理隔离离线运行时及其回滚，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_runtime_bin_candidates — 该附属代码负责管理隔离离线运行时及其回滚，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：resolve_executable — 该附属代码负责Pyright、TypeScript、clangd 与 csharp-ls 的真实 LSP 证据采集，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：doctor_report — 该附属代码负责检查依赖版本和本地运行条件，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：path_to_uri — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：uri_to_path — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：LspClient — 该附属代码负责调用语言提供器并整理定义、符号与诊断证据，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：LspClient.__init__ — 该附属代码负责固定仓库来源并规划扫描、页面与审阅批次，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：LspClient._read_stdout — 该附属代码负责调用语言提供器并整理定义、符号与诊断证据，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：LspClient._read_stderr — 该附属代码负责调用语言提供器并整理定义、符号与诊断证据，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：LspClient.send — 该附属代码负责调用语言提供器并整理定义、符号与诊断证据，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：LspClient.notify — 该附属代码负责调用语言提供器并整理定义、符号与诊断证据，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：LspClient.request — 该附属代码负责调用语言提供器并整理定义、符号与诊断证据，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：LspClient.stop — 该附属代码负责调用语言提供器并整理定义、符号与诊断证据，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_provider_spec — 该附属代码负责调用语言提供器并整理定义、符号与诊断证据，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_fallback_flags — 该附属代码负责Pyright、TypeScript、clangd 与 csharp-ls 的真实 LSP 证据采集，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_flatten_symbols — 该附属代码负责Pyright、TypeScript、clangd 与 csharp-ls 的真实 LSP 证据采集，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：collect_semantics — 该附属代码负责调用语言提供器并整理定义、符号与诊断证据，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：实现时会用到 [[AutomationTest.event]]。"} {:block/title "协作：主要代码单元是 [[LspClient.start]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[query_graph 与 _networkx_modules 的协作实现]]。"} {:block/title "协作：实现时会用到 [[render_integration 与 _looks_windows 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[run]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：实现时会用到 [[status 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.event 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[LspClient.start]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[main（fake_logseq 测试）]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file]] 会使用这里提供的行为。"} {:block/title "协作：[[remove]] 会使用这里提供的行为。"} {:block/title "协作：[[remove 与 skill_root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "MigrationTest"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`MigrationTest` 构造连续两个源码提交和迁移前后知识库。 它验证增量复用、delta 审阅、目录提升后的固定快照重定位、可变数据续写和基线防篡改。"} {:block/title "需要修改时：迁移计划、切换方式、审阅字段或可变层规则变化时，需要修改该测试类。"} {:block/title "源码入口：[打开源码：tests/test_migration.py 第 57 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_migration.py:57:1)  `tests/test_migration.py:57-189`"} {:block/title "内部实现：MigrationTest.setUp — 创建临时 Python 仓库、首个 Git 提交和确定性测试提供器。"} {:block/title "内部实现：MigrationTest.tearDown — 恢复测试环境变量并清理临时迁移夹具。"} {:block/title "内部实现：MigrationTest.test_exact_blob_facts_and_agent_reviews_are_reused — 验证精确 blob 与中文审阅复用、可变层基线、目录提升后的路径重定位及篡改失败门。"} {:block/title "协作：实现时会用到 [[AutomationTest.event]]。"} {:block/title "协作：实现时会用到 [[audit_migration]]。"} {:block/title "协作：实现时会用到 [[audit_migration 与 _entity_key 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：实现时会用到 [[status 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：[[AutomationTest.event]] 关联到这里的验证场景。"} {:block/title "协作：[[AutomationTest.event 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[LspClient.start]] 关联到这里的验证场景。"} {:block/title "协作：[[LspClient.start 与 _version_matches 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[MigrationTest 等测试场景]] 汇总了本页。"} {:block/title "协作：[[SourceLinkRenderer.uri]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[add_git_bootstrap_arguments]] 关联到这里的验证场景。"} {:block/title "协作：[[add_initial_arguments]] 关联到这里的验证场景。"} {:block/title "协作：[[add_initial_arguments 与 add_git_bootstrap_arguments 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[create_source_snapshot]] 关联到这里的验证场景。"} {:block/title "协作：[[create_source_snapshot 与 git 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[execute]] 关联到这里的验证场景。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[parser]] 关联到这里的验证场景。"} {:block/title "协作：[[prepare_vault]] 关联到这里的验证场景。"} {:block/title "协作：[[prepare_vault 与 install_obsidian 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[remove]] 关联到这里的验证场景。"} {:block/title "协作：[[remove 与 skill_root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[run]] 关联到这里的验证场景。"} {:block/title "协作：[[run 与 CkbError 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[status]] 关联到这里的验证场景。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 关联到这里的验证场景。"}]}
{:page {:block/title "MigrationTest 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件验证知识库在新 Git 提交上的精确增量迁移和完成态目录提升。 它覆盖 blob 与中文审阅复用、可变笔记、Hook 数据、不可变基线、Wiki 重链接、输出路径重定位及篡改失败门。"} {:block/title "需要修改时：迁移复用、目录切换、可变层或审计契约变化时，需要修改该文件。"} {:block/title "源码入口：[打开源码：tests/test_migration.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/test_migration.py:1:1)  `tests/test_migration.py:1-194`"} {:block/title "内部实现：git — 在临时夹具仓库执行 Git 命令并把非零退出转为测试错误。"} {:block/title "内部实现：review_all — 逐包填充固定中文审阅字段并通过普通审阅门。"} {:block/title "协作：主要代码单元是 [[MigrationTest]]。"} {:block/title "协作：实现时会用到 [[run]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：实现时会用到 [[status 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：[[AutomationTest.event]] 关联到这里的验证场景。"} {:block/title "协作：[[LspClient.start]] 关联到这里的验证场景。"} {:block/title "协作：[[LspClient.start 与 _version_matches 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[create_source_snapshot]] 关联到这里的验证场景。"} {:block/title "协作：[[create_source_snapshot 与 git 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[execute]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[run]] 关联到这里的验证场景。"} {:block/title "协作：[[run 与 CkbError 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[status]] 关联到这里的验证场景。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "SourceLinkRenderer.uri"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`SourceLinkRenderer.uri` 将仓库内源码位置编码为编辑器可打开的 URI。 它复用已校验绝对路径，并按 VS Code、Insiders、file 或自定义模板生成确定性链接。"} {:block/title "需要修改时：新增编辑器、调整 URI 编码或改变自定义模板字段时，需要修改该函数。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/source_links.py 第 60 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/source_links.py:60:1)  `scripts/ckb_core/source_links.py:60-75`"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]]。"} {:block/title "协作：实现时会用到 [[render_integration 与 _looks_windows 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[LspClient.start 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 汇总了本页。"} {:block/title "协作：[[audit_migration]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件生成指向本地源码精确位置和 Obsidian 笔记的可点击链接。 它集中验证打开器配置、限制仓库路径边界，并让批量检索复用同一渲染器与路径缓存。"} {:block/title "需要修改时：支持的编辑器、URI 格式、路径边界或缓存行为变化时，需要修改该文件。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/source_links.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/source_links.py:1:1)  `scripts/ckb_core/source_links.py:1-184`"} {:block/title "内部实现：SourceLinkRenderer — 复用已验证配置和路径缓存，为多个实体生成可点击的本地源码链接。"} {:block/title "内部实现：SourceLinkRenderer.__init__ — 一次验证源码打开器配置并初始化仓库根目录与路径缓存。"} {:block/title "内部实现：SourceLinkRenderer.cache_size — 报告当前已缓存的源码相对路径数量。"} {:block/title "内部实现：SourceLinkRenderer.absolute_path — 校验相对路径边界，并按需缓存对应的仓库内绝对路径。"} {:block/title "内部实现：SourceLinkRenderer.markdown_link — 把源码 URI、可读路径和行范围组合为 Markdown 链接。"} {:block/title "内部实现：default_openers — 根据仓库和可选快照目录生成默认源码打开器配置。"} {:block/title "内部实现：ensure_local_openers — 读取已有打开器配置，或在缺失时写入并返回默认配置。"} {:block/title "内部实现：validate_local_openers — 校验编辑器类型、源码视图、模板和仓库根目录字段。"} {:block/title "内部实现：update_local_openers — 合并用户指定的编辑器选项并重新验证后保存配置。"} {:block/title "内部实现：source_absolute_path — 通过一次性渲染器把仓库相对路径转换为受边界约束的绝对路径。"} {:block/title "内部实现：source_uri — 通过一次性渲染器生成指定源码行列的编辑器 URI。"} {:block/title "内部实现：source_markdown_link — 通过一次性渲染器生成带可读行范围的源码 Markdown 链接。"} {:block/title "内部实现：obsidian_open_uri — 把本地 Markdown 路径编码为 Obsidian 打开链接。"} {:block/title "内部实现：audit_source_uri — 解析并核对源码 URI 是否准确指向配置根目录内的目标行。"} {:block/title "协作：主要代码单元是 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[render_integration 与 _looks_windows 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[main（generate_large_fixture 测试）]] 覆盖相关行为。"} {:block/title "协作：[[SourceLinkRenderer.uri]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "__init__ 实现概览"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件声明 code-knowledge-builder 核心包的 Schema 与发行版本。 它为 CLI、迁移、审计和打包流程提供统一版本事实。"} {:block/title "需要修改时：发布版本或机器 Schema 发生兼容性变化时，需要修改该文件。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/__init__.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/__init__.py:1:1)  `scripts/ckb_core/__init__.py:1-5`"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "add_git_bootstrap_arguments"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`add_git_bootstrap_arguments` 登记可选 Git 初始化、首次提交信息和作者参数。 它把非 Git 目录的显式初始化能力限制在首次构建入口，避免隐式改动仓库。"} {:block/title "需要修改时：Git 启动授权、提交元数据或参数名称变化时，需要修改该函数。"} {:block/title "源码入口：[打开源码：scripts/ckb.py 第 104 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb.py:104:1)  `scripts/ckb.py:104-112`"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：实现时会用到 [[status 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[main（generate_large_fixture 测试）]] 覆盖相关行为。"} {:block/title "协作：[[add_initial_arguments]] 会使用这里提供的行为。"} {:block/title "协作：[[add_initial_arguments 与 add_git_bootstrap_arguments 的协作实现]] 汇总了本页。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"}]}
{:page {:block/title "add_initial_arguments"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`add_initial_arguments` 为首次构建命令登记仓库、输出、范围、格式和语言选项。 它复用统一参数定义，保证 `init` 等入口采用一致的固定快照输入契约。"} {:block/title "需要修改时：初始化参数、局部扫描选项或 Git 启动方式变化时，需要修改该函数。"} {:block/title "源码入口：[打开源码：scripts/ckb.py 第 80 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb.py:80:1)  `scripts/ckb.py:80-95`"} {:block/title "协作：实现时会用到 [[add_git_bootstrap_arguments]]。"} {:block/title "协作：实现时会用到 [[add_initial_arguments 与 add_git_bootstrap_arguments 的协作实现]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[main（generate_large_fixture 测试）]] 覆盖相关行为。"} {:block/title "协作：[[add_initial_arguments 与 add_git_bootstrap_arguments 的协作实现]] 汇总了本页。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"}]}
{:page {:block/title "add_initial_arguments 与 add_git_bootstrap_arguments 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该代码页汇总 Code Knowledge Builder 的命令行入口、参数路由和退出状态。 它把构建、迁移、检索、工作区记录与自动化命令连接到确定性核心，并新增会话级 `automation activate` 路由。"} {:block/title "需要修改时：当命令、参数、退出码、会话激活入口或子命令调度变化时，需要修改本页并重跑 CLI 与 Hook canary。"} {:block/title "源码入口：[打开源码：scripts/ckb.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb.py:1:1)  `scripts/ckb.py:1-495`"} {:block/title "内部实现：add_csharp_arguments — 该附属代码负责计算机器交付物的完整性摘要，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：emit — 该附属代码负责命令行参数、子命令路由、统一退出码和顶层异常边界，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：主要代码单元是 [[add_git_bootstrap_arguments]]。"} {:block/title "协作：主要代码单元是 [[add_initial_arguments]]。"} {:block/title "协作：主要代码单元是 [[main（ckb 实现）]]。"} {:block/title "协作：主要代码单元是 [[parser]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[main（generate_large_fixture 测试）]] 覆盖相关行为。"} {:block/title "协作：[[add_initial_arguments]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "audit_migration"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`audit_migration` 对增量迁移计划、复用集合、可变层基线、审阅状态和目标图来源执行确定性复核。 它区分不可变的迁移基线与可继续写入的工作副本，验证 Markdown/JSON 可读性和 SQLite 完整性，并在全部门通过后提升迁移状态。"} {:block/title "需要修改时：当迁移计划 Schema、允许的可变文件类型、完整性规则或完成状态语义变化时，需要修改该函数并增加失败注入测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/migration.py 第 353 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/migration.py:353:1)  `scripts/ckb_core/migration.py:353-460`"} {:block/title "内部实现：audit_migration.check — 把单项迁移门的名称、真假结果和证据追加到审计清单。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[audit_migration 与 _entity_key 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute]] 覆盖相关行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests]] 会使用这里提供的行为。"} {:block/title "协作：[[MigrationTest]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 汇总了本页。"} {:block/title "协作：[[create_source_snapshot 与 git 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[main（generate_large_fixture 测试）]] 会使用这里提供的行为。"} {:block/title "协作：[[run]] 会使用这里提供的行为。"}]}
{:page {:block/title "audit_migration 与 _entity_key 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该代码页汇总 Code Knowledge Builder 的增量迁移实现，覆盖精确 blob 复用、审阅重建、可变知识保留和迁移审计。 它把已经审计的旧知识库迁移到新的固定 Git 提交，并用不可变基线证明用户笔记与自动化数据库已被保留，同时允许 Hook 后续继续追加内容。"} {:block/title "需要修改时：当复用判据、可变层范围、重链接规则、迁移状态机或审计门发生变化时，需要修改本页并重跑迁移回归测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/migration.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/migration.py:1:1)  `scripts/ckb_core/migration.py:1-585`"} {:block/title "内部实现：_entity_key — 提取实体的路径、blob、类型、名称和源码范围，形成可比较的迁移复用键。"} {:block/title "内部实现：_review_shape — 把实体分类归并为附录说明或完整叙述两种审阅字段形状。"} {:block/title "内部实现：_review_for_new_entity — 把旧实体中已经核实的中文审阅字段改写到目标提交的新实体标识上。"} {:block/title "内部实现：_copy_file — 复制可变文件并记录初始位置、大小和完整性证据。"} {:block/title "内部实现：_mutable_target — 把迁移清单中的相对路径解析到输出目录内并阻止路径越界。"} {:block/title "内部实现：_add_mutable_baseline — 为迁移时保留的工作副本创建不可变基线并登记两者的初始证据。"} {:block/title "内部实现：_generated_paths — 读取投影所有权清单，识别可重建文件与用户维护文件的边界。"} {:block/title "内部实现：_preserve_mutable_layers — 复制用户笔记、工作区记录和自动化数据库，同时为每项生成迁移基线。"} {:block/title "内部实现：_selected_entities — 按照范围清单汇集目标实体，并从固定 Git 提交批量读取对应源码。"} {:block/title "内部实现：_replace_review_packs — 把实体拆分为可复用审阅包和需要重新核实的差量审阅包。"} {:block/title "内部实现：migrate_output — 编排目标初始化、精确文件复用、可变知识保存、分段构建和差量审阅检查点。"} {:block/title "内部实现：_semantic_page_key — 用源码路径、实体类型和限定名建立跨提交页面标题匹配键。"} {:block/title "内部实现：relink_preserved_notes — 根据新旧页面的语义匹配确定性重写保留笔记中的 Wiki 链接和本地文件位置。"} {:block/title "内部实现：relink_preserved_notes.replace_links — 按标题长度排序替换普通 Wiki 链接与带别名的 Wiki 链接。"} {:block/title "内部实现：migration_status — 汇总迁移复用统计、待审阅包、下一模板和当前迁移审计结果。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：主要代码单元是 [[audit_migration]]。"} {:block/title "协作：实现时会用到 [[create_source_snapshot 与 git 的协作实现]]。"} {:block/title "协作：实现时会用到 [[ingest_event 与 default_registry_path 的协作实现]]。"} {:block/title "协作：实现时会用到 [[module_name 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[render_integration 与 _looks_windows 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：实现时会用到 [[status 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[MigrationTest]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "create_source_snapshot"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`create_source_snapshot` 为固定 commit 创建独立 detached worktree 并验证其干净状态。 它让语言提供器始终读取不可变基线，同时允许 Agent 在实时工作树继续修改代码。"} {:block/title "需要修改时：快照布局、Git worktree 管理或漂移检查方式变化时，需要修改该函数。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/gitrepo.py 第 230 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/gitrepo.py:230:1)  `scripts/ckb_core/gitrepo.py:230-257`"} {:block/title "协作：实现时会用到 [[LspClient.start]]。"} {:block/title "协作：实现时会用到 [[create_source_snapshot 与 git 的协作实现]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[run]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[create_source_snapshot 与 git 的协作实现]] 汇总了本页。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "create_source_snapshot 与 git 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该页面汇总 Git 预检、固定快照、来源对象读取和扫描范围发现。 它把干净 commit、blob 和仓库内路径设为知识图谱的真实性边界，并排除依赖目录。"} {:block/title "需要修改时：Git 边界、默认排除项、快照方式或来源读取协议变化时，需要修改本文件。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/gitrepo.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/gitrepo.py:1:1)  `scripts/ckb_core/gitrepo.py:1-417`"} {:block/title "内部实现：git — 该附属代码负责Git 预检、固定源码快照、tracked source 范围和 blob 读取，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_git_probe — 该附属代码负责Git 预检、固定源码快照、tracked source 范围和 blob 读取，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_identity_value — 该附属代码负责Git 预检、固定源码快照、tracked source 范围和 blob 读取，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：prepare_git_repository — 该附属代码负责Git 预检、固定源码快照、tracked source 范围和 blob 读取，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：preflight — 该附属代码负责Git 预检、固定源码快照、tracked source 范围和 blob 读取，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：assert_unchanged — 该附属代码负责Git 预检、固定源码快照、tracked source 范围和 blob 读取，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：assert_source_snapshot — 该附属代码负责建立并验证与固定提交一致的源码快照，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：tracked_sources — 该附属代码负责解析 tracked source 范围、显式路径和边界实体，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：tracked_csharp_project_files — 该附属代码负责计算机器交付物的完整性摘要，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：blob_bytes — 该附属代码负责Git 预检、固定源码快照、tracked source 范围和 blob 读取，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：blob_bytes_many — 该附属代码负责Git 预检、固定源码快照、tracked source 范围和 blob 读取，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：object_exists — 该附属代码负责Git 预检、固定源码快照、tracked source 范围和 blob 读取，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：resolve_scope_paths — 该附属代码负责解析 tracked source 范围、显式路径和边界实体，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：实现时会用到 [[audit_migration]]。"} {:block/title "协作：主要代码单元是 [[create_source_snapshot]]。"} {:block/title "协作：实现时会用到 [[remove]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[run]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：实现时会用到 [[status 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[create_source_snapshot]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "execute"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`execute` 是源码中负责真实语义提供器精确、近似和失败路径集成测试的命名代码单元。 它在所属模块内执行真实语义提供器精确、近似和失败路径集成测试，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当真实语义提供器精确、近似和失败路径集成测试所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：tests/provider_integration.py 第 19 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/provider_integration.py:19:1)  `tests/provider_integration.py:19-24`"} {:block/title "协作：实现时会用到 [[run]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.event 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[LspClient.start 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[MigrationTest]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration]] 关联到这里的验证场景。"} {:block/title "协作：[[execute 等测试场景]] 汇总了本页。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（fake_logseq 测试）]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[run]] 关联到这里的验证场景。"} {:block/title "协作：[[run 与 CkbError 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "execute 等测试场景"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现真实语义提供器精确、近似和失败路径集成测试。 它是 Code Knowledge Builder 中承载真实语义提供器精确、近似和失败路径集成测试的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当真实语义提供器精确、近似和失败路径集成测试的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：tests/provider_integration.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/provider_integration.py:1:1)  `tests/provider_integration.py:1-252`"} {:block/title "内部实现：git — 该附属代码负责真实语义提供器精确、近似和失败路径集成测试，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：provider_case — 该附属代码负责调用语言提供器并整理定义、符号与诊断证据，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：clangd_case — 该附属代码负责真实语义提供器精确、近似和失败路径集成测试，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：csharp_case — 该附属代码负责计算机器交付物的完整性摘要，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：main — 该附属代码负责接收命令参数并把请求路由到对应实现，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：主要代码单元是 [[execute]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：[[AutomationTest.event]] 关联到这里的验证场景。"} {:block/title "协作：[[AutomationTest.event 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[LspClient.start]] 关联到这里的验证场景。"} {:block/title "协作：[[LspClient.start 与 _version_matches 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[add_git_bootstrap_arguments]] 关联到这里的验证场景。"} {:block/title "协作：[[add_initial_arguments]] 关联到这里的验证场景。"} {:block/title "协作：[[add_initial_arguments 与 add_git_bootstrap_arguments 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[create_source_snapshot]] 关联到这里的验证场景。"} {:block/title "协作：[[create_source_snapshot 与 git 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[execute]] 关联到这里的验证场景。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config]] 关联到这里的验证场景。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file]] 关联到这里的验证场景。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[parser]] 关联到这里的验证场景。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[remove]] 关联到这里的验证场景。"} {:block/title "协作：[[remove 与 skill_root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[status]] 关联到这里的验证场景。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer]] 关联到这里的验证场景。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "ingest_event"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`ingest_event` 是所有 Harness Hook 进入 CKB 自动化核心的统一边界。 它先匹配项目，再核对当前 session 的精确 Skill 激活；显式 prompt、原生 Skill 元数据或已登记激活通过后才脱敏、入队和写入数据库。"} {:block/title "需要修改时：当项目路由、激活判据、静默忽略语义、脱敏或 Hook 输出约定变化时，需要修改该函数并重跑正反向 canary。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/automation.py 第 1321 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation.py:1321:1)  `scripts/ckb_core/automation.py:1321-1392`"} {:block/title "协作：实现时会用到 [[AutomationTest.event]]。"} {:block/title "协作：实现时会用到 [[ingest_event 与 default_registry_path 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.event 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 汇总了本页。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"}]}
{:page {:block/title "ingest_event 与 default_registry_path 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该代码页汇总跨 Harness 会话与修改自动化，包括项目登记、session 级 Skill 激活、事件规范化、脱敏、spool、SQLite 和 Agent 审阅。 它要求仓库登记与 `code-knowledge-builder` 精确激活同时成立；激活前事件静默忽略，激活后才记录会话、工具、修改与待审阅证据。"} {:block/title "需要修改时：当激活证据、注册表 Schema、Harness 事件映射、SQLite 表、幂等键、路径过滤或晋升规则变化时，需要修改本页。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/automation.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation.py:1:1)  `scripts/ckb_core/automation.py:1-1632`"} {:block/title "内部实现：default_registry_path — 选择环境变量指定或用户目录下的自动化注册表路径。"} {:block/title "内部实现：_path_key — 把跨平台路径规范化为可稳定比较的注册键。"} {:block/title "内部实现：_is_within — 判断事件或文件路径是否位于指定根目录内部。"} {:block/title "内部实现：_read_registry — 读取自动化注册表，并把 schema 1/2 确定性补齐为要求 session Skill 激活的 schema 3 视图。"} {:block/title "内部实现：register_project — 登记仓库、知识库、工作区和 Harness，并固定 required skill 与 session 激活硬门。"} {:block/title "内部实现：unregister_project — 按源码仓库根移除项目注册但保留已经落库的知识记录。"} {:block/title "内部实现：registry_status — 返回当前自动化注册表、项目数量和登记明细。"} {:block/title "内部实现：_registration_for_event — 按直接仓库优先和最深 workspace 根选择唯一事件注册。"} {:block/title "内部实现：_walk_values — 递归遍历嵌套事件中的键值以支持通用字段提取。"} {:block/title "内部实现：_first_scalar — 从不同 Harness 嵌套字段中选择首个非空标量。"} {:block/title "内部实现：_text_content — 从消息、part、content 或 delta 结构中抽取文本。"} {:block/title "内部实现：_event_name — 从原始 payload 的候选字段中确定原生事件名称。"} {:block/title "内部实现：_message_role — 提取并规范化 OpenCode 等消息结构中的角色。"} {:block/title "内部实现：_normalized_skill_name — 去除调用前缀与括号并规范化 skill name，供精确名称比较使用。"} {:block/title "内部实现：_skill_name — 从原生 Skill 元数据、Skill 工具输入或显式调用 prompt 中提取精确的 code-knowledge-builder 名称。"} {:block/title "内部实现：_canonical_type — 把宿主事件映射到十种规范事件，并将原生 Skill 调用映射为 `skill.applied`。"} {:block/title "内部实现：_extract_paths — 从路径字段和 apply_patch 文本中收集候选变化路径。"} {:block/title "内部实现：normalize_event — 把 Harness 原始负载规范化为统一事件，同时保留 session、turn、工具、路径与 Skill 激活证据。"} {:block/title "内部实现：_redact_text — 按凭据、私钥、自定义规则和长度上限处理单个文本字段。"} {:block/title "内部实现：redact_event — 递归脱敏规范事件并记录脱敏类型与次数。"} {:block/title "内部实现：redact_event.redact — 递归处理事件字典、数组、标量和敏感键值。"} {:block/title "内部实现：_automation_root — 建立自动化 spool、失败区和待审阅 sidecar 目录。"} {:block/title "内部实现：initialize_automation_database — 初始化自动化 SQLite、FTS 与 `skill_activations` 表，并升级既有数据库 Schema。"} {:block/title "内部实现：_git_status_paths — 读取限定在源码仓库当前目录的 Git 变化路径。"} {:block/title "内部实现：_change_path_allowed — 过滤缓存、依赖、构建目录和字节码文件。"} {:block/title "内部实现：_working_file_state — 记录变化文件的存在性、大小和内容摘要以识别后续修改。"} {:block/title "内部实现：_relative_changed_paths — 把 workspace 或仓库相对路径安全映射为源码仓库相对路径。"} {:block/title "内部实现：_drain_lock — 以单输出锁串行化并发 Hook 的队列导入。"} {:block/title "内部实现：enqueue_event — 把脱敏事件原子写入待处理 spool。"} {:block/title "内部实现：_spool_events — 按文件名稳定排序一个 spool 目录中的事件。"} {:block/title "内部实现：_session_key — 由 Harness、外部会话和源码仓库生成稳定会话键。"} {:block/title "内部实现：default_session_id — 按 Harness 从环境变量推断当前 session ID，并优先使用通用 CKB session 变量。"} {:block/title "内部实现：_activation_key — 根据 Harness、session、仓库根和精确 skill name 生成稳定激活键。"} {:block/title "内部实现：_record_skill_activation — 幂等写入 session Skill 激活证据，并返回首次激活或已激活状态。"} {:block/title "内部实现：_skill_activation — 查询指定 Harness、session 和仓库是否已经存在精确 Skill 激活记录。"} {:block/title "内部实现：activate_skill_session — 实现 Agent 主动激活命令，匹配注册项目后为当前 Harness session 写入激活证据。"} {:block/title "内部实现：_explicit_skill_application — 判定规范事件是否携带显式 prompt、原生 Skill 事件或精确 Harness 元数据激活证据。"} {:block/title "内部实现：_ensure_session — 创建或恢复自动化 session，并在首次 Skill 激活时保存当前 Git 工作树基线。"} {:block/title "内部实现：_resolve_turn — 把 Skill/session 事件保持在会话层，并为 prompt、工具与 Stop 确定性解析活动 turn。"} {:block/title "内部实现：_event_id — 优先使用外部幂等键，否则由规范事件内容生成稳定事件键。"} {:block/title "内部实现：_pending_review_content — 把用户请求、最终回答和变化路径聚合为机器待审阅正文。"} {:block/title "内部实现：_create_pending_review — 在轮次结束时合并路径证据并创建唯一待审阅记录。"} {:block/title "内部实现：_process_event — 在单个 SQLite 事务内更新事件、轮次、工具、路径与审阅状态。"} {:block/title "内部实现：drain_automation — 持锁导入待处理 spool，并把成功、重复和失败事件分别归档。"} {:block/title "内部实现：retry_failed_automation — 把失败事件显式移回待处理区并重新执行导入。"} {:block/title "内部实现：_hook_context — 为已经激活的 Skill session 生成简短中文自动化上下文和待审阅计数。"} {:block/title "内部实现：_hook_output — 按 Harness 原生输出协议返回附加上下文或空对象。"} {:block/title "内部实现：automation_status — 汇总 Skill 激活、事件、会话、轮次、路径、待审阅、spool 与 SQLite 完整性状态。"} {:block/title "内部实现：pending_automation_reviews — 查询待审阅或全部自动化审阅记录。"} {:block/title "内部实现：write_automation_review_template — 根据机器记录写出带逐路径来源检查的审阅模板。"} {:block/title "内部实现：_heading_errors — 检查修改记录是否包含修改内容、原因和验证结果三个标题。"} {:block/title "内部实现：review_automation — 验证中文正文与逐路径证据后把机器记录晋升为人类笔记。"} {:block/title "内部实现：search_automation — 使用 SQLite FTS 检索机器层自动化记录。"} {:block/title "内部实现：automation_documents — 按类型和时间导出机器检索可消费的自动化文档。"} {:block/title "协作：实现时会用到 [[AutomationTest.event]]。"} {:block/title "协作：实现时会用到 [[LspClient.start]]。"} {:block/title "协作：实现时会用到 [[LspClient.start 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[audit_migration]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：主要代码单元是 [[ingest_event]]。"} {:block/title "协作：实现时会用到 [[record_note]]。"} {:block/title "协作：实现时会用到 [[record_note 与 page_tag 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[main（generate_large_fixture 测试）]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.event 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "load_page_config"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`load_page_config` 是源码中负责规范化并校验不可漂移的页面配置的命名代码单元。 它在所属模块内执行规范化并校验不可漂移的页面配置，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当规范化并校验不可漂移的页面配置所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/page_config.py 第 209 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/page_config.py:209:1)  `scripts/ckb_core/page_config.py:209-219`"} {:block/title "协作：实现时会用到 [[load_page_config 与 _merge_known 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 汇总了本页。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "load_page_config 与 _merge_known 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现页面配置合并、规范化、校验和固定。 它是 Code Knowledge Builder 中承载页面配置合并、规范化、校验和固定的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当页面配置合并、规范化、校验和固定的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/page_config.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/page_config.py:1:1)  `scripts/ckb_core/page_config.py:1-244`"} {:block/title "内部实现：_merge_known — 该附属代码负责合并批次实体和跨批次关系，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_integer — 该附属代码负责页面配置合并、规范化、校验和固定，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_section_list — 该附属代码负责页面配置合并、规范化、校验和固定，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：normalize_page_config — 该附属代码负责规范化并校验不可漂移的页面配置，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：page_config_bytes — 该附属代码负责规范化并校验不可漂移的页面配置，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：page_config_sha256 — 该附属代码负责规范化并校验不可漂移的页面配置，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：write_page_config — 该附属代码负责规范化并校验不可漂移的页面配置，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：主要代码单元是 [[load_page_config]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[load_page_config]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "main 与 sha256 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现离线运行时清单、可复现压缩和归档校验。 它是 Code Knowledge Builder 中承载离线运行时清单、可复现压缩和归档校验的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当离线运行时清单、可复现压缩和归档校验的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/build_runtime_payload.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/build_runtime_payload.py:1:1)  `scripts/build_runtime_payload.py:1-117`"} {:block/title "内部实现：sha256 — 该附属代码负责计算机器交付物的完整性摘要，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：relative_files — 该附属代码负责离线运行时清单、可复现压缩和归档校验，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：build — 该附属代码负责离线运行时清单、可复现压缩和归档校验，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：主要代码单元是 [[main（build_runtime_payload 实现）]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "main 相关实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该代码页汇总从空基线生成 Skill 文本统一补丁的实现。 它遍历可交付 UTF-8 文本文件，排除 `.git`、第三方 vendored 源码、运行时资产、缓存和二进制文件，再为每个文件生成新增补丁段。"} {:block/title "需要修改时：当源码目录结构、文本交付边界或补丁格式变化时，需要修改本页并检查输出中不含仓库元数据。"} {:block/title "源码入口：[打开源码：scripts/make_source_patch.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/make_source_patch.py:1:1)  `scripts/make_source_patch.py:1-47`"} {:block/title "协作：主要代码单元是 [[main（make_source_patch 实现）]]。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "main 等测试场景（fake_logseq 测试）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现Logseq CLI 命令形状和投影计数的确定性测试替身。 它是 Code Knowledge Builder 中承载Logseq CLI 命令形状和投影计数的确定性测试替身的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当Logseq CLI 命令形状和投影计数的确定性测试替身的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：tests/fake_logseq.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/fake_logseq.py:1:1)  `tests/fake_logseq.py:1-80`"} {:block/title "内部实现：option — 该附属代码负责Logseq CLI 命令形状和投影计数的确定性测试替身，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：主要代码单元是 [[main（fake_logseq 测试）]]。"} {:block/title "协作：由 [[main（fake_logseq 测试）]] 覆盖相关行为。"} {:block/title "协作：[[main（fake_logseq 测试）]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "main 等测试场景（generate_large_fixture 测试）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现大规模混合语言仓库样例生成。 它是 Code Knowledge Builder 中承载大规模混合语言仓库样例生成的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当大规模混合语言仓库样例生成的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：tests/generate_large_fixture.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/generate_large_fixture.py:1:1)  `tests/generate_large_fixture.py:1-42`"} {:block/title "协作：主要代码单元是 [[main（generate_large_fixture 测试）]]。"} {:block/title "协作：可从 [[tests 职责导览]] 进入本页。"}]}
{:page {:block/title "main（build_runtime_payload 实现）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`main` 是源码中负责接收命令参数并把请求路由到对应实现的命名代码单元。 它在所属模块内执行接收命令参数并把请求路由到对应实现，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当接收命令参数并把请求路由到对应实现所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/build_runtime_payload.py 第 101 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/build_runtime_payload.py:101:1)  `scripts/build_runtime_payload.py:101-112`"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：[[main 与 sha256 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "main（ckb 实现）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`main` 解析命令并把请求分派到对应的确定性实现。 它将 `automation activate` 路由到 session Skill 激活逻辑，并继续统一输出机器可读 JSON 与约定退出码。"} {:block/title "需要修改时：当子命令分派、激活返回值、错误传播或退出状态变化时，需要修改该函数。"} {:block/title "源码入口：[打开源码：scripts/ckb.py 第 331 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb.py:331:1)  `scripts/ckb.py:331-483`"} {:block/title "协作：实现时会用到 [[AutomationTest.event]]。"} {:block/title "协作：实现时会用到 [[AutomationTest.event 等测试场景]]。"} {:block/title "协作：实现时会用到 [[LspClient.start]]。"} {:block/title "协作：实现时会用到 [[LspClient.start 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[add_initial_arguments 与 add_git_bootstrap_arguments 的协作实现]]。"} {:block/title "协作：实现时会用到 [[audit_migration]]。"} {:block/title "协作：实现时会用到 [[audit_migration 与 _entity_key 的协作实现]]。"} {:block/title "协作：实现时会用到 [[ingest_event]]。"} {:block/title "协作：实现时会用到 [[ingest_event 与 default_registry_path 的协作实现]]。"} {:block/title "协作：实现时会用到 [[load_page_config]]。"} {:block/title "协作：实现时会用到 [[load_page_config 与 _merge_known 的协作实现]]。"} {:block/title "协作：实现时会用到 [[package_showcase]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：实现时会用到 [[query_graph]]。"} {:block/title "协作：实现时会用到 [[query_graph 与 _networkx_modules 的协作实现]]。"} {:block/title "协作：实现时会用到 [[record_note]]。"} {:block/title "协作：实现时会用到 [[record_note 与 page_tag 的协作实现]]。"} {:block/title "协作：实现时会用到 [[remove 与 skill_root 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：[[add_initial_arguments 与 add_git_bootstrap_arguments 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "main（fake_logseq 测试）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`main` 是源码中负责接收命令参数并把请求路由到对应实现的命名代码单元。 它在所属模块内执行接收命令参数并把请求路由到对应实现，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当接收命令参数并把请求路由到对应实现所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：tests/fake_logseq.py 第 22 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/fake_logseq.py:22:1)  `tests/fake_logseq.py:22-75`"} {:block/title "协作：实现时会用到 [[LspClient.start 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[main 等测试场景（fake_logseq 测试）]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：[[main 等测试场景（fake_logseq 测试）]] 汇总了本页。"}]}
{:page {:block/title "main（generate_large_fixture 测试）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`main` 是源码中负责接收命令参数并把请求路由到对应实现的命名代码单元。 它在所属模块内执行接收命令参数并把请求路由到对应实现，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当接收命令参数并把请求路由到对应实现所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：tests/generate_large_fixture.py 第 8 行](vscode://file/E:/knowledge_builder/self-workspace/source/tests/generate_large_fixture.py:8:1)  `tests/generate_large_fixture.py:8-37`"} {:block/title "协作：实现时会用到 [[audit_migration]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[run]]。"} {:block/title "协作：[[AutomationTest.event]] 关联到这里的验证场景。"} {:block/title "协作：[[AutomationTest.event 等测试场景]] 关联到这里的验证场景。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[add_git_bootstrap_arguments]] 关联到这里的验证场景。"} {:block/title "协作：[[add_initial_arguments]] 关联到这里的验证场景。"} {:block/title "协作：[[add_initial_arguments 与 add_git_bootstrap_arguments 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[main 等测试场景（generate_large_fixture 测试）]] 汇总了本页。"} {:block/title "协作：[[parser]] 关联到这里的验证场景。"} {:block/title "协作：[[remove]] 关联到这里的验证场景。"} {:block/title "协作：[[remove 与 skill_root 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 关联到这里的验证场景。"} {:block/title "协作：[[retrieve]] 关联到这里的验证场景。"}]}
{:page {:block/title "main（make_source_patch 实现）"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`main` 解析补丁输出路径并生成从空目录到完整 Skill 的文本统一差异。 它确定性过滤 `.git`、第三方 vendored 源码与二进制资产，按路径排序拼接新增文件补丁并以 UTF-8/LF 写入。"} {:block/title "需要修改时：当排除集合、编码策略、排序规则或统一差异格式变化时，需要修改该函数。"} {:block/title "源码入口：[打开源码：scripts/make_source_patch.py 第 19 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/make_source_patch.py:19:1)  `scripts/make_source_patch.py:19-42`"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：[[main 相关实现]] 汇总了本页。"}]}
{:page {:block/title "module_name"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`module_name` 是源码中负责页面配额、实体归属、关系预算和上下文预算的确定性决策的命名代码单元。 它在所属模块内执行页面配额、实体归属、关系预算和上下文预算的确定性决策，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当页面配额、实体归属、关系预算和上下文预算的确定性决策所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/navigation.py 第 37 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/navigation.py:37:1)  `scripts/ckb_core/navigation.py:37-39`"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "module_name 与 estimated_tokens 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现页面配额、实体归属、关系预算和上下文预算的确定性决策。 它是 Code Knowledge Builder 中承载页面配额、实体归属、关系预算和上下文预算的确定性决策的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当页面配额、实体归属、关系预算和上下文预算的确定性决策的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/navigation.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/navigation.py:1:1)  `scripts/ckb_core/navigation.py:1-456`"} {:block/title "内部实现：estimated_tokens — 该附属代码负责页面配额、实体归属、关系预算和上下文预算的确定性决策，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_test_path — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_graph_facts — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_ancestors — 该附属代码负责页面配额、实体归属、关系预算和上下文预算的确定性决策，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_eligible — 该附属代码负责页面配额、实体归属、关系预算和上下文预算的确定性决策，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_rank — 该附属代码负责页面配额、实体归属、关系预算和上下文预算的确定性决策，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：build_navigation_plan — 该附属代码负责按固定排序计算页面、附录、归属和审阅预算，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：apply_navigation_plan — 该附属代码负责按固定排序计算页面、附录、归属和审阅预算，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：page_limit — 该附属代码负责按固定排序计算页面、附录、归属和审阅预算，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：context_budget_record — 该附属代码负责页面配额、实体归属、关系预算和上下文预算的确定性决策，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：build_review_packs — 该附属代码负责按固定排序计算页面、附录、归属和审阅预算，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：build_review_packs.partition — 该附属代码负责按固定排序计算页面、附录、归属和审阅预算，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：build_review_packs.partition.flush — 该附属代码负责按固定排序计算页面、附录、归属和审阅预算，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：主要代码单元是 [[module_name]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "package_showcase"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`package_showcase` 是源码中负责构建可复现发行归档并复核成员集合的命名代码单元。 它在所属模块内执行构建可复现发行归档并复核成员集合，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当构建可复现发行归档并复核成员集合所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/showcase.py 第 66 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/showcase.py:66:1)  `scripts/ckb_core/showcase.py:66-172`"} {:block/title "协作：实现时会用到 [[package_showcase 与 _parse_sample 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[package_showcase 与 _parse_sample 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "package_showcase 与 _parse_sample 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现人类可读样例集合、中文 Wiki 和展示归档。 它是 Code Knowledge Builder 中承载人类可读样例集合、中文 Wiki 和展示归档的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当人类可读样例集合、中文 Wiki 和展示归档的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/showcase.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/showcase.py:1:1)  `scripts/ckb_core/showcase.py:1-173`"} {:block/title "内部实现：_parse_sample — 该附属代码负责解析命名声明并保存稳定源码范围，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_root_wiki — 该附属代码负责人类可读样例集合、中文 Wiki 和展示归档，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：主要代码单元是 [[package_showcase]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：[[package_showcase]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "parse_file"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`parse_file` 是源码中负责解析命名声明并保存稳定源码范围的命名代码单元。 它在所属模块内执行解析命名声明并保存稳定源码范围，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当解析命名声明并保存稳定源码范围所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/parsers.py 第 191 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/parsers.py:191:1)  `scripts/ckb_core/parsers.py:191-329`"} {:block/title "协作：实现时会用到 [[AutomationTest.event]]。"} {:block/title "协作：实现时会用到 [[LspClient.start]]。"} {:block/title "协作：实现时会用到 [[LspClient.start 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[parse_file 与 _language 的协作实现]]。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：实现时会用到 [[remove]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 汇总了本页。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "parse_file 与 _language 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现Tree-sitter 实体提取、C# partial 合并和词法引用关系。 它是 Code Knowledge Builder 中承载Tree-sitter 实体提取、C# partial 合并和词法引用关系的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当Tree-sitter 实体提取、C# partial 合并和词法引用关系的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/parsers.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/parsers.py:1:1)  `scripts/ckb_core/parsers.py:1-437`"} {:block/title "内部实现：_language — 该附属代码负责Tree-sitter 实体提取、C# partial 合并和词法引用关系，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_walk — 该附属代码负责Tree-sitter 实体提取、C# partial 合并和词法引用关系，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_node_text — 该附属代码负责Tree-sitter 实体提取、C# partial 合并和词法引用关系，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_byte_position — 该附属代码负责Tree-sitter 实体提取、C# partial 合并和词法引用关系，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_find_name — 该附属代码负责Tree-sitter 实体提取、C# partial 合并和词法引用关系，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_public_evidence — 该附属代码负责Tree-sitter 实体提取、C# partial 合并和词法引用关系，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_single_statement_shape — 该附属代码负责计算机器交付物的完整性摘要，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_csharp_fallback_name — 该附属代码负责计算机器交付物的完整性摘要，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：merge_csharp_partials — 该附属代码负责合并批次实体和跨批次关系，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：lexical_links — 该附属代码负责Tree-sitter 实体提取、C# partial 合并和词法引用关系，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：实现时会用到 [[AutomationTest.event]]。"} {:block/title "协作：实现时会用到 [[LspClient.start]]。"} {:block/title "协作：主要代码单元是 [[parse_file]]。"} {:block/title "协作：实现时会用到 [[remove]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[parse_file]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "parser"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`parser` 定义 Code Knowledge Builder 的完整命令树和参数约束。 它为自动化增加 `activate` 子命令，接收 Harness、session、任务根、注册表和激活来源，同时保留原有构建与审计接口。"} {:block/title "需要修改时：当命令名称、参数默认值、Harness 列表或激活握手字段变化时，需要修改该函数。"} {:block/title "源码入口：[打开源码：scripts/ckb.py 第 115 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb.py:115:1)  `scripts/ckb.py:115-324`"} {:block/title "协作：实现时会用到 [[AutomationTest.event]]。"} {:block/title "协作：实现时会用到 [[AutomationTest.event 等测试场景]]。"} {:block/title "协作：实现时会用到 [[LspClient.start]]。"} {:block/title "协作：实现时会用到 [[add_git_bootstrap_arguments]]。"} {:block/title "协作：实现时会用到 [[add_initial_arguments]]。"} {:block/title "协作：实现时会用到 [[add_initial_arguments 与 add_git_bootstrap_arguments 的协作实现]]。"} {:block/title "协作：实现时会用到 [[ingest_event 与 default_registry_path 的协作实现]]。"} {:block/title "协作：实现时会用到 [[remove]]。"} {:block/title "协作：实现时会用到 [[remove 与 skill_root 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：实现时会用到 [[status 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[main（generate_large_fixture 测试）]] 覆盖相关行为。"} {:block/title "协作：[[add_git_bootstrap_arguments]] 会使用这里提供的行为。"} {:block/title "协作：[[add_initial_arguments]] 会使用这里提供的行为。"} {:block/title "协作：[[add_initial_arguments 与 add_git_bootstrap_arguments 的协作实现]] 汇总了本页。"} {:block/title "协作：[[create_source_snapshot]] 会使用这里提供的行为。"} {:block/title "协作：[[execute 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[main（build_runtime_payload 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[main（generate_large_fixture 测试）]] 会使用这里提供的行为。"} {:block/title "协作：[[main（make_source_patch 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file]] 会使用这里提供的行为。"} {:block/title "协作：[[source_files 与 sha256 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "prepare_vault"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`prepare_vault` 清理上一轮生成器拥有的文件，并建立页面与五类笔记目录。 它以所有权清单控制重建范围，避免覆盖用户页面和 Obsidian 工作区状态。"} {:block/title "需要修改时：生成器所有权、保留目录或 vault 初始化规则变化时，需要修改该函数。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/obsidian.py 第 23 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/obsidian.py:23:1)  `scripts/ckb_core/obsidian.py:23-43`"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：[[prepare_vault 与 install_obsidian 的协作实现]] 汇总了本页。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "prepare_vault 与 install_obsidian 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该页面汇总 Obsidian vault 准备、最小配置安装、生成器所有权和审计实现。 它让生成页面可重建，同时保留用户 workspace、笔记和未知文件。"} {:block/title "需要修改时：Obsidian 目录、保留规则、CSS 或审计约束变化时，需要修改本文件。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/obsidian.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/obsidian.py:1:1)  `scripts/ckb_core/obsidian.py:1-134`"} {:block/title "内部实现：install_obsidian — 写入最小 Obsidian 配置、样式和默认图谱设置。"} {:block/title "内部实现：write_generated_ownership — 该附属代码负责稳定读取或写入机器状态记录，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：audit_obsidian — 检查必要目录、配置、单标签和用户文件保留契约。"} {:block/title "协作：主要代码单元是 [[prepare_vault]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "query_graph"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`query_graph` 是源码中负责构造职责关系图并提供职责群或路径查询的命名代码单元。 它在所属模块内执行构造职责关系图并提供职责群或路径查询，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当构造职责关系图并提供职责群或路径查询所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/graphify_core.py 第 526 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/graphify_core.py:526:1)  `scripts/ckb_core/graphify_core.py:526-583`"} {:block/title "协作：实现时会用到 [[query_graph 与 _networkx_modules 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[source_files]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "query_graph 与 _networkx_modules 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现Graphify 兼容图、确定性职责群、路径查询和关系报告。 它是 Code Knowledge Builder 中承载Graphify 兼容图、确定性职责群、路径查询和关系报告的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当Graphify 兼容图、确定性职责群、路径查询和关系报告的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/graphify_core.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/graphify_core.py:1:1)  `scripts/ckb_core/graphify_core.py:1-676`"} {:block/title "内部实现：_networkx_modules — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_canonical_json_sha256 — 该附属代码负责稳定读取或写入机器状态记录，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_source_location — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_node_label — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_description — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_confidence — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_graphify_node — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_graphify_link — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_build_networkx — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_community_records — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_report — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：project_graphify — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：audit_graphify — 该附属代码负责执行确定性完整性与来源真实性检查，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_load_projected_graph — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_terms — 该附属代码负责维护旧版页面索引兼容接口并生成受预算约束的阅读包，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_seed_scores — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_adjacency — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_query_size — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_resolve_node — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：shortest_path — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：explain_node — 该附属代码负责Graphify 兼容图、确定性职责群、路径查询和关系报告，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：实现时会用到 [[LspClient.start]]。"} {:block/title "协作：主要代码单元是 [[query_graph]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[run]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：实现时会用到 [[status 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[LspClient.start 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "record_note"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`record_note` 是源码中负责保存实时工作区变化和双链知识记录的命名代码单元。 它在所属模块内执行保存实时工作区变化和双链知识记录，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当保存实时工作区变化和双链知识记录所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/workspace_notes.py 第 106 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/workspace_notes.py:106:1)  `scripts/ckb_core/workspace_notes.py:106-183`"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]]。"} {:block/title "协作：实现时会用到 [[record_note 与 page_tag 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve 与 _tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：实现时会用到 [[sync_human_layer 与 _source_manifest 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 汇总了本页。"} {:block/title "协作：[[start_session 与 _session_directory 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "record_note 与 page_tag 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现实时工作树覆盖层和分析、变更、踩坑、实验、会话笔记。 它是 Code Knowledge Builder 中承载实时工作树覆盖层和分析、变更、踩坑、实验、会话笔记的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当实时工作树覆盖层和分析、变更、踩坑、实验、会话笔记的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/workspace_notes.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/workspace_notes.py:1:1)  `scripts/ckb_core/workspace_notes.py:1-374`"} {:block/title "内部实现：page_tag — 该附属代码负责实时工作树覆盖层和分析、变更、踩坑、实验、会话笔记，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_markdown_root — 该附属代码负责实时工作树覆盖层和分析、变更、踩坑、实验、会话笔记，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_resolve_page_titles — 该附属代码负责实时工作树覆盖层和分析、变更、踩坑、实验、会话笔记，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_source_links_for_titles — 该附属代码负责生成并核对可直接打开源码位置的 URI，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：queue_pending_note — 该附属代码负责管理 Agent 会话、构建中记录和修改总结落页，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：selectors_for_changed_paths — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：materialize_pending_notes — 该附属代码负责管理 Agent 会话、构建中记录和修改总结落页，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：audit_notes — 该附属代码负责执行确定性完整性与来源真实性检查，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：sync_workspace — 该附属代码负责保存实时工作区变化和双链知识记录，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：workspace_status — 该附属代码负责保存实时工作区变化和双链知识记录，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]]。"} {:block/title "协作：主要代码单元是 [[record_note]]。"} {:block/title "协作：实现时会用到 [[retrieve 与 _tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[start_session]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session 与 _session_directory 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "remove"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`remove` 是源码中负责管理隔离离线运行时及其回滚的命名代码单元。 它在所属模块内执行管理隔离离线运行时及其回滚，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当管理隔离离线运行时及其回滚所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/runtime.py 第 126 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/runtime.py:126:1)  `scripts/ckb_core/runtime.py:126-147`"} {:block/title "协作：实现时会用到 [[LspClient.start 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[remove 与 skill_root 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[main（generate_large_fixture 测试）]] 覆盖相关行为。"} {:block/title "协作：[[create_source_snapshot 与 git 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[remove 与 skill_root 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "remove 与 skill_root 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现私有离线运行时部署、复用检查和移除。 它是 Code Knowledge Builder 中承载私有离线运行时部署、复用检查和移除的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当私有离线运行时部署、复用检查和移除的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/runtime.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/runtime.py:1:1)  `scripts/ckb_core/runtime.py:1-153`"} {:block/title "内部实现：skill_root — 该附属代码负责私有离线运行时部署、复用检查和移除，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：lock_document — 该附属代码负责私有离线运行时部署、复用检查和移除，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：payload_path — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：deployment_plan — 该附属代码负责管理隔离离线运行时及其回滚，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：deploy — 该附属代码负责管理隔离离线运行时及其回滚，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_host_snapshot — 该附属代码负责建立并验证与固定提交一致的源码快照，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：实现时会用到 [[LspClient.start 与 _version_matches 的协作实现]]。"} {:block/title "协作：主要代码单元是 [[remove]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[main（generate_large_fixture 测试）]] 覆盖相关行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[remove]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "render_integration"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`render_integration` 在隔离目录中生成指定 Harness 的完整适配包和清单。 它写入 Hook/Plugin 文件，并在 `integration.json` 固定 `session_skill_activation_required=true` 与精确 required skill。"} {:block/title "需要修改时：当适配器文件布局、集成版本、激活字段或宿主命令模板变化时，需要修改该函数。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/automation_integrations.py 第 391 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation_integrations.py:391:1)  `scripts/ckb_core/automation_integrations.py:391-501`"} {:block/title "内部实现：render_integration.write — 把适配器文件按 UTF-8 写入隔离目标并登记到清单。"} {:block/title "协作：实现时会用到 [[AutomationTest.event]]。"} {:block/title "协作：实现时会用到 [[render_integration 与 _looks_windows 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "render_integration 与 _looks_windows 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该代码页汇总 Codex、Claude Code、OpenCode、DSH、Gemini、Copilot、Cursor 和 generic 的 Hook/Plugin 配置生成器。 它为各 Harness 生成原生事件适配，并声明 session 必须明确应用 `code-knowledge-builder`；Claude 和 OpenCode 还输出原生 Skill/command 激活事件。"} {:block/title "需要修改时：当宿主协议、事件名称、timeout、Skill 调用事件或配置格式变化时，需要修改本页并重跑 JSON 与 JavaScript 语法检查。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/automation_integrations.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/automation_integrations.py:1:1)  `scripts/ckb_core/automation_integrations.py:1-502`"} {:block/title "内部实现：_looks_windows — 根据可执行文件路径判断适配包是否面向 Windows。"} {:block/title "内部实现：_commands — 生成 POSIX 与 Windows cmd 两种 CKB Hook 命令。"} {:block/title "内部实现：_powershell_command — 生成带安全单引号转义的 PowerShell Hook 命令。"} {:block/title "内部实现：_powershell_command.quote — `quote` 负责执行其命名职责对应的确定性处理，并把结果交给所属主流程。"} {:block/title "内部实现：_handler — 构造 Codex 兼容 command Hook 的超时和状态字段。"} {:block/title "内部实现：_codex_hooks — 生成 Codex/DSH 生命周期 Hook，并在说明中固定显式 Skill 应用后的同步边界。"} {:block/title "内部实现：_claude_hooks — 生成 Claude Hooks，并增加 UserPromptExpansion 与 Skill 工具的原生激活监听。"} {:block/title "内部实现：_claude_hooks.handler — `handler` 负责执行其命名职责对应的确定性处理，并把结果交给所属主流程。"} {:block/title "内部实现：_gemini_hooks — 按毫秒超时生成 Gemini CLI 生命周期 Hook 配置。"} {:block/title "内部实现：_gemini_hooks.handler — `handler` 负责执行其命名职责对应的确定性处理，并把结果交给所属主流程。"} {:block/title "内部实现：_copilot_hooks — 生成同时包含 bash 与 PowerShell 的 Copilot VS Code 兼容配置。"} {:block/title "内部实现：_copilot_hooks.handler — `handler` 负责执行其命名职责对应的确定性处理，并把结果交给所属主流程。"} {:block/title "内部实现：_cursor_hooks — 生成 Cursor 项目级会话、工具、文件和停止事件配置。"} {:block/title "内部实现：_cursor_hooks.handler — `handler` 负责执行其命名职责对应的确定性处理，并把结果交给所属主流程。"} {:block/title "内部实现：_opencode_stable_plugin — 生成 OpenCode stable Plugin，监听消息、命令、工具和 session 事件并输出精确 Skill 激活事件。"} {:block/title "内部实现：_opencode_v2_plugin — 生成 OpenCode V2 Plugin，通过 context、tool hook 与事件流同步 Skill 激活和会话证据。"} {:block/title "内部实现：_generic_schema — 生成含 `skill.applied`、精确 skill name 与显式布尔证据的通用事件 Schema。"} {:block/title "协作：实现时会用到 [[AutomationTest.event]]。"} {:block/title "协作：实现时会用到 [[LspClient.start]]。"} {:block/title "协作：实现时会用到 [[LspClient.start 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[add_initial_arguments 与 add_git_bootstrap_arguments 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：主要代码单元是 [[render_integration]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[main（generate_large_fixture 测试）]] 覆盖相关行为。"} {:block/title "协作：[[LspClient.start 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[SourceLinkRenderer.uri]] 会使用这里提供的行为。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "retrieve"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`retrieve` 是源码中负责维护旧版页面索引兼容接口并生成受预算约束的阅读包的命名代码单元。 它在所属模块内执行维护旧版页面索引兼容接口并生成受预算约束的阅读包，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当维护旧版页面索引兼容接口并生成受预算约束的阅读包所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/agent_index.py 第 440 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_index.py:440:1)  `scripts/ckb_core/agent_index.py:440-568`"} {:block/title "协作：实现时会用到 [[LspClient.start]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[module_name 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve 与 _tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[main（generate_large_fixture 测试）]] 覆盖相关行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 汇总了本页。"} {:block/title "协作：[[retrieve_machine]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session]] 会使用这里提供的行为。"}]}
{:page {:block/title "retrieve 与 _tokens 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现旧版 SQLite 页面索引和检索接口兼容层。 它是 Code Knowledge Builder 中承载旧版 SQLite 页面索引和检索接口兼容层的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当旧版 SQLite 页面索引和检索接口兼容层的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/agent_index.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_index.py:1:1)  `scripts/ckb_core/agent_index.py:1-569`"} {:block/title "内部实现：_tokens — 该附属代码负责旧版 SQLite 页面索引和检索接口兼容层，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_terms — 该附属代码负责维护旧版页面索引兼容接口并生成受预算约束的阅读包，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_note_documents — 该附属代码负责保存实时工作区变化和双链知识记录，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_projection — 该附属代码负责旧版 SQLite 页面索引和检索接口兼容层，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_page_documents — 该附属代码负责旧版 SQLite 页面索引和检索接口兼容层，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：build_agent_index — 该附属代码负责维护旧版页面索引兼容接口并生成受预算约束的阅读包，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：audit_agent_index — 该附属代码负责执行确定性完整性与来源真实性检查，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_agent_index_ready — 该附属代码负责维护旧版页面索引兼容接口并生成受预算约束的阅读包，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_fts_query — 该附属代码负责维护旧版页面索引兼容接口并生成受预算约束的阅读包，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_next_pack_path — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：主要代码单元是 [[retrieve]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[run]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：实现时会用到 [[status 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[record_note]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "retrieve_machine"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`retrieve_machine` 根据问题生成来源绑定且受预算约束的 Agent 阅读包。 它组合精确锚点、中文与标识符词项、实体和章节 FTS、元数据加权、测试折扣、固定图传播、路径多样化及缓存上下文，稳定保留最相关源码目标。"} {:block/title "需要修改时：候选上限、召回字段、权重、测试意图、图算法、缓存策略或阅读包结构变化时，需要修改该函数。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/machine_knowledge.py 第 942 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/machine_knowledge.py:942:1)  `scripts/ckb_core/machine_knowledge.py:942-1315`"} {:block/title "内部实现：retrieve_machine.add — 把一个实体的阶段分值、分解项和中文入选原因合并到查询状态。"} {:block/title "内部实现：retrieve_machine.entity_column — 为兼容旧机器库选择现有列或提供空值别名。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[ingest_event 与 default_registry_path 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：实现时会用到 [[status 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.event 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests]] 会使用这里提供的行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[LspClient.start 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[MigrationTest]] 会使用这里提供的行为。"} {:block/title "协作：[[create_source_snapshot]] 会使用这里提供的行为。"} {:block/title "协作：[[create_source_snapshot 与 git 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[execute 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[main（generate_large_fixture 测试）]] 会使用这里提供的行为。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[package_showcase]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 汇总了本页。"} {:block/title "协作：[[start_session]] 会使用这里提供的行为。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "retrieve_machine 与 estimated_tokens 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件实现完整 SQLite 机器知识库及纯确定性 Agent 检索。 它负责构建实体、关系、来源、中文章节和 FTS 索引，并以固定候选上限、批量读取、图传播和静态缓存生成低上下文阅读包。"} {:block/title "需要修改时：机器 Schema、检索召回规则、排序权重、缓存失效条件或阅读包预算策略变化时，需要修改该文件。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/machine_knowledge.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/machine_knowledge.py:1:1)  `scripts/ckb_core/machine_knowledge.py:1-1458`"} {:block/title "内部实现：estimated_tokens — 按 UTF-8 字节数估算阅读文本的上下文令牌占用。"} {:block/title "内部实现：contains_chinese_narrative — 检查叙述字段是否达到最小中文字符数量。"} {:block/title "内部实现：_split_camel — 把驼峰式代码标识符稳定拆成可检索词项。"} {:block/title "内部实现：search_terms — 将问题规范化为英文标识符片段以及中文二元、三元检索词项。"} {:block/title "内部实现：explicit_anchors — 从问题中提取具备代码标识符特征的精确查询锚点。"} {:block/title "内部实现：_fts_query — 把高信息词项转义并组合为有界的 SQLite FTS 查询。"} {:block/title "内部实现：_human_projection — 按固定优先级定位已完成的人类知识库投影。"} {:block/title "内部实现：_note_documents — 读取分析、修改、踩坑、实验和会话 Markdown，转成机器文档记录。"} {:block/title "内部实现：_review_paths — 建立实体到已通过 Agent 审阅文件的来源映射。"} {:block/title "内部实现：_source_texts — 从固定源码快照读取图中涉及文件的完整文本。"} {:block/title "内部实现：_description — 按实体分类拼接独立页三段说明或附录单句说明。"} {:block/title "内部实现：_human_map — 读取实体所属人类页面以及页面元数据映射。"} {:block/title "内部实现：_sections_for_entity — 把中文说明、来源核对和有界源码摘录拆成机器检索章节。"} {:block/title "内部实现：_create_schema — 创建完整实体、关系、来源、文档、章节和 FTS 表的 SQLite Schema。"} {:block/title "内部实现：build_machine_knowledge — 从已审阅事实图构建完整机器知识库及全文检索索引。"} {:block/title "内部实现：_markdown_sections — 按 Markdown 标题切分人类笔记并保留无标题导言。"} {:block/title "内部实现：audit_machine_knowledge — 核对机器库完整性、外键、实体覆盖、中文叙述与投影数量。"} {:block/title "内部实现：_adjacency — 从关系表构建带权双向邻接表和实体度数。"} {:block/title "内部实现：_fast_graph_scores — 从种子实体执行固定两跳传播并施加度数惩罚。"} {:block/title "内部实现：_deterministic_ppr — 以固定轮次和重启率计算确定性的加权 PageRank 分数。"} {:block/title "内部实现：_next_pack_path — 为下一份 Agent 阅读包分配稳定且不冲突的 Markdown 与 JSON 路径。"} {:block/title "内部实现：_sql_placeholders — 根据参数数量生成 SQLite 参数占位符列表。"} {:block/title "内部实现：_utf8_prefix — 在不截断 UTF-8 字符的前提下取得指定字节预算内的文本前缀。"} {:block/title "内部实现：_bulk_entity_context — 用两次批量查询取得候选实体、源码范围、人类归属和章节内容。"} {:block/title "内部实现：_diverse_candidates — 按得分稳定排序，并优先保留来自不同源码路径的候选实体。"} {:block/title "内部实现：_compact_entity_block — 为每个入选实体生成保留来源链接且可按预算截断章节的紧凑区块。"} {:block/title "内部实现：_static_retrieval_key — 用机器库大小、修改时间和源码打开器时间生成缓存失效键。"} {:block/title "内部实现：_static_retrieval_context — 一次加载不可变实体元数据、章节、关系图和源码链接渲染器并缓存复用。"} {:block/title "内部实现：_openers — 读取本地源码打开器配置，缺省时从知识库状态构造配置。"} {:block/title "内部实现：coverage — 汇总机器知识库审计、逐实体审阅和中文说明覆盖率。"} {:block/title "内部实现：entity_lookup — 按稳定 ID、名称或限定名查询实体及精确源码范围。"} {:block/title "内部实现：neighbor_lookup — 从指定实体按深度和关系类型进行有界图邻居展开。"} {:block/title "内部实现：source_lookup — 返回指定实体在固定源码中的精确范围及少量上下文。"} {:block/title "内部实现：change_documents — 按类型和数量列出分析、修改、踩坑、实验或会话记录。"} {:block/title "内部实现：sync_workspace_changes — 把工作树变更同步为机器层覆盖记录而不改动固定基线图。"} {:block/title "协作：实现时会用到 [[LspClient.start]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri]]。"} {:block/title "协作：实现时会用到 [[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[ingest_event 与 default_registry_path 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve]]。"} {:block/title "协作：主要代码单元是 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[run]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[source_files]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[start_session]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session 与 _session_directory 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "run"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`run` 是源码中负责错误类型、JSON 写入、子进程调用、路径约束和状态标记的命名代码单元。 它在所属模块内执行错误类型、JSON 写入、子进程调用、路径约束和状态标记，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当错误类型、JSON 写入、子进程调用、路径约束和状态标记所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/common.py 第 77 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/common.py:77:1)  `scripts/ckb_core/common.py:77-99`"} {:block/title "协作：实现时会用到 [[audit_migration]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.event 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests]] 会使用这里提供的行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[LspClient.start 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[MigrationTest 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[create_source_snapshot]] 会使用这里提供的行为。"} {:block/title "协作：[[create_source_snapshot 与 git 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[execute]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（generate_large_fixture 测试）]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[run 与 CkbError 的协作实现]] 汇总了本页。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "run 与 CkbError 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现错误类型、JSON 写入、子进程调用、路径约束和状态标记。 它是 Code Knowledge Builder 中承载错误类型、JSON 写入、子进程调用、路径约束和状态标记的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当错误类型、JSON 写入、子进程调用、路径约束和状态标记的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/common.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/common.py:1:1)  `scripts/ckb_core/common.py:1-158`"} {:block/title "内部实现：CkbError — 该附属代码负责错误类型、JSON 写入、子进程调用、路径约束和状态标记，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：DependencyError — 该附属代码负责检查依赖版本和本地运行条件，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：ReviewRequired — 该附属代码负责核对并登记逐实体 Agent 审阅结果，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：AuditError — 该附属代码负责执行确定性完整性与来源真实性检查，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：StaleSourceError — 该附属代码负责错误类型、JSON 写入、子进程调用、路径约束和状态标记，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：utc_now — 该附属代码负责错误类型、JSON 写入、子进程调用、路径约束和状态标记，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：stable_id — 该附属代码负责错误类型、JSON 写入、子进程调用、路径约束和状态标记，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：sha256_file — 该附属代码负责计算机器交付物的完整性摘要，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：json_load — 该附属代码负责稳定读取或写入机器状态记录，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：json_write — 该附属代码负责稳定读取或写入机器状态记录，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：command_version — 该附属代码负责错误类型、JSON 写入、子进程调用、路径约束和状态标记，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：clear_markers — 该附属代码负责错误类型、JSON 写入、子进程调用、路径约束和状态标记，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：write_marker — 该附属代码负责稳定读取或写入机器状态记录，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：safe_title — 该附属代码负责错误类型、JSON 写入、子进程调用、路径约束和状态标记，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：path_inside — 该附属代码负责构造职责关系图并提供职责群或路径查询，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：safe_rmtree — 该附属代码负责错误类型、JSON 写入、子进程调用、路径约束和状态标记，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：temp_directory — 该附属代码负责错误类型、JSON 写入、子进程调用、路径约束和状态标记，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：主要代码单元是 [[run]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute]] 覆盖相关行为。"} {:block/title "协作：[[LspClient.start 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[create_source_snapshot]] 会使用这里提供的行为。"} {:block/title "协作：[[create_source_snapshot 与 git 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[load_page_config]] 会使用这里提供的行为。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[package_showcase]] 会使用这里提供的行为。"} {:block/title "协作：[[package_showcase 与 _parse_sample 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file 与 _language 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[prepare_vault]] 会使用这里提供的行为。"} {:block/title "协作：[[prepare_vault 与 install_obsidian 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[remove]] 会使用这里提供的行为。"} {:block/title "协作：[[remove 与 skill_root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[run]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[start_session]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session 与 _session_directory 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[sync_human_layer]] 会使用这里提供的行为。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "scripts 职责导览"} :blocks [{:block/title "标签：#类型/职责"} {:block/title "页面说明：这里汇集 scripts 范围内彼此协作的类、函数和辅助实现。"} {:block/title "协作：继续浏览 [[LspClient.start 与 _version_matches 的协作实现]]。"} {:block/title "协作：继续浏览 [[SourceLinkRenderer.uri 与 SourceLinkRenderer 的协作实现]]。"} {:block/title "协作：继续浏览 [[__init__ 实现概览]]。"} {:block/title "协作：继续浏览 [[add_initial_arguments 与 add_git_bootstrap_arguments 的协作实现]]。"} {:block/title "协作：继续浏览 [[audit_migration 与 _entity_key 的协作实现]]。"} {:block/title "协作：继续浏览 [[create_source_snapshot 与 git 的协作实现]]。"} {:block/title "协作：继续浏览 [[ingest_event 与 default_registry_path 的协作实现]]。"} {:block/title "协作：继续浏览 [[load_page_config 与 _merge_known 的协作实现]]。"} {:block/title "协作：继续浏览 [[main 与 sha256 的协作实现]]。"} {:block/title "协作：继续浏览 [[main 相关实现]]。"} {:block/title "协作：继续浏览 [[module_name 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：继续浏览 [[package_showcase 与 _parse_sample 的协作实现]]。"} {:block/title "协作：继续浏览 [[parse_file 与 _language 的协作实现]]。"} {:block/title "协作：继续浏览 [[prepare_vault 与 install_obsidian 的协作实现]]。"} {:block/title "协作：继续浏览 [[query_graph 与 _networkx_modules 的协作实现]]。"} {:block/title "协作：继续浏览 [[record_note 与 page_tag 的协作实现]]。"} {:block/title "协作：继续浏览 [[remove 与 skill_root 的协作实现]]。"} {:block/title "协作：继续浏览 [[render_integration 与 _looks_windows 的协作实现]]。"} {:block/title "协作：继续浏览 [[retrieve 与 _tokens 的协作实现]]。"} {:block/title "协作：继续浏览 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：继续浏览 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：继续浏览 [[source_files 与 sha256 的协作实现]]。"} {:block/title "协作：继续浏览 [[start_session 与 _session_directory 的协作实现]]。"} {:block/title "协作：继续浏览 [[status 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：继续浏览 [[sync_human_layer 与 _source_manifest 的协作实现]]。"} {:block/title "协作：可从 [[source 代码导览]] 进入本页。"}]}
{:page {:block/title "source 代码导览"} :blocks [{:block/title "标签：#类型/职责"} {:block/title "页面说明：从这里按职责进入项目中的类、函数和相关实现。"} {:block/title "协作：继续浏览 [[scripts 职责导览]]。"} {:block/title "协作：继续浏览 [[tests 职责导览]]。"}]}
{:page {:block/title "source_files"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`source_files` 按发行类型返回稳定排序的待归档文件集合。 它排除版本库和缓存产物，并确保 lite 包不携带 Windows 私有运行时载荷。"} {:block/title "需要修改时：源码布局、排除目录或 lite/full 文件边界变化时，需要修改该函数。"} {:block/title "源码入口：[打开源码：scripts/package_release.py 第 27 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/package_release.py:27:1)  `scripts/package_release.py:27-38`"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：[[query_graph]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[source_files 与 sha256 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "source_files 与 sha256 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件构建可复验的 lite 与 full-win-x64 Skill 发行包。 它校验锁定运行时、筛选源码、生成逐文件清单，并验证 ZIP CRC 与归档内清单。"} {:block/title "需要修改时：版本号、载荷锁、发行文件规则或归档验证要求变化时，需要修改该文件。"} {:block/title "源码入口：[打开源码：scripts/package_release.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/package_release.py:1:1)  `scripts/package_release.py:1-142`"} {:block/title "内部实现：sha256 — 流式计算发行文件的 SHA-256 校验值。"} {:block/title "内部实现：validate_full_payload — 核对 full 运行时载荷的路径、大小、哈希和必需成员。"} {:block/title "内部实现：build — 按 lite 或 full 规则收集文件、生成清单并写出可复验 ZIP。"} {:block/title "内部实现：main — 解析发行打包参数并输出构建结果。"} {:block/title "协作：实现时会用到 [[parser]]。"} {:block/title "协作：主要代码单元是 [[source_files]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"}]}
{:page {:block/title "start_session"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`start_session` 是源码中负责管理 Agent 会话、构建中记录和修改总结落页的命名代码单元。 它在所属模块内执行管理 Agent 会话、构建中记录和修改总结落页，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当管理 Agent 会话、构建中记录和修改总结落页所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/agent_maintenance.py 第 70 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_maintenance.py:70:1)  `scripts/ckb_core/agent_maintenance.py:70-142`"} {:block/title "协作：实现时会用到 [[LspClient.start]]。"} {:block/title "协作：实现时会用到 [[record_note 与 page_tag 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[start_session 与 _session_directory 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：实现时会用到 [[status 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：[[start_session 与 _session_directory 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "start_session 与 _session_directory 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现Agent 任务会话、构建中笔记排队、修改总结和生命周期状态。 它是 Code Knowledge Builder 中承载Agent 任务会话、构建中笔记排队、修改总结和生命周期状态的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当Agent 任务会话、构建中笔记排队、修改总结和生命周期状态的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/agent_maintenance.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/agent_maintenance.py:1:1)  `scripts/ckb_core/agent_maintenance.py:1-255`"} {:block/title "内部实现：_session_directory — 该附属代码负责管理 Agent 会话、构建中记录和修改总结落页，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_new_session_id — 该附属代码负责管理 Agent 会话、构建中记录和修改总结落页，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_record_or_queue — 该附属代码负责Agent 任务会话、构建中笔记排队、修改总结和生命周期状态，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_summary_heading_errors — 该附属代码负责Agent 任务会话、构建中笔记排队、修改总结和生命周期状态，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：finish_session — 该附属代码负责管理 Agent 会话、构建中记录和修改总结落页，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：sessions_status — 该附属代码负责管理 Agent 会话、构建中记录和修改总结落页，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：实现时会用到 [[record_note]]。"} {:block/title "协作：实现时会用到 [[record_note 与 page_tag 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：主要代码单元是 [[start_session]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[start_session]] 会使用这里提供的行为。"}]}
{:page {:block/title "status"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`status` 返回当前构建、审阅、完成门和下一步动作。 它通过统一状态加载先验证固定快照，并在迁移 staging 已被提升时执行受条件约束的输出路径重定位。"} {:block/title "需要修改时：状态字段、下一步决策或完成态目录提升规则变化时，需要修改该函数。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/pipeline.py 第 3246 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/pipeline.py:3246:1)  `scripts/ckb_core/pipeline.py:3246-3256`"} {:block/title "协作：实现时会用到 [[status 与 _replace_output_prefix 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[AutomationTest.event 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests]] 会使用这里提供的行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[LspClient.start 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[MigrationTest]] 会使用这里提供的行为。"} {:block/title "协作：[[MigrationTest 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[create_source_snapshot]] 会使用这里提供的行为。"} {:block/title "协作：[[create_source_snapshot 与 git 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[execute 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event]] 会使用这里提供的行为。"} {:block/title "协作：[[ingest_event 与 default_registry_path 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[load_page_config 与 _merge_known 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main 与 sha256 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（build_runtime_payload 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[main（fake_logseq 测试）]] 会使用这里提供的行为。"} {:block/title "协作：[[module_name 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[package_showcase]] 会使用这里提供的行为。"} {:block/title "协作：[[parse_file]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[prepare_vault 与 install_obsidian 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note]] 会使用这里提供的行为。"} {:block/title "协作：[[record_note 与 page_tag 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[remove]] 会使用这里提供的行为。"} {:block/title "协作：[[remove 与 skill_root 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration]] 会使用这里提供的行为。"} {:block/title "协作：[[render_integration 与 _looks_windows 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine 与 estimated_tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[run 与 CkbError 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[source_files 与 sha256 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session]] 会使用这里提供的行为。"} {:block/title "协作：[[start_session 与 _session_directory 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 汇总了本页。"} {:block/title "协作：[[sync_human_layer]] 会使用这里提供的行为。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 会使用这里提供的行为。"}]}
{:page {:block/title "status 与 _replace_output_prefix 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件实现从范围解析、分段构建、审阅、合并到双知识层投影和最终审计的主流水线。 它统一维护固定快照、页面配置、迁移目录提升后的路径重定位、机器与人类投影及完成状态机。"} {:block/title "需要修改时：构建阶段、审计门、投影格式、完成状态或输出目录迁移契约变化时，需要修改该文件。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/pipeline.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/pipeline.py:1:1)  `scripts/ckb_core/pipeline.py:1-3314`"} {:block/title "内部实现：_replace_output_prefix — 递归改写 JSON 中属于旧输出目录的普通、正斜杠和 URI 编码路径前缀。"} {:block/title "内部实现：_relocate_completed_output — 在 staging 目录被同卷提升后验证本地固定快照，并重定位输出自有 JSON 路径且保留不可变基线原字节。"} {:block/title "内部实现：_load_state — 读取状态、兼容旧 chunks 字段、处理已完成输出重定位，并验证固定快照与页面配置。"} {:block/title "内部实现：_module — 从仓库相对路径提取用于分段和索引的模块名称。"} {:block/title "内部实现：_parse_entry — 解析带语言、路径和限定名的显式入口选择器。"} {:block/title "内部实现：_resolve_entries — 在候选实体中解析入口并对重名情况生成确定性候选结果。"} {:block/title "内部实现：_expand_entries — 按 callers、callees 和深度扩展入口范围并形成一跳边界。"} {:block/title "内部实现：_chunks — 按模块、文件数、实体数和源码大小上限制定可恢复分段计划。"} {:block/title "内部实现：_chunks.flush — 把当前累计文件与实体封装成一个确定性分段记录。"} {:block/title "内部实现：_chunks.units_for_file — 按顶层声明边界把超大文件拆成可独立分段的单元。"} {:block/title "内部实现：_write_review_pack_templates — 为页面说明与附录说明生成逐实体源码证据审阅模板。"} {:block/title "内部实现：_normalize_repo_selector — 规范化 C# 工作区选择器并约束其位于仓库内部。"} {:block/title "内部实现：_resolve_csharp_workspace — 解析显式或自动发现的 C# solution/project 工作区。"} {:block/title "内部实现：_prepare_csharp_restore — 在固定快照中执行受控 C# restore 并记录生成文件清单。"} {:block/title "内部实现：_prepare_csharp_fallback_workspace — 缺少 C# 项目文件时生成有界近似工作区和精度证据。"} {:block/title "内部实现：_rekey_reused_file_parse — 把精确复用的文件解析结果按目标提交重建实体与关系标识。"} {:block/title "内部实现：initialize — 固定 Git 提交、发现范围、解析源码、规划分段并建立初始状态。"} {:block/title "内部实现：_selected_catalog — 从目录、范围和边界清单中返回本次扫描实际选中的事实集合。"} {:block/title "内部实现：_chunk — 按标识读取单个分段并对不存在的标识报告输入错误。"} {:block/title "内部实现：_review_pack — 按标识读取单个审阅包并对不存在的标识报告输入错误。"} {:block/title "内部实现：_invalidate_after_build — 重建后撤销旧投影和完成标记，使后续审计基于最新事实。"} {:block/title "内部实现：build_chunk — 按 syntax、semantics、classify、project 阶段构建或返工指定分段。"} {:block/title "内部实现：_substantive_chinese — 判断审阅字段是否包含足够的中文叙述而不是空泛占位。"} {:block/title "内部实现：_single_chinese_sentence — 验证附录说明为一条简短且含中文语义的句子。"} {:block/title "内部实现：_source_check — 在固定 Git blob 的指定范围内核对实体名称和源码位置。"} {:block/title "内部实现：_partial_fragment_source_errors — 检查超大文件分段后的局部实体范围是否仍与源码一致。"} {:block/title "内部实现：audit_chunk — 执行范围、语法、分类、语义、来源、中文说明和段内链接门。"} {:block/title "内部实现：audit_chunk.gate — 把分段审计的单项结果与证据加入当前审计报告。"} {:block/title "内部实现：review_chunk — 校验旧式逐分段 Agent 审阅文件并登记通过状态。"} {:block/title "内部实现：review_pack — 校验页面或附录审阅包的实体集合、源码位置和中文字段后登记结果。"} {:block/title "内部实现：merge — 合并已经通过的分段事实并解析跨段关系，生成唯一逻辑图。"} {:block/title "内部实现：_repository_name — 从仓库根路径提取适合人类首页使用的项目名称。"} {:block/title "内部实现：_short_code_unit_name — 生成保留类归属且去除多余命名空间的代码单元标题。"} {:block/title "内部实现：_source_role — 依据后缀和路径把源码文件判定为接口、测试或实现。"} {:block/title "内部实现：_human_page_base_title — 根据页面对应的类、函数或文件聚合生成无机器前缀的基础标题。"} {:block/title "内部实现：_assign_human_titles — 为页面分配短标题，并用源码角色和位置确定性消解重名。"} {:block/title "内部实现：_logical_projection — 把完整实体图压缩为受页面配额和关系预算约束的人类导航图。"} {:block/title "内部实现：_logical_projection.new_page — 初始化人类导航页的实体归属、附录、边界和链接容器。"} {:block/title "内部实现：_logical_projection.relation_category — 把底层关系类型归并为人类导航所需的有限关系类别。"} {:block/title "内部实现：_source_manifest — 生成页面到固定源码路径和行范围的可点击来源清单。"} {:block/title "内部实现：_page_sections — 按页面类型与配置返回需要渲染的章节顺序。"} {:block/title "内部实现：_overview_text — 从 Agent 审阅字段组合代码页的中文概览。"} {:block/title "内部实现：_aggregate_overview — 为文件、模块或边界聚合页生成简明中文职责说明。"} {:block/title "内部实现：_canonical_page_context — 生成供机器检索与人类投影共同使用的规范页面上下文。"} {:block/title "内部实现：_logical_context_budgets — 根据模块和任务上限计算每个导航入口的上下文预算。"} {:block/title "内部实现：_relation_phrase — 把确定性关系类型转换为自然中文短语。"} {:block/title "内部实现：_human_relation_sentences — 把压缩后的页面关系渲染为有限数量的中文导航句。"} {:block/title "内部实现：_normalized_edn_document — 把唯一逻辑投影序列化为 Markdown 与 Logseq 共用的规范 EDN。"} {:block/title "内部实现：_render_markdown_page — 按配置章节、双链和源码链接渲染一张人类可读 Markdown 页面。"} {:block/title "内部实现：_logseq_file_graph_config_bytes — 生成 Logseq 文件图谱要求的最小 config.edn 内容。"} {:block/title "内部实现：_install_logseq_file_graph_config — 把最小 Logseq 配置安装到导入根和兼容目录。"} {:block/title "内部实现：_audit_logseq_file_graph_config — 核对 Logseq 配置存在、内容一致且位于正确导入根。"} {:block/title "内部实现：_human_page_filenames — 为人类页面生成稳定、去前缀且无文件名冲突的 Markdown 文件名。"} {:block/title "内部实现：_wiki_document — 生成中文知识库首页、阅读路线、标签说明和范围边界。"} {:block/title "内部实现：_readability_report — 统计标题、属性、标签、源码链接和中文叙述问题并生成可读性审计。"} {:block/title "内部实现：project_markdown — 从逻辑图生成 Markdown/Obsidian 页面、索引、反向链接和规范 EDN。"} {:block/title "内部实现：_logseq — 以受控环境运行 Logseq CLI 并返回原样输出与退出状态。"} {:block/title "内部实现：_logseq_count — 递归读取 Logseq CLI 查询结果中的实体或关系计数。"} {:block/title "内部实现：_logseq_count.visit — 遍历 Logseq 查询返回值并累计目标键对应的整数。"} {:block/title "内部实现：_logseq_count.contains_null_result — 检查 Logseq 查询返回值是否包含空结果。"} {:block/title "内部实现：project_logseq — 导入规范 EDN、验证图谱、导出 SQLite 并核对双投影一致性。"} {:block/title "内部实现：_audit_markdown — 检查 Markdown 双链、反向链接、页面配额、来源字段和归属集合。"} {:block/title "内部实现：audit_global — 重跑分段、审阅、迁移、来源、中文、投影、SQLite 和链接等全局完成门。"} {:block/title "内部实现：finalize — 在全局审计通过后重新读取状态并独占写入机器、人类与总完成标记。"} {:block/title "内部实现：relink_sources — 更新本地可点击源码链接配置并重新执行完整完成门。"} {:block/title "内部实现：build_context — 按模块或入口构造受 token 预算约束的确定性 Agent 上下文包。"} {:block/title "内部实现：run_fast — 实现 run/resume 快速入口，依次推进初始化、首段构建、审阅检查点和完成。"} {:block/title "协作：实现时会用到 [[LspClient.start 与 _version_matches 的协作实现]]。"} {:block/title "协作：实现时会用到 [[audit_migration 与 _entity_key 的协作实现]]。"} {:block/title "协作：实现时会用到 [[create_source_snapshot]]。"} {:block/title "协作：实现时会用到 [[create_source_snapshot 与 git 的协作实现]]。"} {:block/title "协作：实现时会用到 [[execute]]。"} {:block/title "协作：实现时会用到 [[load_page_config]]。"} {:block/title "协作：实现时会用到 [[load_page_config 与 _merge_known 的协作实现]]。"} {:block/title "协作：实现时会用到 [[parse_file]]。"} {:block/title "协作：实现时会用到 [[prepare_vault]]。"} {:block/title "协作：实现时会用到 [[record_note 与 page_tag 的协作实现]]。"} {:block/title "协作：实现时会用到 [[render_integration 与 _looks_windows 的协作实现]]。"} {:block/title "协作：实现时会用到 [[retrieve_machine]]。"} {:block/title "协作：实现时会用到 [[run]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：主要代码单元是 [[status]]。"} {:block/title "协作：实现时会用到 [[sync_human_layer]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[CodeKnowledgeBuilderTests]] 会使用这里提供的行为。"} {:block/title "协作：[[LspClient.start 与 _version_matches 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[MigrationTest]] 会使用这里提供的行为。"} {:block/title "协作：[[MigrationTest 等测试场景]] 会使用这里提供的行为。"} {:block/title "协作：[[add_git_bootstrap_arguments]] 会使用这里提供的行为。"} {:block/title "协作：[[audit_migration 与 _entity_key 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[create_source_snapshot 与 git 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[main（ckb 实现）]] 会使用这里提供的行为。"} {:block/title "协作：[[parser]] 会使用这里提供的行为。"} {:block/title "协作：[[query_graph 与 _networkx_modules 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve 与 _tokens 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[retrieve_machine]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[start_session]] 会使用这里提供的行为。"} {:block/title "协作：[[status]] 会使用这里提供的行为。"}]}
{:page {:block/title "sync_human_layer"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：`sync_human_layer` 是源码中负责生成事实层与中文人类层并核对跨层一致性的命名代码单元。 它在所属模块内执行生成事实层与中文人类层并核对跨层一致性，并把确定结果交给调用方、审计门或投影阶段。"} {:block/title "需要修改时：当生成事实层与中文人类层并核对跨层一致性所依赖的输入格式、排序规则、状态转换或输出契约变化时，需要修改该代码单元。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/knowledge_layers.py 第 126 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/knowledge_layers.py:126:1)  `scripts/ckb_core/knowledge_layers.py:126-181`"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：实现时会用到 [[sync_human_layer 与 _source_manifest 的协作实现]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[status 与 _replace_output_prefix 的协作实现]] 会使用这里提供的行为。"} {:block/title "协作：[[sync_human_layer 与 _source_manifest 的协作实现]] 汇总了本页。"}]}
{:page {:block/title "sync_human_layer 与 _source_manifest 的协作实现"} :blocks [{:block/title "标签：#类型/代码"} {:block/title "页面说明：该文件集中实现可重建事实层、中文人类 vault 镜像与跨层一致性审计。 它是 Code Knowledge Builder 中承载可重建事实层、中文人类 vault 镜像与跨层一致性审计的源码边界，并连接相邻模块或测试。"} {:block/title "需要修改时：当可重建事实层、中文人类 vault 镜像与跨层一致性审计的输入、输出、状态门或验证契约发生变化时，需要同步修改该文件并运行对应测试。"} {:block/title "源码入口：[打开源码：scripts/ckb_core/knowledge_layers.py 第 1 行](vscode://file/E:/knowledge_builder/self-workspace/source/scripts/ckb_core/knowledge_layers.py:1:1)  `scripts/ckb_core/knowledge_layers.py:1-239`"} {:block/title "内部实现：_source_manifest — 该附属代码负责可重建事实层、中文人类 vault 镜像与跨层一致性审计，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：build_facts_layer — 该附属代码负责生成事实层与中文人类层并核对跨层一致性，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：audit_facts_layer — 该附属代码负责执行确定性完整性与来源真实性检查，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_copy_file — 该附属代码负责可重建事实层、中文人类 vault 镜像与跨层一致性审计，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：_generated_files — 该附属代码负责可重建事实层、中文人类 vault 镜像与跨层一致性审计，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：audit_human_layer — 该附属代码负责执行确定性完整性与来源真实性检查，并把结果交给所属页面中的主流程使用。"} {:block/title "内部实现：mirror_note — 该附属代码负责保存实时工作区变化和双链知识记录，并把结果交给所属页面中的主流程使用。"} {:block/title "协作：实现时会用到 [[retrieve_machine 与 estimated_tokens 的协作实现]]。"} {:block/title "协作：实现时会用到 [[run 与 CkbError 的协作实现]]。"} {:block/title "协作：实现时会用到 [[status]]。"} {:block/title "协作：主要代码单元是 [[sync_human_layer]]。"} {:block/title "协作：由 [[AutomationTest.event 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests]] 覆盖相关行为。"} {:block/title "协作：由 [[CodeKnowledgeBuilderTests 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest]] 覆盖相关行为。"} {:block/title "协作：由 [[MigrationTest 等测试场景]] 覆盖相关行为。"} {:block/title "协作：由 [[execute 等测试场景]] 覆盖相关行为。"} {:block/title "协作：[[record_note]] 会使用这里提供的行为。"} {:block/title "协作：可从 [[scripts 职责导览]] 进入本页。"} {:block/title "协作：[[sync_human_layer]] 会使用这里提供的行为。"}]}
{:page {:block/title "tests 职责导览"} :blocks [{:block/title "标签：#类型/职责"} {:block/title "页面说明：这里汇集 tests 范围内彼此协作的类、函数和辅助实现。"} {:block/title "协作：继续浏览 [[AutomationTest.event 等测试场景]]。"} {:block/title "协作：继续浏览 [[CodeKnowledgeBuilderTests 等测试场景]]。"} {:block/title "协作：继续浏览 [[MigrationTest 等测试场景]]。"} {:block/title "协作：继续浏览 [[execute 等测试场景]]。"} {:block/title "协作：继续浏览 [[main 等测试场景（fake_logseq 测试）]]。"} {:block/title "协作：继续浏览 [[main 等测试场景（generate_large_fixture 测试）]]。"} {:block/title "协作：可从 [[source 代码导览]] 进入本页。"}]}
]}
